# CLAUDE.md ― APパートナーズ 契約書管理システム

> このファイルはセッション開始時に自動で読み込まれる「常時オンの薄い層」です。
> **詳細仕様（テーブル定義・CSVマッピング・STEP仕様など）は `docs/SYSTEM_DESIGN.md` にあります。**
> 必要になったときだけ `docs/SYSTEM_DESIGN.md` の該当セクションを読んでください。ここには全体像と運用ルールだけを置きます。

---

## ⚙️ 運用ルール（必ず守る）

1. **1チャット＝4機能まで** で区切る（2026-07-15変更：従来は1機能。チャット引き継ぎ自体のトークンコストと、1本に詰め込んだ場合のコンテキスト劣化コストを天秤にかけ、伊藤さんの判断で4機能まで許容に緩和）。長い1本に詰め込まない（コンテキスト劣化＝忘れの最大要因）。関連性の低い機能を無理に4つ束ねるのではなく、区切りの良いタイミングで新チャットに移ること。
2. **仕様・判断が決まったら、その場で差分だけ追記する**。`docs/SYSTEM_DESIGN.md` を**全文再生成しない**こと（トークン浪費）。確定仕様は該当章を部分編集、判断の経緯は「意思決定ログ」に1行追記。
3. **実装コードを正（source of truth）とする**。設計書とコードがズレたらコードを優先し、ズレは設計書側を訂正する。
4. **コードと設計書の突合（監査）は節目だけ**（機能の区切り・本番リリース前など）。毎回はやらない（重い）。
5. **CSV・Excel等の機密ファイル（個人情報・契約データ）はGit管理対象外**。コミット前に `.gitignore` が効いているか必ず確認（過去に誤アップロード事故2回）。
6. テーブル／ファイルを「無いはず」と判断する前に、**必ず実環境（Supabaseのテーブル一覧・GitHub・ローカル）を確認**する（思い込みによる衝突事故あり）。
7. 「マッピングできてる？」等の確認質問には即答せず、**実データ・実コードを再検証してから回答**する。
8. ユーザー（伊藤さん）はプログラミング知識ゼロ。手順は「VSCodeを開く」から省略せず全手順を書く。
9. **画面に表示する文章は、改行位置を意識して読みやすく配置する**。特に警告・確認メッセージ・バナー等は、意味の切れ目（句点等）で改行し、不自然な位置で折り返さないようにする（2026-07-01決定）。
10. **「未実装だから」を理由に、その場しのぎの最小限対応で済ませない**。関連する新機能に着手する前に、`docs/SYSTEM_DESIGN.md`・過去のトーク履歴（`過去のトーク履歴.docx`）を確認し、想定より大きな設計・仕様がすでに決まっていないか必ず確認する。実装できるのは「計算ロジックなど中身」だけが後回しになる場合でも、**将来の完成形を見据えた構造（骨格・データ設計・UIの出し分け）は今のうちに作っておく**。中身が空でも、後から中身を差し込むだけで済む形にしておくことが目的（2026-07-02決定）。
11. **既存の大きなファイル（目安：数百行以上）を1〜数行だけ直したい時は、伊藤さんに「コードをチャットに貼り付け」ではなく「ファイルそのものをドラッグ＆ドロップでアップロード」してもらう**。貼り付けだとClaudeが全文を手で書き写す必要が生じ、トークン消費・時間・写し間違いリスクが大きい。ファイルをアップロードしてもらえれば、Claudeはコピーコマンドで正確に複製した上でピンポイント編集でき、速く正確（2026-07-02決定・教訓）。
12. **レイアウト・配置・見た目など視覚的な修正は、実装前に必ず方向性を伊藤さんに確認する**。PDF帳票のレイアウト崩れ・要素の位置ズレ・画面のデザイン変更など「見た目」に関わる修正依頼を受けた際、いきなりコードを直してレンダリングテストで確認する、という進め方はしない。先に「原因はこうで、対策としてこういう見た目にしようと思うがどうか」を、可能であればモック（簡易な画像・レイアウト案・文章での配置イメージ）を示して確認を取り、伊藤さんの合意を得てから実装に着手する。合意なく直してテストさせると、期待とズレていた場合に伊藤さんの確認の手間・時間が二重にかかるため（2026-07-09決定・教訓：印鑑位置修正で先に実装→テストしてしまい指摘を受けた）。
13. **伊藤さんがVercelのデプロイ完了を伝えたら、Claudeは自分でブラウザ（Claude in Chrome）とGmail連携を使って実機セルフチェックを行う**。伊藤さんに手作業で確認させるのではなく、Claude自身が①該当ダッシュボードにテストアカウントでログインし、対象の画面操作（承認・PDFプレビュー・署名フロー等、その回の変更内容に応じたもの）を実行し、②必要であれば送信された通知メールをGmail連携（ito@appart.co.jp）で実際に開いて内容を確認する。SELECT（閲覧）だけでなく、承認・保存などの更新系操作も含めて実際に画面から実行して確認すること（2026-07-15の教訓：RLS変更をSELECT確認だけで済ませ、UPDATE時のみ発生する不具合を見逃した）。問題が見つかった場合はその場で原因調査・修正まで行い、伊藤さんには結果（正常だったこと／見つかって直した不具合）を報告する（2026-07-15決定）。
14. **コード修正を行ったら、その回答の中で必ず`git add`・`git commit`・`git push`の3行をそのまま貼り付けられる形で毎回明示する**。「先ほどのコマンドで」「デプロイしてください」等の省略表現で済ませない。git操作は必ず伊藤さんのVSCode等の**ターミナルから**実行してもらう（Claudeがbashサンドボックス経由で直接pushすることはしない。過去にbashのファイルキャッシュ遅延でファイル破損事故が2回発生しているため）。1つの依頼の中で複数箇所を修正した場合も、全ての修正が終わった最後に1回だけ、その時点の全差分をまとめてpushするよう案内する（毎回の小修正のたびに逐一pushを促さない）（2026-07-16決定・教訓：pushの案内を省略し、伊藤さんに「まだpushしてないけど」と指摘された）。
15. **「残りのタスクは？」と聞かれたら、まず下の「🔴 残タスク一覧」章だけを見て即答する**（過去ログを毎回掘り返して調べ直さない）。逆に、①新しいタスク・不具合報告・要望を認識した瞬間、②着手した瞬間、③完了・実機確認まで終わった瞬間、の3つのタイミングでは必ずその場で「🔴 残タスク一覧」を更新する（追加・状態変更・削除）。他のセクション（実装済みタスク一覧・次の優先タスク等の詳細ログ）を更新した「だけ」で残タスク一覧の更新を後回しにしない。この一覧とSYSTEM_DESIGN.md 10章の実際の記録がズレていないか、月1回程度は突き合わせる（2026-07-30決定・教訓：デモ実演で出た指摘⑥〜⑨や追加報告が「次の優先タスク」欄に一度も転記されず、伊藤さんに聞かれるたびにSYSTEM_DESIGN.mdを掘り直す羽目になっていた）。
16. **見た目の修正に限らず、あらゆる機能修正・新規実装は、着手前に必ず修正方針をプロの業務改善責任者／PdM／UI-UXデザイナー目線で提案し、伊藤さんの明示的なOKを得てから実装する**（ルール12の対象を視覚修正だけでなく機能修正全般に拡張。2026-07-30決定）。伊藤さんが「OK」と言うまでコードは書かない。技術調査だけで完結する項目（例：既存実装の有無確認）は、方針提案の前に調査結果を先に共有し、その結果を踏まえて次にどうするかを伊藤さんと決める。
17. **「パターンA／B／C」（雇用契約書＝A・就業条件明示書＝B・兼用＝C）のような、伊藤さんとClaudeの会話の中だけで使う内部的な略称は、画面・メール等ユーザー（管理部・SSC・担当営業・従業員）が実際に目にする箇所には絶対に表示しない**。他の人には「パターンA」が何を指すか伝わらないため（2026-07-31決定）。該当箇所が見つかったら、正式名称（雇用契約書／就業条件明示書／雇用契約書兼就業条件明示書、または既存の`getDocumentLabel()`等の変換関数）に置き換える。あわせて、テスト・実機確認のためにテストデータを作成する際も、就業場所名・部門名など実際の業務データとして画面やメールに表示される項目には「（パターンB）」のような内部呼称を含めないこと（スタッフ氏名を「テスト太郎」等の明らかなダミー名にするのは問題ない。伊藤さんとの区別：氏名の場合はダミーだと一目で分かるが、就業場所名等の項目に紛れ込むと本物の業務データと誤認されうるため）。2026-07-31に発見・修正済みの実例：`app/dashboard/sales/contracts/[id]/page.tsx`・`app/dashboard/ssc/contracts/[id]/page.tsx`の契約詳細画面で「パターン{pattern} / {contractType}」とそのまま表示していた箇所を「雇用区分」のみの表示に修正（書類種別は1つ上の行で正式名称を別途表示済みのため情報は失われない）。
18. **日付・日時を画面・PDF・メールに出力する処理を新規に書く／既存箇所を修正する際は、必ず `lib/dateFormat.ts` の `formatDateJp`（日付のみ・年月日・ゼロ埋め）／`formatDateTimeJp`（日時・年月日 時:分）／`formatPeriodJp`（期間レンジ）を使う**（2026-08-05決定。詳細はSYSTEM_DESIGN.md 10章2026-08-05「日付表記の不統一の解消」参照）。この3関数がアプリ内の日付表記の唯一の正（source of truth）であり、`toLocaleDateString`・`toLocaleString`・`new Date().getFullYear()`等を使った自前の日付整形コードをファイル内にその場で書かない・コピーしない（過去に同じロジックが10箇所以上に重複コピーされ「ハイフン生値／スラッシュ／漢字」の3方式が混在する事態を招いた教訓）。表記は常に漢字表記（例：2026年08月05日）に統一し、ゼロ埋めなし（2026年8月5日）にはしない。例外は2つのみ：①`<input type="date">`等のブラウザネイティブ日付入力欄の値（'YYYY-MM-DD'固定でよい。表示形式の対象外）、②Dateオブジェクトを直接受け取る箇所（期日アラート等）で、文字列用の`formatDateJp`へ`toISOString()`経由で委譲すると「JST 0時=UTC前日15時」変換により日付がずれるため、ローカルのDateから直接組み立てる（`app/dashboard/sales/page.tsx`・`app/dashboard/sales/contracts/[id]/page.tsx`の`formatDate(d: Date)`が実例）。またサーバー側（Vercel・既定UTC実行）から日時（時刻を含む）を扱う場合、`formatDateTimeJp`自体が内部で`Intl.DateTimeFormat`によりAsia/Tokyo基準で計算するため、個別に`{ timeZone: 'Asia/Tokyo' }`等のタイムゾーン指定を追加する必要はない（むしろ二重対応しないこと）。

---

## 🔴 残タスク一覧（伊藤さんが「残りは？」と聞いたら、まずここだけ見る）

> このセクションが唯一の正解。完了したらここから消す、新しく判明したらここに足す、を都度その場で行う（ルール15）。詳しい経緯は各項目末尾のSYSTEM_DESIGN.md参照先へ。

### 2026-08-06デプロイ後・実機確認完了（外部総合品質監査レポート由来・優先度1）
- **【完了・実機確認済み】C-08：社員番号の再利用（退職者番号を新入社員へ再割当）で前任者の書類が新入社員に見える問題を解消**。伊藤さんへ提案した2案（案A＝自動アーカイブ方式／案B＝スキップして手動対応）のうち案A（推奨）でOKを得て実装。当初提案していた「部分ユニークインデックス」方式ではなく、実装時の詳細検討で「アーカイブ時に旧行の`employee_number`自体をリネームして退避する」方式（`staff.archived_at`・`staff.archived_original_employee_number`列を新設）を採用（既存の`.eq('employee_number', X)`系クエリ15箇所超を一切変更せずに旧行を自然に検索対象外にできるため、より低リスクと判断）。StaffExpressスタッフマスタ取込（`app/api/admin/csv-import/route.ts`の`processStaffMasterFile()`）で、同じ社員番号でも氏名が一致しなければ「同一人物の更新」とみなさないよう変更（`lib/staffMasterImportShared.ts`に`normalizeStaffNameForCompare()`＝全角/半角スペース差を吸収する氏名比較ヘルパーを新設）。氏名不一致の場合：①既存行が退職済み（`retired_at`設定済み）→正当な再割当と判断し、旧行の`employee_number`をリネームして退避（`archived_at`記録）した上で新規UUIDの行として登録（前任者の契約・誓約書等の履歴は退避された旧`staff.id`にそのまま残り削除されない。新入社員のマイページは完全に空の状態からスタート）。②既存行が現役（`retired_at`がNULL）→データ異常の疑いとして自動処理せずスキップし「要確認」として報告（人手判断に委ねる）。管理部ダッシュボードのCSVインポート結果表示（`app/dashboard/admin/page.tsx`）に「再割当件数」「要確認件数・詳細（社員番号・旧氏名・新氏名）」を追加表示、`master_imports`テーブルに`reassigned_rows`・`needs_review_rows`・`needs_review_detail`列を新設。ログイン・STEP1検索・更新期限管理等の他のemployee_number参照箇所（15箇所超を監査）は、既存の退職者除外ロジック（`excludeRetiredStaffOr()`）と今回のリネーム方式により追加修正不要と確認済み。**残存リスク**：ローカルCLI用の手動フォールバックスクリプト`scripts/import-master.js`は今回の氏名照合ロジックを含んでいないため、万一これを直接実行すると本対応をバイパスする。実際のインポートは必ずWeb画面（管理部ダッシュボード「CSVインポート」タブ）経由で行うこと。【デプロイ後・実機確認完了・2026-08-06】Claude in Chrome＋Supabase MCPで、①退職済みスタッフと氏名不一致の再割当シナリオ（自動アーカイブ→新規登録）、②現役スタッフと氏名不一致の「要確認」シナリオ（自動処理せずスキップ＋管理部ダッシュボードへの詳細表示）の両方を実データに近いテストExcelで実行し、意図通りの挙動を確認済み。テストデータは確認後すべて削除・DBを元の状態に復元済み。
- **【完了・実機確認済み】C-05：アルバイト誓約書PDFでMIXパターン時に勤務日が最大1件サイレントに消える問題を解消**。伊藤さんへ提案した2案（案A＝入力上限をMIX時9件に制御／案B＝PDFレイアウトを11行対応に拡張）のうち案A（推奨・低リスク）でOKを得て実装。`app/pledge/apply/page.tsx`で、期間指定（`hasPeriod`）が登録済みのときだけ単日登録の上限を10件→9件に動的制御する`singleEntryLimit`を新設（期間指定なし＝単日のみ利用時は従来通り10件のまま）。あわせて、単日を先に10件登録した後で期間を追加登録しようとするケース（合計11件になり得る唯一の経路）を防ぐため、期間登録モーダルの「登録する」ボタンに、単日が既に10件登録済みの場合はブロックしてエラー表示するガードを追加。PDFレイアウト（`lib/pdf/PledgePdf.tsx`のMAX_SCHEDULE_ROWS=10）は変更していない。**実機確認中に「登録するボタンのガードだけでは不十分」という追加不具合を発見・その場で修正済み**：期間登録モーダルの日付・時間欄は入力した瞬間に（「登録する」ボタンを押さなくても、モーダルをキャンセル／×で閉じても）`hasPeriod`へ即座に反映される作りだったため、ボタンのガード自体は正しくエラー表示してモーダルを閉じさせないものの、STEP3画面の背後では「期間1件＋単日10件＝11件」という上限超過状態がそのまま成立してしまい、STEP4以降・提出まで気づかれずに素通りできてしまうことが判明。入力経路によらず必ず検知できるよう、実際の合計行数（`(hasPeriod?1:0)+singleEntries.length`）で判定する`scheduleRowOverflow`を新設し、①STEP3画面に常時表示の警告バナー、②STEP3「次へ進む」ボタンでのブロック、③`handleSubmit`実行時点での最終安全網（万一ここまで来てもSTEP3へ強制的に差し戻す）、の3箇所に反映。構文チェック（`ts.transpileModule`）診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-06】Claude in Chromeで①単日10件を先に登録→期間の日付・時間欄に入力しただけで（「登録する」ボタンを押さず×で閉じても）STEP3画面に常時警告バナー「期間1件＋単日10件で、帳票の上限（合計10件）を超えています。単日を1件以上削除するか、期間の登録を削除してください。」が表示されること、②その状態で「次へ進む」を押すとブロックされSTEP4へ進めないこと、③期間を削除して10件のみ（上限内）に戻すと警告が消え「次へ進む」で正常にSTEP4へ進めること、を確認。テスト申請は「この申請をやめる」で破棄（STEP6提出前のためDBへの残留データなし）。これでC-05は完了。

### 2026-08-06デプロイ後・実機確認完了（C-01・C-02：RLS強化）
- **【完了・実機確認済み】C-01：`staff`テーブルのRLS全開放＋機密列REVOKE漏れを解消**。伊藤さんへ4点セット（①機密列REVOKE＋csv_diff_logsロックダウン、②staff部門スコープRLS、③csv_imports/master_imports管理部・SSC限定、④csv_raw_dataは今回見送り）の方針を提案し「すべて実施する」でOKを得て実装。`staff`の機密5列（`password_hash`・`login_auth_code`・`login_auth_code_expires_at`・`login_auth_attempts`・`login_password_attempts`）を`authenticated`・`anon`双方から遮断。行レベルRLSは既存の`contracts`等と同じ`current_dept_scope()`パターン（管理部・SSCは全件、担当営業は自部門グループのみ、どのロールでも自分自身の行は`email`一致で必ず閲覧可）に変更。**実装中に判明した追加問題**：Supabaseの初期設定でstaffテーブルに`GRANT ALL`（テーブル全体・全列）が既に付与されており、Postgresの仕様上「テーブル全体への権限」が「列単位のREVOKE」を上書きしてしまうため、単純な列REVOKEだけでは機密列を実際には遮断できないことが判明。テーブル全体の権限を一度REVOKEした上で許可する20列だけを明示的にGRANTし直す方式（アローリスト）に修正。この結果、STEP1スタッフ検索（`app/apply/page.tsx`・`app/pledge/apply/page.tsx`）が使っていた`select('*')`が「権限の無い列が1つでもあるとSELECT全体がエラーになる」というPostgresの仕様により機能しなくなる副作用が判明（アプリ側がエラーを握りつぶし空配列にフォールバックする作りだったため、画面上は検索しても0件になるだけで気づきにくい）。機密5列を除いた20列の列名リストを`app/apply/_lib/helpers.ts`に`STAFF_SEARCH_SAFE_COLUMNS`として新設し、該当4箇所の`select('*')`を置き換えて解消。
- **【完了・実機確認済み】C-02：`csv_raw_data`・`csv_imports`・`master_imports`・`csv_diff_logs`のRLS全開放を解消**。`csv_imports`・`master_imports`（インポート履歴）は管理部ダッシュボードでのみ参照されることをコード監査済みのため、SELECTを管理部・SSCロール限定に変更。`csv_diff_logs`は全文検索で使用箇所ゼロと確認済みの未使用テーブルのため、`authenticated`からのアクセスを完全遮断。`csv_raw_data`は部門を示す列が存在せず単純な行ポリシーでは部門制限できないため、今回は現状維持（既知の残課題として記録。次回以降の設計課題）。
　→ 【デプロイ後・実機確認完了・2026-08-06】Claude in Chromeで、①管理部（admin-test）アカウントで`/apply`STEP1検索（999900）が正常にヒットすること、②管理部ダッシュボード「データ登録」→「CSVインポート」タブのインポート履歴（`csv_imports`/`master_imports`参照）が正常表示されること、③担当営業（デモアカウント・北海道営業所）アカウントで`/apply`・`/pledge/apply`双方のSTEP1検索が自部門スコープ内（三國駿様・105026）で正しくヒットすること、④管理部ダッシュボードのヘッダー氏名表示・サマリー集計（自己参照・部門スコープのRLSが絡む箇所）に回帰が無いこと、を確認済み。DBの変更は無し（閲覧のみのテストのため復元作業も不要）。
　→ 詳細はSYSTEM_DESIGN.md 10章2026-08-06「C-01・C-02のRLS強化」参照。

### 2026-08-06実装完了・要デプロイ＋実機確認（C-04・C-06・C-07）
伊藤さんへ「すべて実施する（推奨）」でOKを得て実装（ルール16）。構文チェック（`ts.transpileModule`）は変更した4ファイルすべて診断0件を確認済み。
- **【実装完了・要デプロイ＋実機確認】C-04：CSVインポートの上書き保護のフェイルオープンを解消**。`app/api/admin/csv-import/route.ts`の保護判定クエリ（既存契約からの参照有無チェック）が失敗した場合、従来は`continue`するだけで該当行が`protectedByRawId`へ登録されず、後続の「保護対象でなければ上書き」判定に素通りして実際には上書きされていた（コメントは「安全側」と書いてあるのに実装が逆だった）。`protectionCheckFailedIds`セットを新設し、判定に失敗した行は`upsertBatch`から除外（今回は上書きしない）してエラー件数・エラー詳細に計上する形に修正。新しいUI・新しいカウンタは追加せず、既存の「エラー」件数・エラー内容表示にそのまま乗る設計。
- **【実装完了・要デプロイ＋実機確認】C-07：個別承認時の署名依頼メール送信失敗を可視化**。SSC/管理部の契約詳細画面（`app/dashboard/ssc/contracts/[id]/page.tsx`。管理部にも共有されている画面）の`handleApprove`・`handleForceApprove`、および誓約書側`app/dashboard/ssc/pledges/[id]/page.tsx`の`handleApprove`が、いずれも`notify-sign-request`呼び出しを`fetch(...).catch{}`のみで扱い`res.ok`を見ていなかったため、APIが400/500を返しても承認完了バナーには何も表示されなかった（管理部が既に対応済みだった一括承認のres.okチェックパターンに合わせて統一）。`res.ok`を確認して`notifySignFailed`フラグを立て、承認完了バナー内に赤字の警告（「承認は完了しましたが、送信依頼メールの送信に失敗しました。この契約は『SSC承認済み』のまま止まっています。」）と、C-06で新設した再送ボタンを表示するよう修正。
- **【実装完了・要デプロイ＋実機確認】C-06：署名依頼メールの再送ボタンを新設**。`notify-sign-request`（契約・誓約書の両API）に`trigger=resend`を追加。①ステータスが既に「署名待ち」＝一度は送信できているがリマインドしたい場合はステータス遷移・`sign_requested_at`の更新を行わず同じ内容のメールを再送（初回ログイン未済みの場合は認証コードを新規発行し直し、古いコードは失効させる）、②ステータスが「SSC承認済み」のまま＝C-07で判明した「送信失敗でロールバックされたまま気づかれず止まっていた」ケースの復旧＝通常の承認直後フローと全く同じ判定・処理をそのまま再試行、の2ケースを1つのtriggerで扱う設計。対面・印刷締結パターンの「SSC承認済み」（＝担当営業の説明完了待ちという正常な状態）は対象外とし、二重案内を防止。SSC/管理部の契約・誓約書の両詳細画面に「📩署名依頼を再送する」ボタンを追加（`useConfirm()`の確認ダイアログ付き）。表示場所は2箇所：①承認直後に送信失敗した場合はその場の警告バナー内、②後日ページを開き直した場合のために「処理済みです」表示エリアにも常設。
　→ 詳細はSYSTEM_DESIGN.md 10章2026-08-06「C-04・C-06・C-07の実装」参照。

### 2026-08-06 外部総合品質監査レポート（84件）由来・未着手（方針提案はこれから・ルール16対象）
伊藤さんが入手した外部の「ap-contract-system 本番リリース前 総合品質監査レポート」（★5=Critical 8件・★4=重大17件・★3=中26件・★2=軽微21件、計84件）について、監査側はこれまでの会話・意思決定経緯を知らない状態での指摘だったため、実コード・実DB（Supabase MCP）で1件ずつ裏取りを実施。★5全8件・★4のうち重点4件を検証済み（詳細はSYSTEM_DESIGN.md参照）。**以下は「バグとして実在を確認できたが、まだ修正方針の提案・伊藤さんのOKを得ていない」項目**（C-08・C-05・C-01・C-02・C-04・C-06・C-07は上記の通り対応済みのためこのリストから除外済み）。着手する際は必ずルール16（方針提案→明示的なOK→実装）に従うこと。
- **B-01：`notify-sign-request`に部門スコープチェックが無い**。ログイン済み（どのロール・どの部門でも可）なら任意の契約IDに対して呼び出せ、ステータス遷移とメール送信を発火できてしまう。PDF取得APIにある`created_by_dept_no === staffAuth.deptNo`相当のチェックがこのAPIには無い。
- **B-08：PDFアクセス用の一時トークンと、マイページのパスワード再設定トークンが、同じ秘密鍵（service roleキー）・同じペイロード形式（`id.expiresAt.署名`）を使い回している**（`lib/pdfAccessToken.ts`・`lib/staffResetToken.ts`）。現状は`contracts.id`と`staff.id`が独立したランダムUUIDのため直ちに悪用可能ではないが、トークン種別を区別する仕組み（プレフィックス等）が無い設計上の穴。
- **B-09：9箇所が`staff_roles`ではなく`staff.email = ログインユーザーのメール`で氏名を検索しており、C-03（`staff.email`が`ito@appart.co.jp`固定）のため実質機能していない可能性が高い**（`app/apply/page.tsx`×3・`app/pledge/apply/page.tsx`×2・`app/dashboard/sales/contracts/[id]/page.tsx`・`app/dashboard/sales/pledges/[id]/page.tsx`・`app/dashboard/admin/page.tsx`・`app/api/renewal-candidates/notify-not-renewing/route.ts`）。C-03単独ではなく、この9箇所への副作用として扱う。
- （参考・監査の指摘だが**実際には該当しないと確認済み**）B-02「staff_rolesの行が無い場合にRLSがすり抜ける」：`current_role_name()`等はSQL関数として行が無ければNULLを返すだけで、ポリシーの等号比較はNULLを偽として扱うためフェイルクローズ（拒否）になる。実際のRLSポリシー定義をSupabase MCPで取得し確認済み。対応不要。
- M・L（★3・★2、計約60件弱）は今回未検証。伊藤さんの指示で今回は見送り、次回以降に着手判断。
　→ SYSTEM_DESIGN.md 10章 2026-08-06「外部総合品質監査レポートの検証」参照

### 本番リリース直前に実施予定（今は着手しない・伊藤さん指示でタイミング固定）
- Excel過去データの本番切り替え時取込
- `staff.email`の実メールアドレスへの一括更新（StaffExpress取込時の固定値`ito@appart.co.jp`を解除。「アカウント管理」が正しく登録された後に着手、という条件付き）
- `RENEWAL_NOTIFY_OVERRIDE_EMAIL`環境変数の削除（本番の担当営業に実メールが届くようにする本番切替作業。2026-07-30に伊藤さんの指示でこちらも本番リリース直前に統一）
　→ SYSTEM_DESIGN.md 10章 2026-07-29「CSVインポート（スタッフマスタ）の伊藤さん質問3件」「残タスクの棚卸しと、本番リリース直前作業2件の実行タイミング確定」

### 2026-07-29上司デモ実演フィードバック由来
- ⑦【実装・デプロイ・実機確認まで完了・2026-08-03（同日中に伊藤さんの実機レビューを受けさらに再設計・再実装済み）】更新期限管理タブの情報設計見直し。5タブ構成（仕分け待ち／CSV自動反映／期間のみ更新／期間以外も修正する／CSVインポートを依頼する）で全面改修完了（詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-31「更新期限管理タブの情報設計見直し ― 業務フロー整理と再設計方針確定」および「実装完了」追記参照）。**以下は初回実装時点の設計メモ（タブ名は当時のまま）。実際に伊藤さんが使ってみた上での2回目・3回目のUI/UXレビューを経た最終仕様は、この直後の追記ブロックを参照すること**：
  - 各タブの意味：仕分け待ち＝分類前の待機所（新規検知分は全員ここ）、CSV自動反映＝次契約のCSVが既に見つかっている（内容確認のみでそのまま一括更新可）、期間のみ更新＝次期間を自分で入力して一括更新、修正更新＝期間以外も直す個別申請（`/apply?renewal=`と同じ画面へ）、CSVインポート待ち＝インポート依頼済みで結果待ち（依頼した人だけを表示）。
  - タブ名の横に？アイコンを設置し、タブごとの役割説明をツールチップ表示（`SubTabBar.tsx`拡張）。
  - 仕分け待ちタブは一覧上でチェック→画面下部固定の「振り分ける」ボタンで選んだ振り分け先タブへ一括移動。全タブ共通で「他のタブへ移動」ボタン（誤分類の訂正用。SSCは閲覧のみのため非表示）。
  - CSV自動反映・期間のみ更新タブは、内容確認後にチェックを入れて画面下部固定バーから一括実行（既存`executeBulkApply`を流用）。
  - CSV自動反映・期間のみ更新タブは詳細画面を経由しないため、発行帳票（雇用契約書／就業条件明示書／兼用）を変更したい場合に備え「他のタブへ移動」から修正更新へ切り替え可能。
  - CSVインポート待ちタブは、管理部の取込完了で自動的にCSV自動反映へ移動。クライアント都合で遅い場合のため「修正更新へ切り替え」「期間更新へ切り替え」ボタンも用意（SSCには非表示）。
  - 「更新しないで確定する」ボタン（担当営業・SSC・管理部いずれも操作可、既存仕様を踏襲）を押すと理由入力の上、管理部へメール通知（新規`/api/renewal-candidates/notify-not-renewing`。他の通知と同じメーリングリストマスタ優先・個人宛フォールバック方式）。
  - 各行のレイアウトは契約一覧（`PledgeListSection`等）と同じ「1行目＝氏名＋操作ボタン、2行目以降＝補足情報グリッド」に統一。就業先名は氏名と残日数バッジの間ではなく2行目の社員番号の隣に表示（伊藤さん承認済みモック通り）。
  - 業務委託（＝雇用契約書のみで締結。専用フィールドは無い）・社内案件は、CSV・派遣期間の概念が無いため常に「期間のみ更新」「修正更新」の2択のみ（画面側でdata_source='csv'でない限りCSVインポート待ちの選択肢自体を出さないことで自然に担保）。
  - 社内案件（`work_place='社内'`）は管理部ダッシュボードのみ・かつ`is_internal_approver`権限を持つアカウントのみ閲覧可（`renewal_candidates`のRLSを`contracts`と同じパターンで拡張。SSC・担当営業は常に非表示）。
  - SSC（`canFinalize=false`）は「他のタブへ移動」「CSV再検索」「切替」等の分類変更系ボタンをすべて非表示にし、閲覧＋内容確認＋「更新しないで確定する」のみ操作可能（既存の「SSCは閲覧のみ」合意を踏襲）。
  - 【2026-07-31・2回目・3回目のUI/UXレビューを受けた再設計・実装・デプロイ・実機確認まで完了・2026-08-03】伊藤さんが実際に使ってみた上での指摘（タブの？アイコンが目立たない・分かりづらい／就業場所名が地味で見づらい／雇用期間と派遣期間を自〜至で分けて見たい／CSVインポート待ちを選んだ時の挙動が不明瞭／各種ボタンがリンクっぽくてダサい／「業務委託」という表示が実在するのか疑わしい、など）に対応：
    - タブ名を「修正更新」→「期間以外も修正する」、「CSVインポート待ち」→「CSVインポートを依頼する」に改名（`TAB_DEFS`の`label`のみの変更。内部識別子`edit`・`import_wait`は無変更）。「CSVインポートを依頼する」を選んで振り分けると、選んだ直後に実際に管理部へインポート依頼が送信される（受動的に待つのではなく能動的な依頼であることが伝わるよう明文化）。
    - タブの？アイコンは、白丸バッジ（常に高コントラストで視認可能）＋タブ自身の色を濃くした同系色の文字、という配色に変更（`SubTabBar.tsx`に`helpColor`追加）。単色を全タブに強制すると却って合わないという指摘を受け、タブごとに調和する配色にした。
    - 一覧カードに「就業場所住所」を追加表示するため`renewal_candidates.work_location_address`列を新設（`work_location_name`と同じタイミング＝`syncCandidates()`実行時に前回契約の`input_data.fields.workLocationAddress`からスナップショット。導入時に既存行への一度きりのバックフィルも実施。DB関数`get_latest_contracts_for_renewal()`も同項目を返すよう再作成）。就業場所名・住所は地図ピンアイコン付きの専用ブロックに独立させ、社員番号等と並ぶ薄いグレーの行からは削除。
    - 雇用期間・派遣期間は「至」だけでなく自〜至の範囲を年月日表記（例：2026年04月01日）で分けて表示し、「（現在）」ラベルを付けて前回契約の情報であることを明示。書類種別（雇用契約書／就業条件明示書／兼用）に応じて該当する期間だけを出し分け（全タブ共通の`renderSecondaryGrid`に集約）。
    - 「契約内容をすべて確認」（前回契約のSTEP項目を全項目表示する読み取り専用ポップアップ。実体は既存の`RenewalContractConfirmModal.tsx`をそのまま流用）へのリンクを、従来存在しなかった仕分け待ち・期間のみ更新・CSVインポートを依頼するの3タブにも追加し全タブ共通化。このポップアップの表示項目は意図的に絞り込んだ既存仕様（給与は基本給等7項目のみ・交通費や保険や備考欄は対象外）のまま維持することを伊藤さんと確認済み（STEP8の完全な複製ではない）。
    - CSV自動反映タブの「派遣先変更のため手入力に切替」独立ボタンを廃止し、「他のタブへ移動」で「期間のみ更新」を選んだ場合だけ理由入力を挟む形に統合（`manual_override_reason`の監査ログ自体は維持）。
    - 「更新しないで確定する」ボタンは「更新しない」に短縮（確認ダイアログ・確定後バッジの文言も統一）。
    - 期間のみ更新タブ（兼用の場合）の入力欄ラベルを「派遣期間の開始日／終了日」「雇用期間の開始日／終了日」に変更し、どちらの期間を入力しているか明確化（既存の「派遣期間を入力すると雇用期間へ自動コピー」ロジック自体は無変更）。
    - レビュー対応の過程で「業務委託」という雇用形態・就業場所区分は`app/apply/_lib/helpers.ts`のどこにも実在しない（雇用形態は正社員／有期契約／無期契約／アルバイトの4種、就業場所区分は現場／社内の2種のみ）ことを実コード確認で判明。モック上の誤表記であり実装には反映していない。
    - 【デプロイ後・実機確認完了・2026-08-03】Claude in Chromeで管理部（管理部テスト）・担当営業（山田太郎・北海道営業所）の2ロールにログインし確認。？アイコンの配色（開発者ツールで実際のcomputed colorを確認）、就業場所名・住所ブロック、雇用/派遣期間の年月日表示・書類種別ベースの出し分け、「契約内容をすべて確認」ポップアップ（仕分け待ちタブから開けること・表示内容が絞り込み版のまま正しいこと）、CSV自動反映タブの「他のタブへ移動」→「期間のみ更新へ」で理由入力ボックスが開くこと、を確認。実際に1件（関谷綺菜様・104747）を仕分け待ち→期間のみ更新へ振り分けて動作確認し、確認後に仕分け待ちへ戻し入力した検証用日付もリセット済み（DBに変更は残っていない）。コンソールエラーも無し。
- ⑧【2026-07-30訂正】契約状況モニタリングの「担当営業に確認依頼」メール送信：芳川さんは「未実装」と明言していたが、実コード調査の結果、ボタン→API（`/api/contract-monitoring/notify`）→送信関数（`sendContractMonitoringFollowupMail()`）→実際のメール送信、まで2026-07-23時点で実装済みと判明（送信失敗時にエラーを握り潰す作りにもなっていない）。芳川さんが「未実装」と感じた理由は不明（権限不足での401、`RENEWAL_NOTIFY_OVERRIDE_EMAIL`によるテスト用宛先上書きに気づかなかった等の可能性）。「宛先を個人メールから部門メーリングリスト宛に変更すること」自体は下記の通り実装完了（要デプロイ＋実機確認）。芳川さんへの「未実装」自体の再確認は伊藤さんが別途行う。
- ⑨【実装完了・実機確認済み・2026-08-03】契約状況モニタリングを担当営業側のダッシュボードにも表示：伊藤さんとの検討の結果「閲覧のみ・自部門のみ」で確定（管理部は従来通り全社分＋対応状況変更＋確認依頼ボタン、変更なし）。DB関数`get_contract_monitoring_status()`を修正し、呼び出し元が担当営業ロールの場合のみ`current_dept_scope()`で自部門グループに絞り込み（管理部・SSC等は従来通り全社分）。`contract_monitoring_actions`テーブルのSELECTポリシーに担当営業（自部門のみ）向けの行を追加（INSERT/UPDATEは引き続き管理部専用）。`ContractMonitoringSection.tsx`に`readOnly`propを新設し、担当営業向けには「対応状況」を非活性バッジ表示に、「担当営業へ確認依頼」ボタンは非表示に変更（自分宛に依頼を送る形になり意味が無いため）。`app/dashboard/sales/page.tsx`の更新期限管理タブに3つ目のサブタブとして追加（`admin/page.tsx`と同じ構成）。【デプロイ後・実機確認完了・2026-08-03】Claude in Chromeで担当営業（山田太郎・北海道営業所）アカウントにログインし、更新期限管理タブの「契約状況モニタリング」サブタブが自部門（三國駿様・105026のみ）に絞り込まれて表示されること、「対応状況」が非活性バッジ表示のみ（クリックできない）で「担当営業へ確認依頼」ボタンが表示されないこと、コンソールエラーが無いことを確認。続けて管理部（管理部テスト）アカウントでも同タブを確認し、従来通り全社分10件が表示され、「担当営業へ確認依頼」ボタン・対応状況の変更ボタン（未着手／依頼済み／対応中／解消）とも変わらず操作可能でコンソールエラーも無いことを確認（readOnly分岐によるリグレッション無し）。DBの変更は無し（閲覧のみのテストのため復元作業も不要）。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「契約状況モニタリングを担当営業ダッシュボードにも追加（閲覧のみ・自部門のみ）」参照

### 2026-08-03実装完了・実機確認済み
- **`periodReady()`・期間のみ更新タブの派遣期間判定が書類種別とズレている**（修正済み）：⑦の実機確認中に発見（雇用契約書のみの契約＝関谷綺菜様・104747で、前回契約の`input_data.fields.dispatchStart/dispatchEnd`にCSV由来の残骸が残っていたため、本来不要な「派遣期間」入力欄が表示され「兼用のため自動で揃えています」という誤った文言も出ていた）。書類種別（document_type）を正として「雇用期間／派遣期間のどちらが必要か」を判定する共通関数`getDocumentPeriodFlags()`を`useRenewalCandidates.ts`に新設し、①`periodReady()`（一括更新チェックボックスの有効化・`executeBulkApply()`実行直前の再チェック）、②`RenewalManagementTab.tsx`の`renderPeriodOnlyRow`（`hasDispatch`/`hasEmploy`＝入力欄の出し分け）、③同ファイルの一括申請確認ダイアログの期限プレビュー表示、の3箇所を統一（`renderSecondaryGrid`は2026-07-31時点で既に書類種別ベースだったため、そちらに合わせる形）。あわせて`executeBulkApply()`のpayload組み立てに、新規契約の書類種別に応じて不要な期間フィールド（employStart/employEnd／dispatchStart/dispatchEnd）を明示的にnullへ揃える処理を追加（上流の経緯を問わず、実際にcontractsへ書き込む直前の1箇所で整合性を担保する恒久対応）。document_typeが欠落した古いデータに当たった場合は、安全のため旧来の「フィールドの有無」判定に自動フォールバックする。デプロイ後、Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①関谷綺菜様（104747・雇用契約書のみ）を仕分け待ちから期間のみ更新へ振り分け、派遣期間の入力欄が表示されず「雇用期間」入力欄のみになること・誤った文言が出ないことを確認→確認後「他のタブへ移動」で仕分け待ちへ戻しDBを元の状態に復帰、②CSV自動反映タブで永井優大様（104010・パターンA/雇用契約書のみ）が雇用期間のみ表示、尾方文音様（104533・パターンC/兼用）が雇用期間・派遣期間の両方を表示し回帰が無いこと、を確認。コンソールエラーは拡張機能由来のもの3件のみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「更新期限管理タブ再設計の実機確認」「periodReady()・期間のみ更新タブの派遣期間判定を書類種別ベースに修正」参照

### 2026-08-03実装完了・実機確認済み（更新期限管理カードのUI改善・3ブロック化）
- **カードレイアウトの改修（3ブロック化）**：伊藤さんの実機レビュー指摘（雇用期間が「－」表示になる／「内容を確認」がテキストリンクでダサい／「他のタブへ移動」と「更新しない」の縦位置がズレている）を受け、プロの業務改善責任者／PdM／UI-UXデザイナー目線での検討・伊藤さんとの往復を経て方針確定・実装。「契約一覧（`app/dashboard/sales/page.tsx`等）と同じレイアウトに寄せてはどうか」という伊藤さんの提案に対し、契約一覧は「一覧で見て→詳細画面を開いて操作する」設計だが更新期限管理タブは「一覧から直接チェック・入力・タブ移動までできる」ことこそが存在意義（7/31再設計・8/3レビューの積み重ね）であるため、レイアウトを丸ごと合わせる（詳細を見るボタン1つに集約する）ことはせず、**見た目の言葉遣いだけ契約一覧に揃える折衷案**で実装。実装内容：①DB（`renewal_candidates.contract_start_date`列を新設・`get_latest_contracts_for_renewal()`関数を再作成・既存行バックフィル済み）で正社員・無期契約の契約条件適用開始日を取得できるようにし、`RenewalManagementTab.tsx`に`formatEmployPeriodDisplay()`を新設。雇用期間が無い場合、契約一覧が既に使っている「期間の定めなし」という表現をそのまま流用（`〜期間の定めなし`。新しい言い回しは作らない）。②書類種別を色付きチップ表示（`bg-[#EAF1FF] text-[#2F5FD0]`の丸みのあるバッジ）に変更。③カードを3ブロックに再構成：`renderRowHead()`を全面改修し、残日数バッジを氏名の上に独立配置、「一括申請/一括更新に含める」チェック（バッジのすぐ右、`text-[13px] font-semibold`で従来より目立つサイズに）を左側に、「他のタブへ移動」「更新しない」を右上（`cornerSlot`引数）に分離。ただし位置は目立たせても見た目（文字サイズ・色）は従来通りの控えめな薄いグレーのテキストのまま維持（契約一覧の「詳細を見る」ボタンと同じ位置に濃い色のボタンを置くと誤タップ誘発リスクがあるため。伊藤さん確認済み）。④`renderSecondaryGrid()`に`confirmSlot`引数を追加し、就業場所名・住所（青背景ブロック）の右端に「内容を確認」ボタン（既存の枠付きピルボタン様式`rounded-full border border-[#D0DAF0]`に統一）を統合配置。従来テキストリンクだった`renderConfirmLink()`・CSV自動反映タブ独自の「内容を確認/内容を閉じる」トグル・修正更新タブの「契約内容をすべて確認」ボタンの3種類をすべてこの統一スタイル・統一位置に集約（全5タブの行レンダラーで共通化）。【デプロイ後・実機確認完了・2026-08-03】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①仕分け待ちタブ（長谷川将之様）で青チップ「雇用契約書」・青背景ブロック右端の「内容を確認」ピルボタンを確認、②CSV自動反映タブで永井優大様（104010・パターンA正社員）が「雇用期間（現在）：2026年04月02日 〜 期間の定めなし」と正しく表示され「－」が解消されていること、尾方文音様（104533・パターンC兼用）が雇用期間・派遣期間の両方を正しく表示すること、③永井優大様の行で「内容を確認」→「内容を閉じる」のトグル・CSV差分表示・埋め込み済みの「契約内容をすべて確認」ボタンが従来通り動作すること、④関谷綺菜様（104747）を仕分け待ち→期間のみ更新へ振り分け、新しい3ブロックレイアウト（残日数バッジ横のチェック「一括更新に含める」の強調表示・右上の「他のタブへ移動」）でも派遣期間の入力欄が出ずperiodReady()修正の回帰が無いことを確認→確認後「他のタブへ移動」で仕分け待ちへ戻しDBを元の状態に復帰（仕分け待ち9件・期間のみ更新0件に復帰したことを確認）。コンソールエラーはブラウザ拡張機能由来の10件のみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「更新期限管理カードのUI改善方針（契約一覧との書式統一・3ブロック化）」「実機確認」参照

### 2026-08-03実装完了・実機確認済み（3ブロック化の追加レビュー6件）
上記3ブロック化の実機確認後、伊藤さんの追加レビュー指摘6件に対応。
- **①縦位置ズレ（再指摘・2回目）**：`renderRowHead()`の外側フレックスが`items-start`（上端揃え）だったため、左の残日数バッジ（丸ピルで上下に余白があり実際の高さが高い）と右の「他のタブへ移動」「更新しない」（プレーンテキストで高さが低い）の上端だけ揃い、右側が浮いて見えていた。`items-center`（縦中央揃え）に変更したが、伊藤さんの再指摘でさらに「他のタブへ移動」と「更新しない」の2つの間にも1〜2px程度の縦ズレが残っていた。Claude in Chromeで両ボタンの`getBoundingClientRect()`を実測し原因を特定：flexboxの仕様上「flexコンテナの直接の子要素はblock化される」が、「他のタブへ移動」だけドロップダウン位置決め用の`<div className="relative">`に包まれておりflexコンテナの直接の子ではなかったため、button自身がinline-blockのままレイアウトされ、直接block化されている「更新しない」とは箱のサイズ計算方法が微妙に異なっていたことが判明。ラッパーdivを`relative inline-flex items-center`にしてbutton自身をこのdivのflexアイテムとしてblock化させることで解消（`MoveToOtherTabMenu`関数）。
- **②「派遣期間（現在）」の「雇用期間と同一」表示を廃止**：雇用期間と派遣期間が同じ日付の場合に省略表示していたが「分かりにくい」との指摘を受け、常に実際の日付レンジ（`formatPeriodJp()`）を表示するよう変更。
- **③「契約条件適用開始日」の文言を再訂正**：同日先の3ブロック化実装では契約一覧の既存表現「期間の定めなし」をそのまま流用し`○○年○○月○○日 〜 期間の定めなし`としていたが、伊藤さんの追加レビューで「契約条件適用開始日であることを明示すべき」との指摘を受け`契約条件適用開始日　○○年○○月○○日 〜`に再修正（`formatEmployPeriodDisplay()`）。
- **④「内容を確認」パネル・「契約内容をすべて確認」ポップアップ両方の雇用期間「－」バグを修正**：一覧の「雇用期間（現在）」は`formatEmployPeriodDisplay()`（契約条件適用開始日へのフォールバックあり）を使っていたが、「内容を確認」を押すと開くCSV差異パネル（`RenewalManagementTab.tsx`の`renderCsvDiff()`）と、「契約内容をすべて確認」ボタンで開く別ポップアップ（`RenewalContractConfirmModal.tsx`）はどちらも生の`employ_start_date`/`employ_end_date`をそのまま独自の`formatPeriod()`に渡していたため、正社員・無期契約で常に「－」表示になっていた（同じバグが2箇所に重複して存在）。`formatEmployPeriodDisplay()`を`useRenewalCandidates.ts`へ共通ヘルパーとして集約し両ファイルから共用する形に統一。あわせて両ファイルの日付表記（従来は独自の生ハイフン表記`2026-05-01`）も、タブ内の他の表示と揃うよう共通ヘルパー`formatDateJp()`/`formatPeriodJp()`（年月日表記）に統一。指摘のきっかけとなった1画面だけでなく、同一バグ・同一表記ゆれを抱えていたもう1画面も一緒に修正（伊藤さんに範囲確認済み・両方まとめて対応を選択）。
- **⑤取り消し線の削除・「前回」→「原契約」への統一**：上記2箇所の差異テーブルで、変更前の値に付けていた取り消し線（`line-through`）を「見づらい」との指摘で削除。あわせて見出し「前回」を、一覧の「（現在）」表記と揃うよう「原契約」に統一（`RenewalContractConfirmModal.tsx`は全STEPセクション共通で使う見出しのため、雇用期間・派遣期間だけでなく全セクションに適用）。
- **⑥「契約内容をすべて確認」ボタンの位置変更**：従来はCSV差異パネル（`renderCsvDiff()`）の冒頭（左上）にあったが、このパネルを開くトリガーボタン「内容を確認」は就業場所ブロックの右端にあり、開いた直後に視線がパネル左上に移る動線になっておらず気づきにくいとの指摘。差異の内容を確認し終えた後の「次のアクション」として自然な位置＝パネル最下部（区切り線の下）に移動。
- **テストデータ投入**：期間のみ更新・期間以外も修正する・CSVインポートを依頼するの3タブが空で動作確認できないとの指摘を受け、仕分け待ちの実データ3件を振り分け。当初選んだ鈴木駿様（104291）→期間のみ更新は、実機確認時に画面から消えてしまう不具合に見えたが、調査の結果、鈴木駿様は`staff.retired_at`が過去日（2025-11-29）の退職済みスタッフであり、既存の「退職者除外」ロジック（`fetchCandidates()`の現役スタッフ絞り込み）により意図通り一覧から除外されていたことが判明（今回のコード修正によるバグではなく、テストデータ選定のミス）。104291を仕分け待ちへ戻し、代わりに在籍中の岩城蒼太様（104018）→期間のみ更新に差し替え。最終的な投入内容：岩城蒼太様（104018）→期間のみ更新、山﨑怜菜様（105180）→期間以外も修正する、室武隼様（105235）→CSVインポートを依頼する。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「更新期限管理カード3ブロック化・追加レビュー6件」「実機確認」参照

### 2026-08-03実装完了・実機確認済み（更新期限管理タブ「他のタブへ移動」の設計見直し4件）
伊藤さんの実機確認中の質問（「CSVインポートを依頼するに移動したら自動で依頼になってるんだっけ？」「他のタブへ移動で確認メッセージが出ないのはどうなの？」等）をきっかけに発見・整理した4件。プロの業務改善責任者／PdM／UI-UXデザイナー目線での確認（ルール16）を経て伊藤さんの明示的な回答を得た上で実装。
- **①CSVインポート依頼が経路によって送信されない実害バグ（修正済み）**：仕分け待ちタブ下部の「振り分け先ラジオ＋振り分けるボタン」経由なら`requestCsvImport()`が呼ばれ実際に管理部へ依頼メールが送信されるが、各行右上の「他のタブへ移動」ドロップダウンから「CSVインポートを依頼する」を選んだ場合は`setRenewalTab()`でタブを書き換えるだけで実際の依頼は送信されない不具合があった（仕分け待ち・CSV自動反映・期間のみ更新・期間以外も修正するの4タブすべてで共通の問題）。伊藤さんの整理（「CSVインポートを依頼するタブに入ったものに対し、CSVインポートを依頼するタブ内で依頼するという手法がベスト。仕分け待ち等はあくまで箱を移し替える入口」）に基づき、既存の`requestCsvImport()`（依頼の保存→成功したら`renewal_tab='import_wait'`へ移動、まで一体で行う設計）を「CSVインポートを依頼するタブへ入るための唯一の入り口」と位置づけ、新設した共通関数`handleMoveToTab()`で、移動先が`import_wait`の場合は必ずこれを通す形に統一（`setRenewalTab`という汎用的な箱移動処理に依頼送信ロジックを混ぜ込む案は伊藤さんの指摘で不採用）。
- **②③確認ダイアログの追加（副作用のある操作のみ・伊藤さん確認済み）**：メール送信という外部への副作用を伴う「CSVインポートを依頼する」への移動時のみ、既存の共通コンポーネント`useConfirm()`（他画面の一括承認等で使用中のものを再利用）で確認ダイアログを追加。単純な仕分け直し（期間のみ更新⇔期間以外も修正する等、副作用のない移動）には確認を付けず、従来通り即時移動でテンポを保つ。「他のタブへ移動」ドロップダウン経由（`handleMoveToTab`）と、仕分け待ちタブの「振り分ける」ボタン経由（複数件を一括処理する`handleTriageExecute`。選択の中にimport_wait対象が含まれる場合のみ件数入りの確認文言を表示）の両方に適用。
- **CSVインポートを依頼するタブの重複ボタン整理（伊藤さん確認済み・推奨案採用）**：「期間以外も修正するに切替」「期間のみ更新に切替」の2ボタンが、同じ行右上の「他のタブへ移動」ドロップダウンと機能が完全に重複していたため廃止し「他のタブへ移動」に一本化。「CSVを再検索」はタブ移動ではない別機能のため維持。
- **④「へ」「に」の不統一（伊藤さん承認済み）**：「他のタブへ移動」ドロップダウンの選択肢が、「〜する」で終わる項目名（期間以外も修正する・CSVインポートを依頼する）だけ助詞が「に」になり他は「へ」になるロジック（`endsWith('する')`判定）のため、リストの中で1項目だけ語尾が違って見えていた。助詞を廃し「→ タブ名」という矢印表記に統一（既存の「更新申請する →」等でも使われている矢印表記と揃える）。あわせて折り返し防止のため`whitespace-nowrap`を付け、最長の項目名「→ CSVインポートを依頼する」でも収まるようドロップダウン幅を`w-44`→`w-60`に拡大。【デプロイ後・実機確認完了・2026-08-03】Claude in Chromeで管理部（管理部テスト）アカウントにログインし確認。①長谷川将之様（100531）の行で「他のタブへ移動」ドロップダウンを開き、4項目すべてが「→ タブ名」の統一形式で折り返さず表示されることを確認、②「→ CSVインポートを依頼する」を選択→確認ダイアログ（「選ぶと同時に管理部へ実際の依頼が送信されます。よろしいですか？」）が表示されることを確認、③「キャンセル」を押すと何も変わらないこと（仕分け待ちの件数・行とも変化なし）を確認、④再度実行し「依頼する」を押すと実際に`requests`テーブルへ`csv_import`の依頼レコードが1件作成され（Supabase MCPで直接確認）、仕分け待ち→CSVインポートを依頼するへ正しく移動することを確認（バグ修正の核心部分）、⑤移動後の行に「CSVを再検索」ボタンのみ表示され、従来あった「期間以外も修正するに切替」「期間のみ更新に切替」の重複ボタンが消えていることを確認。テスト用に作成した依頼レコードは削除、対象スタッフは仕分け待ちへ戻しDBを元の状態（更新期限対象9件・他0件）に復帰済み。コンソールエラーはブラウザ拡張機能由来のもののみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「更新期限管理タブ『他のタブへ移動』の設計見直し」「実機確認」参照

### 2026-08-03実装完了・実機確認済み（誤操作・誤入力リスクの再監査2件）
伊藤さんの指示「振り分けるボタンを押した際も確認メッセージ必要じゃない？誤った内容の入力やクリックを想定してください」「他にも該当するところない？ちゃんとチェックして」を受け、`RenewalManagementTab.tsx`全体・関連ファイル（`ContractMonitoringSection.tsx`・`WageRevisionSection.tsx`）を対象に確認メッセージ不足・入力チェック不足を再監査。新たに2件を発見・修正。【実機確認完了・2026-08-03】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①長谷川将之様・山﨑怜菜様を仕分け待ちタブでそれぞれ「期間のみ更新」「期間以外も修正するに」選択（CSVインポートを依頼するを含まない組み合わせ）→「振り分ける」を押すと「選択した2件を振り分けます。・期間のみ更新：1件／・期間以外も修正する：1件」という内訳付き確認ダイアログが表示されること、「キャンセル」で何も変わらないこと（Supabase側で変化なしを確認）、「実行する」で実際に該当タブへ移動すること、②長谷川将之様を期間のみ更新タブで雇用期間の終了日を開始日より前（2026/06/01、開始日2026/07/01）にすると、赤字で「終了日は開始日以降の日付にしてください。」と表示され「一括更新に含める」チェックボックス・実行ボタンとも無効化されること、を確認。確認後、テストで変更したデータはすべて元の状態（仕分け待ち9・CSV自動反映2・他0件）に復元済み。
- **①仕分け待ちタブ「振り分ける」ボタンの確認漏れ**：従来は選択の中に「CSVインポートを依頼する」が含まれる場合のみ確認ダイアログを表示し、「期間のみ更新」「期間以外も修正する」だけの選択では確認なしに即実行していた。複数件をラジオボタンで選んでから最後に1回だけ実行するこのボタンは、選択漏れ・選択し忘れの誤クリックの影響範囲が1件ずつの「他のタブへ移動」より大きいため、内容に関わらず必ず内訳（「・期間のみ更新：◯件」等、何件がどこへ振り分けられるか）付きの確認ダイアログを表示するよう`handleTriageExecute()`を修正。CSVインポートを依頼する対象が含まれる場合は、その件数について管理部へ実際の依頼メールが送信される旨も文中に明記。単純な1件ずつの「他のタブへ移動」（副作用のない移動）は2026-08-03の合意通り確認なしのまま据え置き。
- **②「期間のみ更新」タブの日付入力に前後チェックが無かった不具合**：開始日・終了日を手入力する際、終了日が開始日より前でも保存・「一括更新に含める」チェック・実行前確認パネル・実行直前の最終チェックのいずれでも止まらず、そのまま実際の契約データとして登録されてしまう作りだった（`/apply`のSTEP3期間入力には以前から同種の「終了日は開始日以降の日付にしてください」チェックが存在しており、更新期限管理タブだけこの基本チェックが漏れていた）。`useRenewalCandidates.ts`に`isPeriodOrderValid(start, end)`を新設し`periodReady()`に組み込むことで、日付順が逆の場合は「一括更新に含める」チェックが自動的に無効化されるようにした（`periodReady()`はチェックボックス制御・一括実行対象の絞り込み・実行直前の最終再チェックの3箇所で共用しているため、1箇所の修正で全経路に反映される）。あわせて、なぜチェックできないのか伊藤さんにも分かるよう、該当する日付欄の下に「終了日は開始日以降の日付にしてください。」という赤字の警告文をその場で表示するようにした（派遣期間欄・雇用期間欄それぞれに対応）。
- 再監査で確認し「問題なし」と判断した箇所（変更なし）：「更新しない」ボタン（理由入力必須＋警告文＋確定/キャンセルの2段階で保護済み）、CSV自動反映タブの手入力切替（理由入力必須＋確定/キャンセルで保護済み）、CSV自動反映／期間のみ更新タブの「一括実行」ボタン（対象者ごとの内訳を示す確認パネルが既にあり良い作り。今回の②の修正の恩恵を受ける）、契約状況モニタリングの「担当営業へ確認依頼」（`useConfirm()`による確認ダイアログが既にあり問題なし）、最低賃金改定対応の「修正して再申請する」（申請画面へ遷移するだけでデータ変更を伴わないため確認不要）。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「誤操作・誤入力リスクの再監査（振り分けるボタン確認・期間のみ更新タブの日付前後チェック）」参照

### 2026-08-03実装完了・実機確認済み（更新期限管理タブに残っていた禁止済みのブラウザネイティブalert()2件）
上記②の実機確認作業中、日付の前後チェックをテストする過程で誤って壊れた日付（年が「0026」等）を入力してしまい保存に失敗した際、ブラウザネイティブの`alert()`ポップアップが実際に表示されてしまった（伊藤さんが画面越しに気づき指摘。「それは禁止しています」）。調査の結果、`useRenewalCandidates.ts`に2箇所、2026-07-22の「ブラウザネイティブalert/confirmの全体置き換え」作業（対象40箇所）から漏れていた古い`alert()`がそのまま残っていたことが判明（`updateCandidate()`の保存失敗時、`requestCsvImport()`の依頼送信失敗時の2箇所。この更新期限管理タブ自体は2026-07-14〜31にかけて実装された機能のため、7/22の全体置き換え時点でこのファイルが対象から漏れていたと考えられる）。他画面と同じ確立済みの置き換え先である`useToast()`の`showError()`（画面右上に一時表示するトースト通知）に統一。`useRenewalCandidates()`フック内で`useToast()`を呼び出し、2箇所とも`alert(...)`を`showError(...)`に置き換え。あわせて他に同種の漏れが無いか`app/`配下を`alert(`で全文検索し、コメント内の記述以外に実際に生きているものが無いことを確認済み（`ConfirmDialog.tsx`内の`window.confirm()`はProviderが無い場合のフォールバック専用として意図的に残しているもの）。
同じ全文検索で、今回のスコープ外の`lib/useSessionCollisionGuard.ts`にも生きた`alert()`が1件見つかった（詳細・対応方針は直後の「未着手」項目を参照）。
【デプロイ後・実機確認完了・2026-08-03】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、長谷川将之様（100531）を一時的に「期間のみ更新」タブへ移動して確認。①デプロイ済みJSバンドルを直接フェッチし、旧`alert(...保存に失敗`パターンが存在しないこと・`showError`と保存失敗メッセージが同じチャンク内に共存していることを確認、②実際に日付欄へ不正な値（2026-02-30）を投入して本物の保存エラーを発生させ（コンソールに`更新候補の保存エラー`のログが出ることで実際にエラーパスを通ったことを確認）、以前のようにブラウザがブロックされる（=alertが表示される）ことなく後続の操作が問題なく続行できることを確認（トースト自体は数秒で自動的に消えるため目視は間に合わなかったが、ブロッキングが起きないことが根本の確認点）。確認後、日付を元の値（2026-09-30）に戻し、対象スタッフも仕分け待ちへ復帰（更新期限対象9件・CSV自動反映2件・他0件に復元）。
　→ SYSTEM_DESIGN.md 10章 2026-08-03「更新期限管理タブに残っていた禁止済みalert()の発見・修正」参照

### 2026-08-04実装完了・実機確認済み
- **`lib/useSessionCollisionGuard.ts`に残っていた禁止済みのブラウザネイティブalert()を修正**：セッション衝突検知機能（別タブ・別端末で別アカウントにログインされたことを検知し、安全のため強制ログアウトする機能。2026-07-22実装）の中に残っていた生きた`alert()`（`onAuthStateChange`のハンドラ内。強制ログアウト理由をユーザーに伝える箇所）を、他画面と同じ`useToast()`の`showError()`に置き換えた。着手前に懸念していた「強制ログアウト直後の画面遷移とのタイミング」を確認した結果、`ToastProvider`はルートレイアウト（`app/layout.tsx`）でアプリ全体を包んでおり、`router.push(loginPath)`はNext.jsのクライアント側遷移（ページ再読み込みを伴わない）のためProvider自体は再マウントされないと判明。したがって`showError()`を呼んだ直後に`router.push()`でログイン画面へ遷移しても、トーストの表示状態はそのまま引き継がれ、遷移後のログイン画面にも正しく表示される。構文チェック（`ts.transpileModule`）は診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-04】Claude in Chromeで2タブを使い実際にセッション衝突を発生させて確認：①タブAで管理部（管理部テスト）にログイン、②タブBで担当営業（山田太郎）にログインすると、タブAが自動的に強制ログアウトされログイン画面へ遷移し、画面右上に赤系のトースト通知（「別のタブまたは別の端末で、別のアカウントにログインされたことを検知しました。安全のため、いったんログアウトしました。お手数ですが、もう一度ログインし直してください。」＋✕で閉じるボタン）が正しく表示されることを確認。ブラウザネイティブのalertダイアログは一切表示されず、画面もブロックされないことを確認。あわせて配信済みのJSバンドルを直接フェッチし、該当メッセージの呼び出し元が`alert(`ではなく`showError`相当の関数呼び出しになっていることも突合確認済み。コンソールエラーはブラウザ拡張機能由来のもの3件のみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-04「useSessionCollisionGuard.tsに残っていた禁止済みalert()の修正」参照

### 2026-08-06実装完了・実機確認済み
- **更新期限管理タブ「CSV反映ありバッジ」の追加＋フォントサイズ拡大**：伊藤さんの実機レビュー指摘（「更新用のCSVがあるなら、目立つように従業員名付近にバッジ付けられないか」）に対応。ルール16に基づきデザイン案を提示し伊藤さんのOK（緑の丸バッジ「CSV反映あり」・氏名の右隣に既存の「社内」バッジと同じ形で表示）を得て実装（2026-08-04）。デプロイ後の実機確認（2026-08-06）で本番稼働中（`仕分け待ち`・`CSV自動反映`タブとも正しく表示、コンソールエラー無し）であることを確認済み。あわせて伊藤さんの追加指摘「もっとフォントサイズを大きくしたい（名前と同じくらいが理想）」を受け、モックアップ（17px＝氏名と同一／14px／12pxの3案）を提示し、伊藤さんが12pxを選択（「社内」バッジは現状維持しCSV反映ありバッジのみサイズ変更）。`RenewalManagementTab.tsx`の`hasCsvMatched`バッジを`text-[10px]`→`text-[12px]`、パディング`px-2`→`px-2.5`、丸ドットを`w-1.5 h-1.5`→`w-[7px] h-[7px]`に変更。5タブ共通の`renderRowHead()`内の1箇所修正のため全タブに反映される。構文チェック（`ts.transpileModule`）は診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-06】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、更新期限管理タブ「仕分け待ち」の長谷川将之様のカードでバッジのDOM要素を直接調査（`getComputedStyle`）し、`font-size: 12px`が実際に適用されていることを確認（`text-[12px]`が正しくビルドに反映済み）。コンソールエラーはブラウザ拡張機能由来のもの3件のみでアプリ起因のエラーは無し。これで本タスクは完了。
　→ SYSTEM_DESIGN.md 10章 2026-08-04「更新期限管理タブへのCSV反映ありバッジ追加」・2026-08-06「CSV反映ありバッジのフォントサイズ拡大」参照

### 2026-08-04実装完了・実機確認済み
- **更新期限管理の追跡単位を「スタッフ単位1件」から「雇用の側面／派遣の側面」単位に見直す（③）**：伊藤さんの実機指摘（永井優大様・104010で正社員なのに雇用期間が期間区切り表示になる／雇用契約書のみの契約なのに派遣期間・指揮命令者等の差異が出る）を発端に、DB関数`get_latest_contracts_for_renewal()`が社員番号ごとに「一番新しい契約1件だけ」を返す設計になっており、実データで長谷川将之様・三國駿様・村田爽彩羅様の3名について「同じ人が別々に持っている雇用契約書／就業条件明示書のどちらか一方が更新期限管理から漏れる」という実害を確認。書類種別の文字列そのものではなく「雇用の側面」「派遣の側面」という2軸で今その側面をカバーしている最新契約を探す方式（アスペクト単位）を採用し実装完了。あわせて「派遣から完全に外れたのに次の書類が出ない」という残存リスクに対応するため、新規申請の最終確認（STEP8「申請する」の直前）で、対象スタッフが現在派遣の側面をカバーされた状態にあり今回の書類種別がそれを含まなくなる場合にのみ「派遣契約を終了扱いにしますか？」と確認するモーダルを実装（終了理由の入力は必須。「いいえ」を選ぶとSTEP1へ戻り申請はブロックされる）。実装着手前に伊藤さんへ⑥の未決定事項3点（終了理由の入力必須化・STEP1事前ヒントの要否・契約状況モニタリングとの連動範囲）についてプロの業務改善責任者／PdM／UI-UXデザイナー目線での推奨案を提示し、全て推奨案（理由入力必須／STEP1事前ヒントを表示する／モニタリングとの連動は分類ロジック共通化＋誤検知防止の最小修正のみ今回実装）で伊藤さんのOKを得てから実装（ルール16準拠）。実装内容：DB側は`is_employment_document()`/`is_dispatch_document()`共通関数の新設、`contracts`への`dispatch_terminated`/`dispatch_terminated_reason`列追加、`get_latest_contracts_for_renewal()`のアスペクト単位への全面書き換え、`get_contract_monitoring_status()`の共通関数化＋`dispatch_terminated`を考慮した誤検知防止（派遣終了済みスタッフを書類未締結の異常として拾わないための列追加。呼び出し側`useContractMonitoring.ts`の反映はモニタリングの本題④への着手時に合わせて実施予定・今回未着手）、STEP8確認モーダル用の`has_active_dispatch_aspect()`関数を新設。フロント側は`useRenewalCandidates.ts`のsyncCandidates()をアスペクト単位の戻り値に対応（社員番号ごとに1件へ潰すMap処理を廃止し、旧契約削除ロジックも「返された契約ID集合に含まれるか」で判定するよう変更）、`RenewalManagementTab.tsx`はカードkeyが既に`c.id`（行UUID）のため1人2枚化に伴う変更は不要と確認済み、`app/apply/page.tsx`・`StepBasic.tsx`（STEP1事前ヒントバナー）・`StepFinalCheck.tsx`（派遣終了確認モーダル・既存の申請確認モーダルと同じUIパターンを流用）、SSC/管理部・担当営業の契約詳細画面（`app/dashboard/ssc/contracts/[id]/page.tsx`・`app/dashboard/sales/contracts/[id]/page.tsx`）へ承認前・閲覧時の派遣終了バナーを追加。【デプロイ後・実機確認完了・2026-08-04】Claude in Chromeで①管理部（管理部テスト）アカウントにログインし長谷川将之様・三國駿様が更新期限管理タブで2枚のカードとして表示されることを確認、②三國駿様（105026・就業条件明示書が有効）で新規発行申請（`/apply`・雇用契約書のみ）を実行し、STEP1の事前ヒントバナーが正しく表示されること、STEP6「申請する」で派遣終了確認モーダルが表示されること、「いいえ」でSTEP1へ戻り案内バナーが出ること、「はい」で終了理由入力必須（未入力時はボタン非活性）・入力後に通常の確認モーダルへ進み実際に申請できることを確認。Supabase MCPで実際に`dispatch_terminated=true`・理由文言が正しく保存されていることを直接確認した上で、テスト用に作成した契約データは削除・DBを実データのみの状態に復元済み。コンソールエラーは無し。伊藤さんの指示により、この後の①（正社員の雇用期間バグ）②（差異表示の書類種別無視バグ）④（更新時に書類種別を変更できない）は次回以降のチャットで着手する。
　→ SYSTEM_DESIGN.md 10章 2026-08-04「更新期限管理の『スタッフ単位1件』設計を『アスペクト単位』に見直す件 ― 実装完了」参照

### 2026-08-04実装完了・実機確認済み
- **更新期限管理タブ①②の実装（正社員の雇用期間バグ・差異表示の書類種別無視バグ）**：③に続けて伊藤さんの指示で①②に着手（④は派遣終了確認フローとの統合設計が必要な別テーマとして次回以降に切り離し）。実装前の調査で、①は当初の理解（CSV自動検索が派遣期間を雇用期間にそのままコピーするだけ）より根が深く、`syncCandidates()`の登録判定自体が書類種別・雇用形態を見ていないため「雇用契約書のみ」の正社員契約でもCSV連携時の派遣期間の残骸だけで誤登録される実害（永井優大様・104010＝バグ報告の実例）を確認。修正方針の提示時、当初案（正社員・無期契約の雇用期間欄を編集不可の注記にするだけ）に対し伊藤さんから「正社員・無期契約でも契約条件適用開始日の更新はあり得る」との指摘を受け、兼用×正社員/無期契約の場合は契約条件適用開始日を編集できる入力欄を新設する方針に修正した上で実装着手（ルール16準拠）。DB：`renewal_candidates`に`new_contract_start_date`列を追加。`useRenewalCandidates.ts`：`isIndefiniteEmployType()`共通関数を新設、`syncCandidates()`の登録判定を書類種別＋雇用形態の二段階でガード（対象社員のcontract_typeを事前取得）、`searchCsvRenewal()`は正社員・無期契約に雇用期間の日付を一切コピーしないよう修正、`periodReady()`・`executeBulkApply()`も契約条件適用開始日ベースの判定・反映に対応。`RenewalManagementTab.tsx`：兼用×正社員/無期契約の雇用期間欄を契約条件適用開始日の編集入力に置き換え、`renderCsvDiff()`の②の修正（書類種別でガード。正社員・無期契約は契約条件適用開始日の差異を表示）。`RenewalContractConfirmModal.tsx`（契約内容をすべて確認ポップアップ）にも`renderCsvDiff()`と全く同じバグが重複して存在していたため、伊藤さんの明示指示は無かったが同一バグの重複箇所としてルール10に基づき自主的に発見・対応。実データクリーンアップ：①の登録判定修正で本来登録されないはずの誤登録行3件（永井優大様・三國駿様・関谷綺菜様）をSupabase MCPで調査（未着手・入力値無しであることを確認してから削除）、新しい登録判定ロジックを再現したSQLで全件スイープし他に該当行が無いことを確認済み（中島明日香様＝兼用×正社員は派遣の側面が正しく登録対象のままであることも確認）。構文チェック（`ts.transpileModule`）は変更3ファイルすべて診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-04】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①CSV自動反映タブで尾方文音様（104533・パターンC・正社員）の契約条件適用開始日の差異表示が正しく（変更が無ければ表示自体されず、有期契約側の長谷川将之様の雇用期間表示には回帰が無いこと）表示されること、②「契約内容をすべて確認」ポップアップでも同様に「－」表示バグが解消され「契約条件適用開始日」として正しく表示されること、③岩城蒼太様（104018・兼用・正社員）を仕分け待ちから期間のみ更新タブへ実際に振り分け、派遣期間の入力欄に加えて新設の「契約条件適用開始日（正社員・無期契約のため雇用期間は期間の定めなしです）」欄が編集可能な状態で表示され現在値（2026年05月01日）が初期表示されることを確認。確認後、岩城蒼太様は「他のタブへ移動」から仕分け待ちへ戻しDBを元の状態（仕分け待ち7件・期間のみ更新0件、`new_contract_start_date`等もnullに復元）に復帰済み。コンソールエラーはブラウザ拡張機能由来のもの3件のみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-04「更新期限管理タブ①②の実装（正社員の雇用期間バグ・差異表示の書類種別無視バグ）― 実装完了」参照

### 2026-08-05実装完了・実機確認済み
- **更新期限管理タブ④の実装（更新時に書類種別を変更できない）**：①②に続けて伊藤さんの指示で④に着手。プロの業務改善責任者／PdM／UI-UXデザイナー目線で3点（対象範囲＝個別申請のみ／狭める変更は③のモーダルを流用／広げる変更は不足STEPを通常入力）を提案し、いずれも推奨案で伊藤さんのOKを得てから実装（ルール16準拠）。調査の結果、「STEP1に戻って書類種別を選び直し、通常のSTEP入力で不足項目を埋める」という動線自体は、最終確認画面の既存「修正する」リンク・`getStepType()`のpattern連動・各STEPの必須項目バリデーションだけで元々成立しており、追加実装不要と判明。実装が必要だったのは「狭める場合の派遣終了確認」のみ：③で作った`needsDispatchTerminationConfirm`が新規申請フロー限定だったため拡張。ただし更新申請フローでグローバルな`hasActiveDispatchAspect`をそのまま使うと、雇用契約書のみ・就業条件明示書のみを別々に保有するスタッフで無関係な誤検知が起きるため、「更新元の契約（source_contract_id）自体が派遣の側面をカバーしていたか」だけを見る専用の判定（`renewalOriginalCoveredDispatch`）を新設。`app/apply/page.tsx`のみの変更で完結（`StepFinalCheck.tsx`のモーダル本体・保存ロジックは無変更）。構文チェック（`ts.transpileModule`）診断0件を確認済み。【1回目の実機確認で新規バグ発見・その場で修正済み・2026-08-05】長谷川将之様（100531・雇用契約書のみ）で検証中、書類種別を変更すると前回契約からプリフィルされていた就業場所名・業務内容等（派遣の側面と無関係な項目）まで巻き添えでリセットされる不具合を発見。既存の「書類種別変更でSTEP2以降を全リセットする」仕組み（新規申請フロー向けに作られたもの）が更新申請フローにも無条件適用されていたのが原因。更新申請フロー（`renewalCandidateId`）ではこのリセット処理自体を無効化する形で修正・構文チェック再確認済み。検証で移動した長谷川将之様は仕分け待ちへ復帰・DB確認済み。【2回目の実機確認完了・2026-08-05】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①中島明日香様（105477・兼用）の更新候補で書類種別を変更せずそのまま「申請する」を押すと、通常の申請確認モーダルのみが表示され派遣終了確認モーダルは出ないこと（誤検知が無いこと）、②同候補で書類種別を「雇用契約書」に変更すると派遣終了確認モーダルが正しく表示され、終了理由が未入力の間は「はい」ボタンが無効化されること、「いいえ」を選ぶとSTEP1へ戻り案内バナーが表示されること、③長谷川将之様（100531・雇用契約書のみ）の更新候補で書類種別を「兼用」に変更すると、修正済みの通り前回契約からプリフィルされたSTEP2（就業場所名・業務内容等）の内容が保持されたまま、新たに必要になった項目（STEP2追加項目「業務に伴う責任の程度」）は未入力状態で表示され、選択するまで次のSTEPへ進めない必須項目チェックが機能すること（STEP3・4の派遣先/派遣元担当者・STEP5の派遣期間は今回のテストケースでは元々CSVデータに含まれていたため自動反映されたが、これは正しい挙動）、を確認。いずれのテストも実際には申請せず「この申請をやめる」で安全に離脱し、対象候補のDB状態に変更が無いことをSupabase MCPで確認済み。コンソールエラーはブラウザ拡張機能由来のもののみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-05「更新期限管理タブ④の実装（更新時に書類種別を変更できない）― 実装完了」参照

### 2026-08-05実装完了・実機確認済み
- **更新期限管理タブ「他のタブへ移動」の確認ダイアログを全移動先で必須化**：2026-08-03に「CSVインポートを依頼する」への移動のみ確認ダイアログを付け、それ以外の単純な仕分け直しは確認なしで即時移動とする方針で伊藤さんの了承を得ていたが、伊藤さんの再指示「全部の他のタブへ移動に確認を必須にする」を受けて方針転換。`handleMoveToTab()`（仕分け待ち・期間のみ更新・期間以外も修正する・CSVインポートを依頼するの4タブが使用）に、import_wait以外の移動先でも「『氏名』様を『タブ名』へ移動します。よろしいですか？」という確認ダイアログを新設。実装の過程で関連するバグを1件発見：CSV自動反映タブだけは別関数`handleMoveOverride()`を使っており、2026-08-03の「CSVインポートを依頼する移動時は必ず実際の依頼を送信する」修正がこの関数には及んでいなかった（このタブから「CSVインポートを依頼する」を選ぶと、依頼を送信せずタブだけ移動してしまう不具合が残っていた）。`handleMoveOverride()`を「期間のみ更新」への移動（既存の理由入力ボックスで別途保護済み）だけを特別処理しfalseを返す形に簡素化し、それ以外は`handleMoveToTab()`側の処理（import_waitなら実際の依頼送信、それ以外は新設した確認ダイアログ）に一本化することで、確認ダイアログの追加と同時にこのバグも解消。構文チェック（`ts.transpileModule`）診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-05】Claude in Chromeで管理部（管理部テスト）アカウントにログインし確認。①長谷川将之様（100531）の行で「他のタブへ移動」ドロップダウンを開き、4項目すべてが「→ タブ名」の統一形式で折り返さず表示されることを確認、②「→ CSVインポートを依頼する」を選択→確認ダイアログ（「選ぶと同時に管理部へ実際の依頼が送信されます。よろしいですか？」）が表示されることを確認、③「キャンセル」を押すと何も変わらないこと（仕分け待ちの件数・行とも変化なし）を確認、④再度実行し「依頼する」を押すと実際に`requests`テーブルへ`csv_import`の依頼レコードが1件作成され（Supabase MCPで直接確認）、仕分け待ち→CSVインポートを依頼するへ正しく移動することを確認（バグ修正の核心部分）、⑤移動後の行に「CSVを再検索」ボタンのみ表示され、従来あった「期間以外も修正するに切替」「期間のみ更新に切替」の重複ボタンが消えていることを確認。テスト用に作成した依頼レコードは削除、対象スタッフは仕分け待ちへ戻しDBを元の状態（更新期限対象9件・他0件）に復帰済み。コンソールエラーはブラウザ拡張機能由来のもののみでアプリ起因のエラーは無し。
　→ SYSTEM_DESIGN.md 10章 2026-08-05「更新期限管理タブ『他のタブへ移動』の確認ダイアログ全移動先必須化」参照

### 2026-08-05実装完了・要デプロイ＋実機確認（デプロイ後の実機確認で追加漏れを発見・その場で修正済み・2回目の実機確認まで完了）
- **日付表記の不統一の解消**：2026-08-03に発見した「ハイフン生値（2026-05-01）／スラッシュ（2026/08/05）／漢字（2026年08月05日）」の3方式混在に対応。伊藤さんへ3点確認（ルール16）：①統一先＝すべて漢字表記②対応範囲＝重複関数の共通化も含め一括で全箇所（約25ファイル）③進め方＝「メール・画面メッセージ読みやすさ見直し」タスクとは独立で先に片付ける。すべて回答通りに実装。`lib/dateFormat.ts`を新設（`formatDateJp`・`formatDateTimeJp`・`formatPeriodJp`の3関数に一本化）し、契約一覧・契約詳細（3ダッシュボード）・誓約書関連・最低賃金改定対応タブ・申請最終確認画面（STEP8。派遣期間・抵触日・雇用期間・試用期間）・契約書PDF・マイページ・通知メール（依頼受付・更新期限ダイジェスト・CSV修正通知）・アカウント管理（凍結日時）・契約状況モニタリング（確認依頼日時）・マスタ管理（更新日）・CSV保護行エクスポートExcelまで統一。重複していたローカルformatDateTime/formatDate関数10箇所を削除し共通ヘルパーへ集約。実装中に2点の潜在バグを予防：①サーバー（Vercel、既定UTC）とクライアント（ブラウザ、実質JST）の両方から使われる`formatDateTimeJp`が、単純にローカル時刻を使うと時刻表示が9時間ずれる問題を`Intl.DateTimeFormat`のタイムゾーン明示指定（Asia/Tokyo固定）で予防、②Dateオブジェクト専用のローカルformatDate（期日アラート等）を文字列用ヘルパーへ`toISOString()`経由で委譲すると「JST 0時=UTC前日15時」変換で日付がずれるため、意図的に共通化せずローカル日付から直接組み立てる実装として残した。あわせて調査時に見落としていたスラッシュ表記（`toLocaleDateString`/`toLocaleString`の直書き。アカウント管理の凍結日時・契約状況モニタリングの確認依頼日時・マスタ管理の更新日・更新期限ダイジェストメールの日付ラベル・依頼受付メールの日時・CSV保護行エクスポートの実行日時）も発見し統一。【伊藤さんの「デプロイ完了」報告を受けた実機確認で追加漏れ3種を発見・その場で修正済み・要再デプロイ】①3ダッシュボード（`admin/page.tsx`・`sales/page.tsx`・`ssc/page.tsx`）の契約一覧カードの「派遣期間」表示（`formatDateTime`等の共通関数を経由しない直書きのハイフン生値）、②契約詳細画面（`sales/contracts/[id]`・`ssc/contracts/[id]`）が`StepFinalCheck.tsx`とは別に独自に持っていたSTEP5（期間・労働条件）ブロックの重複コピー（派遣期間・抵触日・雇用期間・試用期間がハイフン生値のまま。抵触日はCSV差分比較用の値も揃えて変換）、③最低賃金の基準日（`effective_from`。`lib/autoChecks.ts`の自動チェック警告文・`StepSalary.tsx`の最低賃金改定対応バナー・`WageRevisionSection.tsx`の一覧・`MasterManagementTab.tsx`の最低賃金マスタ現在値/履歴、計4箇所）。原因は当初調査が関数名ベースのgrepとSTEP8画面中心だったため、直書きテンプレートリテラル・詳細画面独自の複製ブロック・`effective_from`という別名の日付列を見落としていたこと。構文チェック（`ts.transpileModule`）は追加修正9ファイルを含め変更・新設した全ファイルで診断0件を確認済み。【2回目の実機確認完了・2026-08-05】伊藤さんの再デプロイ完了報告を受け、Claude in Chromeで管理部（管理部テスト）アカウントにログインし`get_page_text`で確認。①契約一覧カードの派遣期間・雇用期間・申請日時、②契約詳細画面STEP5の派遣期間・雇用期間、③最低賃金改定対応タブ（6件）の基準日、④マスタ管理「最低賃金マスタ」（29部門）・「派遣料金額マスタ」（8営業所＋本社）の日付、⑤アカウント管理の凍結日時（4件）、⑥契約状況モニタリングの確認依頼日時、いずれも漢字表記で正しく表示されることを確認。契約詳細画面の自動チェック警告文（「2025-10-04時点の最低賃金...」）のみハイフン表記が残っていたが、これは`auto_check_results`が申請時点にDBへ保存済みの文字列スナップショットをそのまま表示する仕様のため、今回の修正対象外（今後新規・再申請される契約から反映。過去分への遡及反映は不可）と判断・伊藤さんへ共有。コンソールエラーはブラウザ拡張機能由来のもののみ。契約書PDF・マイページ・通知メールの実機確認は、blob:タブのスクリーンショット取得不可・スタッフ側ログイン要・メール送信タイミング待ちという技術的制約により今回は見送り（コードレビュー・構文チェックでは問題なし。次回該当操作を行うタイミングで確認）。
　→ SYSTEM_DESIGN.md 10章 2026-08-05「日付表記の不統一の解消」参照

### 2026-07-31判明・未着手（更新期限管理タブとは別タスク）
- **スタッフ稼働状況管理**：派遣先変更・休職/怪我・退職などで「今は働いていない」状態をシステム上で把握できず、更新期限管理が正しく機能しない懸念（伊藤さん指摘）。あわせて、①署名済み契約に急きょ内容変更が必要な場合に、現在生きている契約を検索して修正申請する仕組み、②担当営業が休職・待機・退職の状態を選択して申請できる仕組み、が必要。契約状況モニタリング（`staff.work_place`）等とも関わる横断的な機能のため、更新期限管理タブの再設計とは別タスクとして扱うことで伊藤さんと合意（今回は着手しない。優先順位・設計は次回以降に改めて検討）。
　→ SYSTEM_DESIGN.md 10章 2026-07-31「更新期限管理タブの情報設計見直し ― 業務フロー整理と再設計方針確定」参照

### 2026-08-06実装完了・実機確認済み（伊藤さんご自身のPDF目視確認も完了）
- **契約書PDF・マイページ・通知メールの実機確認（日付表記の不統一の積み残し3項目）**：Claude in Chromeで契約書PDF全パターン（A＝関谷綺菜様104747／B＝三國駿様105026／C＝岩城蒼太様104018、いずれも未署名のためその場で現行コードにより生成されたPDF）を実際に画面表示し確認。パターンCのページ2「労使協定の有効期間の終了日」欄で「2027年3月31日」とゼロ埋めされていない不具合を新規発見（`lib/pdf/documentText.ts`の`getAgreementLaborText()`が労使協定終了年を計算する特殊な組み立て箇所で、`formatDateJp`を経由しない直書きテンプレートだったため2026-08-05の一括統一時に見落とされていたもの）。`${endYear}年03月31日`に修正・構文チェック0件・パターンB/Cの両PDFに反映されることを確認済み。あわせてマイページ（尾方文音様104533で実際に承認→署名依頼メール受信→ログイン→署名完了まで実施、一覧・詳細とも「2026年08月05日に署名済み」を確認）・通知メール（スタッフマスタ登録依頼メールで「入社日：2026年08月01日」「依頼日時：2026年08月06日 08:56」を確認）も正しくゼロ埋め漢字表記で動作することを確認済み（テストデータ・DB状態は確認後すべて元に復元済み。Google Driveの孤児ファイル1件のみ削除ツール制約により残存・実害なし）。デプロイ後、伊藤さんご自身が契約書PDF（パターンC＝兼用の労使協定終了日欄含む）を目視確認し「問題なし」と確認済み。これで日付表記の不統一の解消（2026-08-03発見）は契約書PDF・マイページ・通知メールを含め全項目完了。
　→ SYSTEM_DESIGN.md 10章 2026-08-06「契約書PDF・マイページ・通知メールの実機確認」参照

### 2026-08-06実装完了・実機確認済み
- **メール・画面メッセージ読みやすさ見直し**：2026-07-31に洗い出し・優先度案のみ完了していた項目に着手。着手前に伊藤さんへ4点確認（ルール16）：①重複確認文言（4画面×2種）の共通定数化の可否②今回の対応範囲③StepFinalCheck/StepPeriodの重大警告文二重管理を同時に解消するか④見直し中に見つかったpledge/applyのDB生エラー露出の扱い。いずれも推奨案（共通定数化する／高・中・低・番外まですべて対応／二重管理も解消／まとめて修正する）で伊藤さんのOKを得て実装。
- **重複確認文言の共通定数化**：`lib/confirmMessages.ts`を新設し、「取り下げ申請の削除」確認ダイアログ（`WITHDRAWN_APPLICATION_DELETE_CONFIRM`。admin/sales/ssc各ダッシュボード＋`PledgeListSection.tsx`の4箇所で重複）と、取り下げ操作が手遅れだった場合のエラー文言（`WITHDRAW_STALE_STATUS_ERROR`。sales/ssc双方の契約・アルバイト誓約書詳細画面の4箇所で重複）を集約し、各画面から参照する形に統一。
- **StepFinalCheck.tsx/StepPeriod.tsxの重大警告文二重管理を解消**：試用期間6ヶ月超警告・正社員で試用期間「無し」警告の2つが、入力時（StepPeriod.tsx）と最終確認時（StepFinalCheck.tsx）に文言をそのままコピーする形で別々に存在していたのを発見。`app/apply/_lib/helpers.ts`に`getTrialOver6WarningMessage()`・`NO_TRIAL_PERIOD_WARNING_MESSAGE`を新設し両ファイルから共用する形に統一（日雇派遣30日ルール警告は元々1箇所のみだったため対象外）。
- **優先度高6件の改行・文言見直し**：①一括承認確認文（admin/ssc）の長大な2文目を3行に分割、②`StepBasic.tsx`最低賃金マスタ未登録ブロック文を句点区切りで整理（従来は読点で不自然に改行）、③`StepSalary.tsx`最低賃金改定差額案内に`<br />`を追加し2文に分割、④`StepWorkInfo.tsx`CSVインポート依頼後案内の不自然な読点区切りの改行を句点区切りへ修正。
- **中優先度**：`ContractMonitoringSection.tsx`の確認依頼ダイアログ・`AccountManagementTab.tsx`の凍結確認ダイアログの改行位置を整理。
- **低優先度**：「の上」/「のうえ」の表記ゆれを、既に多数派だった「のうえ」（ひらがな）に統一（`sign/[id]/page.tsx`・`staff/mypage/documents/[id]/page.tsx`・`StepFinalCheck.tsx`・`StepWorkInfo.tsx`・`apply/page.tsx`・`useRenewalCandidates.ts`・`app/apply/_lib/helpers.ts`の計9箇所。「上限」等の無関係な「の上」は対象外）。`lib/mail.ts`の`sendContractMonitoringFollowupMail`にも同種の読点区切り改行漏れ（担当者未特定時の注記が2文とも改行なしで連結）を発見し句点区切りに修正（text版・HTML版とも）。
- **番外**：`pledge/apply/page.tsx`でDBの生エラーメッセージ（`error.message`）がそのまま画面に表示されていた1箇所を、`app/apply/page.tsx`の総合レビュー指摘27対応（2026-07-17）と同じ方式（詳細はコンソールのみに残し、画面には汎用メッセージを表示）に統一。
- メール5関数のうち、`sendStaffLoginCodeMail`/`sendAccountSetupMail`/`sendStaffDocumentReadyMail`は確認の結果、既に他のメールと同水準の改行・構成になっており変更不要と判断。`sendFaqAnswerMail`も同様に問題なし。`sendContractMonitoringFollowupMail`のみ上記の1件を修正。
- 構文チェック（`ts.transpileModule`）は変更・新設した全23ファイルで診断0件を確認済み。【デプロイ後・実機確認完了・2026-08-06】Claude in Chromeで管理部（管理部テスト）アカウントにログインし、①契約一覧「一括承認する」の確認文（3行分割）、②取り下げタブの「削除」確認ダイアログ（共通定数化した`WITHDRAWN_APPLICATION_DELETE_CONFIRM`）、③アカウント管理の凍結確認ダイアログ（3文に分割）、④最低賃金改定対応タブ「修正して再申請する」→STEP7給与画面の差額案内バナー（2行分割）、⑤契約状況モニタリングの「担当営業へ確認依頼」ダイアログ、をいずれも修正後の文言・改行で正しく表示されることを確認。①〜⑤とも「キャンセル」「この申請をやめる」で実際には確定・保存せず安全に離脱し、DBに変更が残っていないことを確認済み。StepPeriod/StepFinalCheckの試用期間警告文（二重管理解消）は、正社員かつ新規採用相当の対象者が必要でテスト候補が見当たらなかったため実画面での再現は見送ったが、両ファイルとも`app/apply/_lib/helpers.ts`の同一関数を直接参照する実装のため、文言の食い違いは構造的に起こり得ないことをコードレベルで確認済み。コンソールエラーは無し。契約書PDF・実際のメール送信（`sendContractMonitoringFollowupMail`の担当者未特定フォールバック文言）は、対象データが無い／実際に送信を要するため今回は見送り（コードレビュー・構文チェックでは問題なし）。
　→ SYSTEM_DESIGN.md 10章 2026-08-06「メール・画面メッセージ読みやすさ見直し ― 実装完了」参照

### 2026-07-30実装完了・実機確認済み
- 雇用契約書PDF（パターンA・兼用版パターンC）への「契約更新の有無・基準・無期転換」条項の追加。対象スタッフの雇用形態が有期契約社員（`contractType==='有期契約'`）の場合のみ表示（伊藤さん指示）。パターンAは元々この条項自体が無かったため新規追加、パターンCは従来「有期・無期問わず常時表示」だったのをこの条件表示に変更（**パターンCはA4 2ページ構成のままでよいと伊藤さんが明示確認済み。1ページ化の対象はパターンAのみ**）。
  - 【完了・実機確認済み・2026-07-31】伊藤さん実機チェックで発覚した2件を修正：①「契約更新上限の有無」の「無」が丸・四角で囲われていない指摘 → 伊藤さんの選択（「【無】でよい」）に従い、既存の他選択項目と同じ全角【　】括弧で囲む表現に統一（`documentText.ts`の`CONTRACT_RENEWAL_TEXT`。罫線で囲む新規部品の実装は見送り）。②パターンAがA4 2ページになってしまった不具合 → tsxで実際にPDFをレンダリングしページ数を検証しながら原因調査。「本文が改行区切りで既に最小5行のため、AutoFitFreeTextのsizes配列に小さい値を追加しても最大値(8.3)が常に選ばれ縮小が一切効いていなかった」「ラベルが2行のため空欄でも行自体の高さが既存の余白を超える」という2点が真因と判明。sizesを最初から7.0pt固定にし、かつ2026-07-10と同じ考え方（タイトル・末尾文言の余白だけを詰める）で、この行が増える有期契約の場合のみpaddingTop/Bottom・marginBottomをもう一段だけ詰める対応（`hasRenewalClause`条件。無期契約・正社員・アルバイトの既存レイアウトには影響しない）で1ページに収まることを確認。実データ（関谷綺菜様の実契約＋住所を大幅に長くしたストレステスト）でtsxレンダリング→PDFバイト列のページ数を直接検証済み。デプロイ後、Claude in Chromeで本番API（`/api/contracts/[id]/pdf`）から実際のPDFを取得しバイト列を直接検証（`/Type /Pages`の`Count`が1であることを確認）、あわせて伊藤さんご自身が実物PDFを目視確認し「OK」との回答を得て完了確認済み。あわせて伊藤さんの指摘で兼用版（パターンC）も未確認だったことに気づき、実契約（長谷川将之様）でCount=2（想定通り変化なし）をバイト列検証、伊藤さんご自身の目視確認でも「OK」を得て、パターンA・C双方の確認完了。
- 従業員マイページ（`/staff/mypage`）へのブックマーク案内画面の新設。パスワードログイン成功後、マイページ本体を表示する前に、ブックマーク登録の案内（PC・スマホ別の手順）を表示する画面を挟む。「次回から表示しない」チェックを入れない限り、ログインする度に表示される（`staff.mypage_bookmark_reminder_dismissed`列で管理）。実在スタッフ（105026・三國駿）で初回認証コードログイン→パスワード設定→案内画面表示→チェック＋「マイページを開く」→ログアウト→パスワード再ログイン→案内画面が再表示されないこと、まで実機確認済み。確認後、テストパスワード・各フラグは初期状態にリセット済み。
- メーリングリストマスタの新設（`mailing_list_master`テーブル）。担当営業は部門ごとに1件（アカウント管理と同じ実運用20部門）、SSC・管理部はロール全体で1件のみ登録可能。管理部ダッシュボード「マスタ管理」タブに新規サブタブ「メーリングリスト」を追加。未登録スコープは個人宛メールにフォールバックする安全策込み。⑧（契約状況モニタリングの確認依頼メール）へ適用済み（登録があればメーリングリスト宛、無ければ個人宛）。他の通知メール（更新期限ダイジェスト・CSVインポート完了等）への適用は今回のスコープ外で次回以降。あわせて、伊藤さんの追加指示により、アカウント管理で新規にアカウントを作成する際、または既存アカウントの役割・部門を変更する際、変更先のスコープにメーリングリストが未登録だと保存できないブロックを実装（個人宛メールへ知らないうちに切り替わり続ける事故を防ぐ安全策）。氏名の修正等、役割・部門を変えない編集は対象外。実機確認中に「メーリングリストマスタへの保存が常に失敗する」という実装バグ（部分ユニークインデックスに対する`ON CONFLICT`指定の不整合）を発見し、その場で修正（通常のUNIQUE制約に差し替え）。修正後、マスタ画面での登録、アカウント編集での未登録時ブロック→登録後の成功→復元、まで一通り実機確認済み。テスト用に登録したメーリングリストは確認後すべて削除済み。
　→ SYSTEM_DESIGN.md 10章 2026-07-30「雇用契約書PDFへの契約更新・無期転換条項追加、マイページブックマーク案内画面、メーリングリストマスタ新設」参照

### 2026-07-31実装完了・実機確認済み
- 【上司デモ指摘由来「CSV検索結果・住所欄クリック時に404エラー」の原因判明・修正完了】伊藤さんが再現手順（社員番号104018・岩城蒼太を兼用で発行申請、STEP2でCSV検索結果を選択後、住所欄をクリックして修正しようとした際に発生）を提供してくれたことで再現に成功。実際の症状は`next/link`等によるルーティング上の404ではなく、JS例外によるタブクラッシュ（Chromeの「このページを読み込めませんでした」ネイティブ画面）で、非エンジニアの伊藤さんには404のように見えていたと判明。原因は`app/apply/page.tsx`の`handleCsvResultSelect`（CSV検索結果選択時の処理）で`setWorkLocationTel(r.tel)`にガードが無く、CSVの電話番号専用カラムが空（今回のように電話番号が住所欄に埋め込まれ、専用カラムには入っていないケース）だと`r.tel`が`null`になり、就業場所電話番号のstateが文字列ではなく`null`そのものになっていたこと。この状態自体では即座には落ちず、電話番号欄（`TelInput`）に一度フォーカスしてから外れた瞬間（`onBlur`）に`validateTel(null)`が呼ばれ`null.replace(...)`で例外が発生していた。素早く複数箇所をクリックする操作（伊藤さんの実機再現・私の自動操作どちらでも再現）だと、住所欄のすぐ隣にある電話番号欄に一瞬フォーカスが乗ってから外れやすく、これが引き金になっていたと考えられる。修正は3点：①`setWorkLocationTel(r.tel || '')`など`handleCsvResultSelect`内の3項目（就業場所名・住所・電話番号）に既存の他箇所と同じ`|| ''`ガードを追加、②`validateTel`関数自体にも引数nullガードを追加（再発防止・構造的対応）。実装前にプロの業務改善責任者／PdM／UI-UXデザイナー目線での評価を伊藤さんに提示し、明示的なOKを得てから実装（ルール16）。デプロイ後、Claude in Chromeで同じ実データ（岩城蒼太・兼松コミュニケーションズ北海道支店、電話番号欄が空のe-staffingレコード）を使い、STEP2の全項目を素早く連続クリックしてもクラッシュしないこと、コンソールに新規エラーが出ないことを確認。あわせて本番の新しいJSチャンクを直接取得し、`validateTel`のソースに`e=>{let l=e||"",t=l.replace(...)...}`というガード付きコードが実際にデプロイされていることも突合確認済み。
　→ SYSTEM_DESIGN.md 10章 2026-07-31「CSV検索結果選択時の電話番号null未ガードによるクラッシュ修正」参照
- 【状態再確認・問題なしと確認済み】最低賃金改定対応機能：2026-07-29に発見・修正した不具合2件（①`/apply?wageAmend=`プリフィルの`dept_no`未設定によりSTEP5給与画面の警告バナーが表示されない、②サブタブ件数バッジが開くまで0表示になる）が実際に解消されているか、Supabase MCPで実データ（最低賃金改定後の対象契約6件・鈴木駿様の時給1,200円→約1,300円等）を確認したうえで、管理部・担当営業の2ダッシュボードで実機確認。①「最低賃金改定対応」サブタブの件数バッジがページ読み込み直後から正しい件数（管理部6件・担当営業（自部門スコープ）5件）で表示され0のまま止まらないこと、②「修正して再申請する」→STEP5給与画面で「最低賃金改定対応：金額の見直しが必要です」の警告バナーが正しい基準額・不足額とともに表示されることを確認。テスト用の申請途中データは「この申請をやめる」で破棄済み（保存されない旨の確認ダイアログどおり実データへの影響なし）。この機能は完了として本一覧から削除する。
　→ SYSTEM_DESIGN.md 10章 2026-07-31「最低賃金改定対応機能の状態再確認」参照
- 依頼（スタッフマスタ登録・CSVインポート）通知メール3種の新設：①新規依頼が届いた際の管理部宛メール、②管理部の「完了にする」ボタン新設＋完了時の依頼元宛メール、③取消時の依頼元宛メールを実装。宛先は他の通知と同じ「依頼元の所属部署のメーリングリスト優先・未登録なら個人宛にフォールバック」方式に統一。伊藤さんの追加指摘を受け、スタッフ登録のみ／登録＋CSV同時依頼／CSVインポート単独の3パターンすべてで入力項目（社員番号・氏名・部門名・入社日・使用システム・派遣開始日・就業場所名）を漏れなく表示し、申請者の所属部署名・氏名を3メールすべてに必須表示、文章の改行位置も改善。実装過程でCSVインポート単独依頼時に対象スタッフの所属部門が保存されていなかった不具合、手動完了メールの文言が自動マッチ時のままだった不具合の2件も発見・修正。3パターン×3メール種別すべてをGmail連携で実機確認済み。
　→ SYSTEM_DESIGN.md 10章 2026-07-31「依頼（スタッフマスタ登録・CSVインポート）通知メール3種の新設と項目網羅・改行改善」参照
- 契約詳細画面（担当営業・SSC/管理部共有）の「パターン / 雇用区分」行に「パターンA」のような内部呼称がそのまま表示されていた不具合を修正し「雇用区分」のみの表示に変更。あわせてCLAUDE.mdルール17として、この種の内部呼称を画面・メール・テストデータに使わないことを恒久ルール化。
　→ SYSTEM_DESIGN.md 10章 2026-07-31「契約詳細画面の『パターンA』内部呼称表示を削除」参照

---

## 🗺️ システム全体像（サマリ）

人材派遣会社APパートナーズの**担当営業が、雇用契約書・就業条件明示書をWebで発行申請する**システム。発行→SSC承認→従業員署名→保管までWeb完結。月額¥0運用。

**技術**：Next.js(App Router)/TypeScript/Tailwind/Supabase/Vercel自動デプロイ
**本番**：https://ap-contract-system.vercel.app/apply ／ **GitHub**：github.com/ap-partners/ap-contract-system
**Supabase**：argpiiznuzxmmqraynfo（東京） ／ **ローカル**：`C:\Users\ito\Desktop\ap-contract-system`
**テストユーザー**：ssc-test / admin-test @appart.co.jp（全て `Test1234!`）。担当営業役は`sales-test@appart.co.jp`が2026-07-27に`ito+demo@appart.co.jp`（社員番号999900・表示名「山田 太郎」）へ置き換え済みのため、担当営業側の確認は`ito+demo@appart.co.jp`＋`Test1234!`を使う（2026-07-29訂正：本欄が置き換え後も更新されておらず古い記載のまま残っていた）。

**ロール**：担当営業（申請）／SSC（確認・承認）／管理部（CSV取込・マスタ・監視）／従業員（署名）

**テーブル（14・2026-07-15訂正、2026-07-29に未使用テンプレート残骸5件を削除）**：`staff` `department_master` `master_imports` `csv_imports` `csv_raw_data`（以上投入済み）／`contracts`（申請本体・稼働中）／`company_master`（派遣元設定key-value・稼働中）／`requests`（依頼管理）／`renewal_candidates`（更新期限管理）／`staff_roles`（ロール管理）／`minimum_wage_master`／`standard_working_hours_master`／`dispatch_fee_master`／`csv_diff_logs`（未使用・骨格のみ）。このほか`pledges`（アルバイト誓約書申請本体）・`office_master`（自社拠点マスタ）・`work_description_templates`・`contract_monitoring_actions`が稼働中（2026-07-22〜23新設、本サマリ行への反映漏れ）。旧`public.users`/`clients`/`documents`/`alert_settings`/`notifications`（初期テンプレート残骸・未使用）は伊藤さんの承認を得て削除済み（詳細はSYSTEM_DESIGN.md 10章2026-07-29参照）。
→ 各カラム定義は `docs/SYSTEM_DESIGN.md` 第3章（ただし`dispatch_fee_master`・`renewal_candidates`・`staff_roles`はDDL節が未整備。指摘38の残課題）。

**画面（2026-07-24再訂正）**：`/apply` `/login` `/dashboard/sales` `/dashboard/ssc` `/dashboard/admin` `/dashboard/ssc/contracts/[id]` `/dashboard/sales/contracts/[id]` `/sign/[id]` `/staff/mypage` `/staff/mypage/documents/[id]` `/pledge/apply` `/dashboard/ssc/pledges/[id]` は本番稼働中。アルバイト誓約書システム（申請・SSC確認・帳票PDF・署名フロー・一括承認まで）は2026-07-23〜24に実装・実機確認済み。`/help/*`のみ未実装。以前この行は「アルバイト誓約書システムは未実装」としていたが、実装済みリスト（本ファイル下部の実装済みタスク一覧）と自己矛盾しており古い記述が残っていた（2026-07-24気づき）。
**運用フェーズについて（2026-07-13念押し）**：本システムは現時点で**本稼働（実際の従業員の業務がこれに依存している状態）していない**。実際のCSVデータ・実在のスタッフ名義を検証に使っていても、それは全てテスト・検証目的であり、DB上に「止まっている」案件等が見つかっても実害・実業務への影響は無い。不具合の緊急度を判断する際、この前提を誤って「実運用に影響あり」と評価しないこと。

**書類パターン**：A=雇用契約書(6STEP)／B=就業条件明示書(6STEP)／C=兼用(8STEP)。社内は雇用契約書(A)のみ。
→ STEP内訳・自動反映ロジックは `docs/SYSTEM_DESIGN.md` 第4章。

**CSV連携**：e-staffing/HRstation/winworks/Staffiaの**4システム**（全件投入済み）。Staffiaのみ「KEF00103（契約詳細）」「KEF00104（スタッフ個人・派遣期間）」の2ファイル構成で、個別契約書番号をキーにコード側で合成する（system_typeとしては'Staffia'1つ。DB制約・実データで確認済み・2026-07-15訂正。旧記述「5ソース」はファイル数とシステム数を混同した誤解を招く表現だった）。検索キー・マッピングは `docs/SYSTEM_DESIGN.md` 第5章。

---


## ✅ 実装済み / 🔜 次にやること

**実装済み**：申請ウィザード`/apply`本体、staff・部門・5システムCSVの投入、CSV検索→STEP2/3自動反映（4システムすべて実機確認済み）、派遣元情報(STEP4)のCSV優先表示、CSV反映項目を営業が修正した場合の特別対応（STEP8）、contractsへの申請保存、STEP1「アルバイト」選択肢追加（2026-07-02）、STEP1/STEP2の依頼（スタッフマスタ登録・CSVインポート）を`requests`テーブルへ実際に保存する処理（2026-07-02）、SSC一覧・個別確認画面から社内案件を除外（2026-07-03）、管理部ダッシュボード「依頼管理」タブ（絞り込み・取消機能。2026-07-03）、担当営業ダッシュボードのタブ再編＋「依頼状況」タブ（部門全体の依頼状況表示。2026-07-03）、自動チェック機能（金額異常値・最低賃金・就業規則整合）の判定ロジック本体＋SSC確認画面へのつなぎ込み（2026-07-06）、帳票PDF生成（雇用契約書＝パターンA・就業条件明示書＝パターンB・兼用版＝パターンC　全対応。2026-07-07/08）、署名機能（フェーズ5）の基盤部分（メール送信・Google Drive連携・DBカラム・SSC承認/説明完了時の自動通知API・PDFへの署名画像埋め込みの仕組み。2026-07-08）、`/sign/[id]`本体（本人確認・パターンA/C=フルネーム入力→丸印鑑自動生成（クラウドサイン方式。手書きサインではない）・パターンB内容確認ボタン・PDF再生成・Google Drive保存・ステータス更新。2026-07-09）、スタッフマスタへの住所（`staff.address`）取込追加＋実データ1748件の再インポート（2026-07-09）、帳票PDFの角印レイアウト最終化・長い自由記述の折り返し＋自動縮小・1ページ収容の余白調整（2026-07-10）、署名機能のVercel実機確認＋ダッシュボードでの署名済みPDFプレビュー・`/sign/[id]`生年月日入力欄の改善（2026-07-10）、SSC一括承認の完全実装（`handleBulkApprove`から選択各契約に`notify-sign-request`APIを呼ぶ処理を追加。Vercel実機で動作確認済み。確認ステップ・処理中/完了の全画面オーバーレイ表示も追加。2026-07-13）、就業条件明示書（パターンB）がSSC承認後に進まない不具合の修正（`notify-sign-request`の判定条件修正。個別・一括承認どちらにも適用。2026-07-13）、担当営業ダッシュボードへの「確認済み日時」可視化（一覧・詳細画面の両方に`signed_at`を表示。2026-07-13）、SSCダッシュボード一覧への「確認済み日時」可視化（2026-07-13）、SSC承認画面の文言修正（パターンBは「署名依頼」ではなく「確認依頼」、一括承認は「署名・確認依頼」に統一。2026-07-13）、就業条件明示書（パターンB）PDFへの角印・会社名表示の追加（従来は角印関連コード自体が無く、全ステータスで角印が出ない不具合だった。パターンA/Cと同じ表示ルール・同じ内容ブロックを流用。2026-07-13）、担当営業ダッシュボード一覧への帳票種別バッジ・派遣期間（パターンB・C）表示の追加（SSC一覧と同等の情報量に統一。2026-07-13）、管理部ダッシュボードへの「契約一覧」タブ追加＋SSCと同等の承認・強制承認・差し戻し・一括承認権限の付与（フェーズ1完了。SSC詳細画面`/dashboard/ssc/contracts/[id]`を管理部にも開放する形で実装。Vercel実機確認済み。2026-07-13）、`/apply`のロールゲート緩和（フェーズ2完了。担当営業に加えSSC・管理部も申請可能に。就業場所区分の選択肢は3ロールとも同一＝絞り込みなし。2026-07-13）、管理部ダッシュボードへの「社内承認」タブ追加（フェーズ3完了。`user_metadata.is_internal_approver`フラグを持つ人にのみ表示。社内案件のみを表示し、SSCを通さず直接承認・差し戻し・一括承認できる。タブに未対応件数バッジも追加。2026-07-13）、社内案件でも「SSC」表記が出てしまっていた箇所の修正（締結パターン説明文・申請完了画面・担当営業ダッシュボードのバナー・ステータス表示・社内承認タブのバッジ、計6箇所。ステータスの内部値自体は`SSC承認済み`のまま変更していない。あわせて申請完了画面の「ダッシュボードに戻る」ボタンがSSC・管理部の場合も担当営業のダッシュボードに固定されていた不具合も修正。2026-07-13）。フェーズ4「SSC代理承認」はフェーズ1で実現済みと判断し追加実装なしで完了扱いに（2026-07-13）。`/sign/[id]`本人確認方式を「社員番号＋生年月日」から「社員番号＋メール記載の6桁認証コード」へ変更（DBスキーマ追加・`notify-sign-request`でのコード生成/メール記載・`/api/sign/[id]/verify`及び`complete`でのコード照合・5回誤入力での失効・2日間有効期限・再発行API・`/sign/[id]`画面のUI変更、全て実装。Vercel本番環境で申請→SSC承認→通知メール送信→正しいコードでの成功／誤コードでの回数表示／失効・再発行、までの一連の流れを実機確認済み。2026-07-14）、SSC・管理部・担当営業の3ダッシュボードへの絞り込み・並び替え・テキスト検索機能の追加（共通コンポーネント化。「承認済み・完了」タブの署名待ち／署名済み混在で目当ての案件が探しにくいという課題に対応。2026-07-14）、実際には到達しない「完了」ステータスの絞り込みピルを3ダッシュボードから削除＋「承認済み・完了」タブを「承認済み・署名状況」に名称変更（2026-07-14）、`contracts`テーブルへの`created_by_dept_no`インデックス追加（スケーラビリティ改善タスク①・見た目/動作への影響なし。2026-07-14）、SSC・担当営業・管理部の3ダッシュボードのUIリニューアル（伊藤さん作成のデザイン案をレビュー・型エラー修正の上で適用。モダンなSaaS系デザインに刷新。管理部にはSSC・担当営業同様の「新規発行申請」ボタンも追加。2026-07-14）、リニューアル後の全体監査で見つかった不具合の修正（担当営業の契約詳細画面に帳票PDFプレビューが元々実装されていなかった問題への対応、管理部の自動チェック警告バッジの赤/黄重要度表示の復元、SSC・管理部の一括承認確認ダイアログの説明文および警告バッジの補足表示・絵文字の復元。2026-07-14）、スケーラビリティ改善タスク②（全件取得の是正）の実装（`contracts.search_text`カラム追加＋バックフィル、共通フック`useApprovedAccumulator`新設、SSC・管理部・担当営業の蓄積型タブへ直近45日絞り込み・さらに読み込む・全期間で検索を実装。Vercel実機確認済み。2026-07-14）、3ダッシュボード共通の細かい見た目修正（契約期間欄の文字サイズ縮小で折り返し解消、詳細ボタンの文字が狭い画面幅で縦に割れる不具合をwhitespace-nowrapで解消。2026-07-14）、依頼フォームの就業場所名の必須条件修正（スタッフマスタ登録のみは不要・CSVインポートが絡む依頼は必須に変更。STEP2の単独CSVインポート依頼に就業場所名入力欄を新設し`client_name`を保存するよう修正。依頼一覧（管理部「依頼管理」・担当営業「依頼状況」）に申請者名・部門名・就業先・入社日を漏れなく常時表示するよう両カードを統一。2026-07-14）、管理部ダッシュボードへの「サマリー」タブ新設（依頼未対応・契約承認待ち・社内承認待ちをカード形式で横断表示し、クリックで該当タブへ遷移。CSV差異・更新期限は未実装のため準備中カードとして枠のみ設置。ログイン後の初期表示タブに変更。「依頼管理」タブから「承認待ち」カードを撤去。2026-07-14）、依頼一覧（管理部「依頼管理」・担当営業「依頼状況」）への直近45日デフォルト絞り込み追加（依頼データが年数とともに無制限に蓄積し件数の意味が薄れる問題への対応。2026-07-14）、`app/apply/page.tsx`分割Phase1・2（Reactに依存しない純粋関数・定数を`app/apply/_lib/helpers.ts`へ、小さな再利用UIコンポーネントを`app/apply/_components/FormParts.tsx`へ切り出し。4166行→3193行に削減。STEP1〜8本体のロジックは無変更。tsc検証済み。2026-07-14）、「この申請をやめる」等のダッシュボード復帰ボタンが管理部・SSCでログイン画面に飛ばされる不具合の修正（ロール判定なしで`/dashboard/sales`に決め打ちしていたのが原因。役割ごとの分岐に統一。Vercel実機確認済み。2026-07-14）、総合レビュー指摘11・12対応（一括承認の選択残留・二重承認ガード）：SSC・管理部（契約一覧・社内承認タブ）で絞り込み・検索・並び替えを変えたら選択状態を必ずクリアするuseEffectを`RenewalManagementTab.tsx`と同じ考え方で追加、個別承認・強制承認・差し戻し（SSC詳細画面）と一括承認（SSC・管理部の計4箇所）のUPDATEに`.eq('status','申請中')`の条件付き更新を追加し、実際に更新できた件数だけを通知・完了扱いにすることで二重承認・notify-sign-requestの二重送信を防止（2026-07-15）。UIUX総合レビュー（実機・2026-07-16）指摘の対応（D・N・A・M・E・G・C・F・I・Bの10項目：STEP1フリーズの原因調査＝ブラウザ自動化ツール固有の見かけ上の問題と判明・更新期限メール宛先不在時の管理部フォールバック・ダッシュボード横スクロール解消・更新期限メール二重送信の検証＝実害なしと確認・申請者のID断片表示を氏名表示へ・強制承認/差し戻しボタンの主副整理・担当営業KPIカード/タブの重複解消・警告バッジの色と意味の不一致修正・ログインオートフィル改善・3ダッシュボードのタブ階層統一）、全て実機確認済み・本番反映済み。あわせて署名依頼メール（`sendSignRequestMail()`）の品質改善（件名の「認証コード在中」削除・書類種別の件名/本文追加・HTML化＋文字版フォールバック・ボタン文言/余白調整・認証コード表示の白背景化・宛名への氏名追加・案内文言の明確化）も実施・実機確認済み（2026-07-16）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。認証方式自体の見直し（SMS等別チャネル併用）は伊藤さんの判断により検討自体を取りやめ・対応不要と確定（2026-07-22）。更新期限管理タブのチャットC・D（一括申請・個別申請の契約データ生成処理）を実装・デプロイ・実機確認（`executeBulkApply()`＝選択した更新候補から`contracts`へ実際にINSERTする一括申請処理をSSC一括承認と同様の確認/処理中/完了オーバーレイUIで実装、`/apply?renewal=候補ID`＝前回契約とCSV最新反映内容をマージしてSTEP1〜7を自動プリフィルし最終確認ステップへ直行する個別申請モードを`?edit=`再申請モードと同じ設計パターンで新規実装。共通の前回⇔CSVフィールド対応表`renewalFieldMap.ts`を新設し両経路で共用）。実装過程で発見・修正した実データ由来の不具合3件：①所定労働日数のCSV反映が実は一度も使われていなかったための誤差分表示の解消、②`staff`テーブルに存在しない`department`列を参照していたため一括申請のstaff_snapshotが全件nullになっていた不具合の修正（`department_master`joinへ変更）、③CSVシステムによって数値で返る項目（Staffiaの休憩時間等）が個別申請プリフィルの文字列専用処理で実行時エラーになり画面が真っ白になっていた不具合の修正（`String()`強制変換）。あわせて業務ルールとして、更新申請では試用期間を前回値に関わらず必ず「無」に自動設定し前後差分を明示する仕様を決定・実装、および3ダッシュボード共通のタブバー縦スクロールバー誤表示（`overflow-x-auto`のCSS仕様上の副作用＋1pxのサブピクセル超過が原因）を修正。すべてVercel実機確認済み（2026-07-17）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。これにより更新期限管理タブ（チャットA〜D）は一通り完成。マスタ系テーブル（`minimum_wage_master`・`standard_working_hours_master`・`dispatch_fee_master`）のRLS見直しをSupabase MCPで実環境調査し、3テーブルとも既に管理部ロール限定で強化済み・`dispatch_fee_master`はサービスロール専用の意図的設計であることを確認、追加変更は不要と判断（2026-07-17）。あわせて総合レビュー指摘のうち未対応で残っていた指摘25（担当営業の部門情報ロード中の検索誤表示）・26（署名失敗時の案内文言の分かりにくさ）・27（保存失敗時のPostgres生エラー表示）・35（verify APIのデッドコード`isPatternC`）を対応し、CLAUDE.md・SYSTEM_DESIGN.mdの残っていた古い記述（DDL取得状況の矛盾・7-5章見出し・`staff.birthday`コメント・ドキュメント整備TODO）も訂正（要デプロイ・未実機確認・2026-07-17）。【実装完了・要デプロイ＋実機確認・2026-07-17】従業員向けマイページ（`/staff/mypage`）を新規実装：過去のトーク履歴で確定していた仕様⑨（A．署名待ち書類の確認・署名／B．署名済み・確認済み書類の一覧・閲覧、新しい順で直近のみ表示し「過去の書類を見る」で展開／お知らせ機能は今回スコープ外と確定）に基づく。認証方式は伊藤さんの判断で「社員番号＋パスワード」の持続ログイン（当初設計どおり。`staff.password_hash`・`is_initial_login`を実際に使用開始）とし、初回ログイン・パスワード再設定は「社員番号＋メール記載の6桁認証コード」で本人確認する（`/sign/[id]`と同じ考え方だが、認証コードの単位を契約ごとから従業員ごと＝`staff`テーブルに変更）。新規：`middleware.ts`（`/staff/mypage`以下をセッションCookieで保護）、`lib/staffPassword.ts`（scryptによるパスワードハッシュ）・`staffSession.ts`（HMAC署名付きセッションCookie。`lib/pdfAccessToken.ts`と同じ方式で新規環境変数なし）・`staffAuth.ts`・`staffResetToken.ts`、API（`/api/staff/login`・`request-code`・`verify-code`・`set-password`・`logout`・`me`・`documents/[id]`）、画面（`/app/staff/login`・`/app/staff/mypage`・`/app/staff/mypage/documents/[id]`）。既存の`/api/sign/[id]/complete`はセッションCookieがあればそちらを優先し、無い場合のみ旧・契約単位の認証コード方式にフォールバックする形に変更（後方互換）。`notify-sign-request`は、`staff.is_initial_login`に応じてメール内容を出し分ける（未設定なら認証コード付き、設定済みならログイン案内のみ）よう変更し、契約単位の認証コード発行はやめた。DB：`staff`に`login_auth_code`・`login_auth_code_expires_at`・`login_auth_attempts`・`login_password_attempts`列を追加した際、これらの機密列を社内ユーザー（担当営業・SSC・管理部）のanonキー経由`select('*')`から読めてしまわないよう、列単位権限（`REVOKE SELECT`）で`authenticated`ロールから明示的に遮断済み（service roleは影響なし）。【完了・実機確認済み・2026-07-17】デプロイ後、初回ログイン→マイページ表示直後にログイン画面へ戻されてしまう不具合を発見・修正：原因はNext.jsのmiddlewareが既定でEdge Runtime動作するため、`lib/staffSession.ts`が使っていたNode標準`crypto`モジュール（`createHmac`・`timingSafeEqual`）がEdge Runtimeで使用不可でトークン検証が常に失敗していたこと。Web Crypto API（`crypto.subtle`）ベースの非同期実装に全面書き換えて解消。あわせて伊藤さんの指定でパスワード要件を「8文字以上」のみから「8文字以上・英大文字/小文字/数字の3種類必須」に強化（`lib/staffPassword.ts`・サーバー側・クライアント側とも対応）。デプロイ後、Claude in Chrome＋Supabase MCP＋Gmail連携で①パスワードログイン→署名待ち書類の確認・署名→マイページに戻るボタンでの遷移②認証コードでの初回ログイン→弱いパスワードの拒否確認→3種類を満たすパスワードでの設定→即マイページ表示③ログアウト→パスワードでの再ログイン、を実機確認済み。検証で実在の従業員アカウント（社員番号105026）に設定したテストパスワードは確認後に初期状態へリセット済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。スタッフマスタ取込のバルク処理化（チャンク単位でのまとめてSELECT→JS側判定→upsert方式へ全面書き換え。Vercelタイムアウト対策）＋SBクルーコード保持ロジック＋担当営業からの「スタッフ登録依頼」自動完了機能（`runStaffRegisterAutoMatch`）を新規実装し、実データのStaffExpressエクスポート（1908行）で本番投入・実機確認済み（新規35件・更新1748件・スキップ125件・エラー0件、crew_code保持157件は1件も消えず維持）。あわせてテストデータでスタッフ登録依頼→インポート→自動完了→通知メール→申請反映までの一連フローも実機確認済み（2026-07-21）。個別契約CSVインポート（e-staffing・HRstation・winworks・Staffia）についても、実データ（2026/7/1付CSV5ファイル）を使い、担当営業からの依頼→管理部インポート→自動マッチ→依頼ステータスの`completed`化→通知メール送信→担当営業側`/apply`STEP2での反映確認、まで4システムすべてClaude in Chromeで実機検証済み（新規/更新/保護件数の突合、自動マッチ成立の確認含む。2026-07-21）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。【完了・実機確認済み・2026-07-22】社内システム担当者によるQA総合監査（`QA監査進捗_2026-07-22.md`）の新規指摘5件（①C1セッション衝突＝別タブで別アカウントにログインされたことを`onAuthStateChange`で検知し強制ログアウトする`lib/useSessionCollisionGuard.ts`を新設しSSC・管理部・担当営業の計5画面に組込み／②C2重複申請＝同一スタッフ・同一書類種別で「申請中」の案件が既にある場合`app/apply/page.tsx`の新規申請保存をブロック／③D1オートフィル汚染＝`app/staff/login/page.tsx`の社員番号欄に`autoComplete="off"`／④A1設計書ズレ＝5-3章のCSVマッピング記述を実装（`helpers.ts`行番号付き）に合わせて訂正／⑤B1文字数上限なし＝`NoBreakTextarea`等の自由記述欄に既定2000文字の`maxLength`を追加）に対応し、①〜⑤すべてVercel実機（2タブでの実際のログイン競合・重複申請の実送信ブロック・DOM上のautocomplete属性・入力上限の動作）で確認済み。あわせてこの実機確認の過程で見つかった「STEP3で雇用期間の終了日を空欄のまま次へ進むとフリーズする」という事象は、`window.alert`を差し替えて再現検証した結果、`validatePeriod()`が正しくアラートを出してSTEP3に留まる正常な入力チェック動作であり、実際にフリーズして見えたのはブラウザ自動化ツール側がネイティブalertダイアログでCDP通信をブロックされていたための見かけ上の問題と判明（対応不要と判断）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。総合レビュー指摘事項（2026-07-14）の未対応9件のうち判断不要な4件に対応（要デプロイ）：指摘28・30は調査の結果いずれも既に意図通り・対応済みと判明（バグではない）、指摘34は`useContractListToolbar.tsx`のコメントを実装（`length > 0`判定）に合わせて訂正、指摘36は`/sign/[id]`のPDF埋め込み用印鑑canvasがプレビューと同解像度のままだった点を560×560に拡大して解消。残る5件のうち29（申請者ID表示）は2026-07-16の対応で既に実質解消済みと確認（追加対応不要）、32（検索デバウンス・エスケープ）・33（依頼一覧の絞り込みUI統一。依頼一覧専用の共通フック新設＋担当営業への検索欄追加）は実装・デプロイ・実機確認済み、39・40（ドキュメントの章間矛盾・トーク履歴の未反映事項）はコード変更を伴わないドキュメント修正のみで対応済み。以上で総合レビュー指摘40件はすべて着手・判断済みとなった。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【実装完了・要デプロイ・2026-07-22】伊藤さんの依頼で全メールテンプレート（`lib/mail.ts`6種）を監査：4種は文面・件名・レイアウトとも問題なし、CSVインポート・スタッフ登録の完了通知2種のみHTML版が無かったため他と同じ装飾を追加し統一。あわせてブラウザタブに表示されるページ名がNext.js初期値「Create Next App」のまま放置されていたことが判明し、社内向け画面を「契約書管理システム」、従業員向けマイページ（`/staff`配下）を「マイページ | 契約書管理システム」に変更（新規`app/staff/layout.tsx`で指定）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【仕様確定・実装未着手・2026-07-22】アルバイト誓約書システムは「方針未決着」ではなく、実は`過去のトーク履歴.docx`に2026-06-18時点の詳細仕様（帳票2種類・`/apply`とは別画面・CSV連携なし全手入力・4STEP構成・雇用期間3パターン等）が既に確定していたことが判明。伊藤さんとの再確認で、基本給・諸控除・締切日支払日・交通費は雇用契約書側の既存コンポーネント・固定文言をそのまま流用する形に簡素化し、諸手当・備考欄は削除、就業場所は「クライアント先／自社」選択式にして自社の場合は新規の自社拠点マスタから自動反映する方針に更新。実装自体は次の新しいチャットで着手予定。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【土台＋STEP1〜2実装完了・要デプロイ＋実機確認・2026-07-22（別チャットで着手）】①自社拠点マスタ（`office_master`テーブル新設＋マスタ管理タブ「自社拠点」サブタブ）、②申請本体テーブル`pledges`新設（contractsの設計を踏襲）、③新規ルート`/pledge/apply`（STEP1：スタッフ検索・帳票種別選択／STEP2：就業先情報〔クライアント先手入力／自社は拠点マスタから自動反映〕・雇用期間パターン選択。STEP3・4は次回実装の骨格のみ）、④担当営業・SSC・管理部の3ダッシュボードへ「アルバイト誓約書 新規発行」ボタン追加、を実装。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【STEP1〜5全STEP実装完了・要デプロイ＋実機確認・2026-07-22（同日継続）】伊藤さんのUI/UXレビュー（3ラウンドのモックアップ確認）で承認された方向性（アイコンカード・セグメントコントロール・日付チップの「削除」表示・雇用期間セクションの文言とライブ集計バッジ）をTabler等の外部アイコンフォントではなく既存の手描きinline SVG方式で実コードに反映し、骨格のみだったSTEP3（業務内容・所定労働時間及び休憩時間。最大5パターンまで追加可）・STEP4（給与。雇用契約書STEP7の賃金・交通費ブロックを流用し保険ブロックなし）・STEP5（最終確認＋`pledges`への実際の保存処理）を新規実装。CP・SPOT用の説明文の誤り（コールセンター→キャンペーンの略でありCPはコールセンターではない）も修正。実機確認後、伊藤さんの追加UI/UXレビュー10件に対応（要デプロイ）：①自社拠点マスタへappart.co.jp/about掲載の実データ8拠点を投入、②STEP2セグメントコントロールの非選択側を白背景+ボーダーで視認可能に、③雇用期間パターンの説明バナー強化＋カード説明文を具体例入りに変更、④就業日チップの削除ボタンを日付に近接配置、⑤STEP3業務内容にテンプレート選択機能を新設（新規マスタ`work_description_templates`＋マスタ管理タブサブタブ＋AP・CL研修用選択時のみSTEP3に表示）、⑥STEP3「休憩時間帯」項目を削除、⑦STEP4給与の種類から月給を削除、⑧STEP4「諸手当」行を削除、⑨STEP4月額換算基準を160時間→140時間（1日7時間×20日）に変更、⑩STEP4「定額残業手当」「住宅手当」を削除、⑪STEP4交通費区分から「定期代＋ガソリン代」を除外（雇用契約書`/apply`側の共通`TRANSPORT_TYPES`は変更せずpledge専用にローカル複製）。次の実装対象：SSC確認画面・帳票PDF生成2種・署名フロー接続。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【実装完了・要デプロイ＋実機確認・2026-07-22（同日継続）】伊藤さんの第2回UI/UXレビュー10件＋Claude自己レビュー5件、合計16件の修正（ダッシュボードボタンの名称統一・配色／STEP2就業先情報のカード化統一／雇用期間説明文の改行修正／自社拠点の表示順固定〔`sort_order`列新設〕／発行枚数バッジの強調／業務内容テンプレートのプルダウン化＋上書き確認／所定労働時間の自動計算＋必須化／給与計算基準を単日〔時給×7〕に再修正／STEP5最終確認画面を`/apply`STEP8パターンに全面刷新／期間パターンカードの文字サイズ統一／交通費区分グリッドの均等化／MIXパターンの重複日付防止）を実装。要デプロイ＋実機確認（前回「チェックが甘い」との指摘を踏まえ厳密に実施予定）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【完了・実機確認済み・2026-07-24】「取り下げ」機能をSSC・管理部の契約・アルバイト誓約書詳細画面（計4画面）にも拡張：自分自身の申請（`isOwnSubmission`）かつ取り下げ可能なステータスの場合、承認・差し戻しUIを取り下げ専用UI＋説明バナーに置き換える方式（自己承認防止も兼ねる）。Claude in Chromeで4パターン（SSC自己申請・管理部自己申請・差し戻し中の取り下げ・他人の申請でのリグレッション確認）実機確認済み。【アカウント作成・編集・凍結機能は実装・実機確認済み／招待フローのパスワード確定も解決済み・2026-07-24】管理部ダッシュボードに新規「アカウント管理」タブを追加（`is_account_admin`権限を持つ人のみ）。氏名・部署の保存先を`staff_roles`に一本化し、担当営業以外（SSC・管理部）にも名前表示ができる土台を整備。新規作成（メール招待コード方式）・編集（氏名/役割/部門/権限）・凍結/凍結解除（削除ではなくログイン不可化。RLSの`current_role_name()`/`current_dept_no()`と`lib/apiAuth.ts`双方で凍結アカウントを即座に拒否）・自己ロックアウト防止（自分自身の凍結・自分自身のアカウント管理権限剥奪をブロック）を実装し、招待メール受信・認証コード確認・編集・凍結・凍結解除・自己ロックアウト防止まで実機確認済み。【解決済み・実機確認済み・2026-07-24】招待された本人がパスワードを確定する最後のステップが`invalid JWT: unrecognized JWT kid <nil> for algorithm ES256`で失敗していた件は、Supabaseの認証ログ調査でservice roleキーを使うAdmin API呼び出し（`/admin/users*`）のみが間欠的に失敗し、セッション認証の自己サービス呼び出し（`/user`）は一度も失敗していないことを突き止め、「管理者がパスワードを強制設定する」方式から「本人確認後に`admin.generateLink`（リトライ付き）でrecoveryトークンを発行→クライアントが`verifyOtp`で自分のセッションを確立→`auth.updateUser`で自分でパスワードを設定→`finalize` APIで招待コードを消込」方式へ変更して解決（`/api/account-setup/complete`書き換え・`/api/account-setup/finalize`新設・`lib/supabase.ts`に`retrySupabaseCall`追加）。Vercel本番で3回連続の招待→パスワード設定→ログインを実機確認済み。**【完了・実機確認済み・2026-07-24】伊藤さん実機レビュー指摘13項目の一括対応**（「GOと言うまで実装しない」方式で蓄積→GO承認後に一括実装。1チャット4機能ルールは伊藤さんの判断で今回例外的に緩和）：①誓約書STEP2の郵便番号（###-####）・電話番号（/applyと同じ`validateTel`）バリデーション、②自社拠点マスタ本社住所の9F修正（DBのみ・契約書PDFの`COMPANY_HQ_ADDRESS_LINES`は10階のまま維持）、③`clampDateYear()`新設による日付入力の年4桁制限（7ファイル21箇所）、④時刻入力の時計アイコンをTab対象から除外（`.time-input-no-icon-tab`・6箇所）、⑤誓約書への金額異常値・最低賃金自動チェック（`pledges`に`auto_check_results`/`warning_level`列追加・`runPledgeAutoChecks()`新設・クライアント先/自社拠点共通・SSC誓約書詳細に警告バナー表示）、⑥誓約書の一括承認（`PledgeListSection`に契約一覧と同じ設計＝警告なしのみ選択可・二重承認ガード・確認/処理中/完了オーバーレイ。SSC・管理部のみ）、⑦誓約書一覧のデザインを契約一覧と統一（検索・並び替え・警告バッジ含む）、⑧承認・差し戻し完了バナー4箇所の過去形化、⑨マイページトップ刷新（白丸ロゴ拡大・システム名表示・ログアウト右上アイコン化・強制改行削除・余白拡大・一覧アイコンを青系角丸バッジに変更）、⑩署名時の氏名完全一致チェック廃止（旧漢字・外国籍の表記揺れ対策。空チェックは維持）、⑪署名待ち画面のv8デザイン適用（紺ヘッダー・戻るボタン・オレンジ確認ボタン・チェックボックスを送信直上へ）、⑫署名済み画面の統一デザイン（署名日時表示・ボタン間64px）。PDFを開くまでの順序ガードは実装しないことで確定。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-24参照。【完了・実機確認済み・2026-07-27】一覧カードのレイアウト崩れ修正（3ダッシュボード＋アルバイト誓約書一覧）：画面幅が縮んでも一覧カードの表示崩れ（不要な折り返し・不要な横スクロール）が起きないよう、固定列幅＋長い文字列は省略表示（`title`でホバー時全文表示）に変更し、あわせてページ全体を`h-screen overflow-auto`（ビューポート固定高＋内部スクロール）に変更することで横スクロールバーが常に画面下端に表示されるようにした（従来はヘッダー〜一覧全体が縦に長い1ブロックだったため、案件数が多いとスクロールバーが画面外はるか下に隠れていた）。伊藤さんとの段階的な方針確認（個別スクロール案→統一スクロール案、専門家目線の懸念提示を経て「操作の一貫性優先」で最終決定）を経て実装。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-27参照。あわせてマスタ管理画面「在籍スタッフ数」の誤表示（`staff`テーブルの1000件取得上限により中部営業所・関西支社等が「未使用の可能性」と誤表示される不具合）を、DB側GROUP BY集計関数（退職者除外込み）への切り替えで修正・実機確認済み。詳細は同10章2026-07-27参照。【完了・実機確認済み・2026-07-27】上長への実演に向けたデモ用データ整備（テスト契約のプレースホルダー文言・重複データの整理、取り下げ／アルバイト誓約書の警告あり／契約状況モニタリングの不足パターンを追加）と、デモ用ログインアカウント（`ito+demo@appart.co.jp`。`sales-test@appart.co.jp`のstaff側メール紐付け切れによる「所属部門を特定できませんでした」エラーを解消）の整備、および`input_data.csvMeta`自動削除処理（保持期間2年・DB関数`cleanup_old_csvmeta`＋新規Cron`app/api/cron/csvmeta-cleanup`）を実装。デプロイ後、Claude in Chromeでログイン・契約一覧・取り下げタブ・アルバイト誓約書の警告バッジ・新規Cronエンドポイントの認証ガードをすべて実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-27参照。【コード修正完了・要デプロイ＋実機確認・2026-07-27〜28】外部レビュー「リリース前総合レビュー」21件（Critical2・High6・Medium9・Low4）に対応：試行回数カウンタの競合状態修正・再発行APIのクールダウン迂回修正（Critical2件）、申請ウィザードのモバイル幅崩れ修正・ダッシュボード見出しの折り返し崩れ修正・モーダルのEsc/フォーカストラップ対応・申請者名未解決時のID表示修正・署名鍵のフェイルクローズ化（High、署名画面デザイン統一の1件のみCLAUDE.mdルール12により見送り）、バリデーション文言具体化・スケルトンローダー・PDFプレビューの読み込みフィードバック・モニタリングボタンの視認性改善・承認確認UIの統合・管理系APIの生エラー隠蔽・一行inputの文字数上限・二重送信防止ガード（Medium9件すべて）、説明文の箇条書き化・テストデータ削除・認証コード比較のタイミングセーフ化（Low、取り下げタブ導線統一の1件のみ将来改善として見送り）を実装。詳細・見送り理由・自己レビュー結果は`docs/SYSTEM_DESIGN.md`10章2026-07-27〜28参照。【完了・実機確認済み・2026-07-28】上記の続き：ビルドエラー（削除済みstateへの残存参照2箇所）を修正・再デプロイし、SSC・管理部の2ロールで主要修正箇所を実機確認（新規不具合なし）。あわせて唯一見送っていた[12]署名画面(v8)デザイン統一・配色統一も、CLAUDE.mdルール12に従い伊藤さんへ方向性確認（`/sign/[id]`の「確認・署名」「完了」の2ステージのみマイページ書類確認画面と同じv8デザインに統一し「本人確認」ステージは現状維持／絞り込みピルの色を新色`#2F5FD0`に統一、のいずれも推奨案で了承）のうえ実装。これでレビュー21件はすべて対応完了（要デプロイ＋実機確認）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-28参照。【完了・実機確認済み・2026-07-29】残タスクの棚卸しの中で、軽微なセキュリティ設定2件（`search_path`可変の修正・「漏洩パスワード保護」はProプラン限定機能と判明し見送り）と、未使用テンプレートテーブル5件（`public.users`/`clients`/`documents`/`alert_settings`/`notifications`。実データ0件・コード内無参照を確認の上、伊藤さんの承認を得て削除）を実施。あわせて管理部ダッシュボード「アカウント管理」タブの部門プルダウンを、`department_master`51件の無絞り込み表示から実際に使う20部門・固定順・「法務部」表示ラベル短縮（実データは変更せず）に変更（`app/api/admin/accounts/route.ts`の`CURATED_DEPT_ORDER`。編集中アカウントの部門が20件に含まれない場合＝本社所属の既存3件は「（現在の設定）」を補って表示）。続けて、在籍スタッフ0名の統括部門（広域本部・北日本営業部・西日本営業部・HRソリューション営業部）向けに、STEP1スタッフ検索・契約一覧・更新期限管理・アルバイト誓約書一覧の4箇所すべてで「グループ範囲」（自部門＋配下の実業務部門群）に検索・表示スコープを拡張する機能を実装（`app/apply/_lib/helpers.ts`の`DEPT_GROUP_SCOPE`／`getDeptSearchScope()`）。この過程で、所属部門判定ロジックが2026-07-24新設の`staff_roles`（アカウント管理の正）とは無関係に旧`staff`テーブル参照のまま残っていた移行漏れを発見し、`/apply`・担当営業ダッシュボードとも`staff_roles`参照に是正（バックフィル込み）。RLS側もグループ範囲を反映するSQL関数`current_dept_scope()`を新設し`contracts`/`pledges`/`renewal_candidates`の関連ポリシーを追随修正。すべてVercel実機確認済み（アカウント管理20件表示・本社アカウント編集・4部門でのグループ範囲検索、いずれも正常動作を確認）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-29「アカウント管理の部門選択肢を絞り込み・並び替え」「担当営業のスタッフ検索・一覧表示の『グループ範囲』拡張機能」参照。続けて契約状況モニタリングのフェーズ3（`staff.work_place`の運用切替）に着手：2026-07-23時点では「本稼働後まで保留」としていた方針を、伊藤さんの「実害の無い検証フェーズの今のうちに中核フロー（`/apply`保存処理）を直しておく方が安全」との指摘を受けて転換し、`/apply`の申請保存時に対象スタッフの`staff.work_place`を同期する処理を追加、既存18名分を直近契約からワンショットでバックフィル。Vercel実機確認済み（テスト申請後に`staff.work_place`が即時反映、既存の契約状況モニタリング表示にも悪影響なし）。詳細は同10章2026-07-29「契約状況モニタリングのフェーズ3」参照。最後に、管理部「更新期限管理」タブを「期限間近の更新候補」「契約状況モニタリング」のサブタブ構成に再編（伊藤さんの「縦積みで下の一覧が見落とされやすい」との指摘を受け、既存の`SubTabBar`共通コンポーネントを流用。初期表示は期限間近の更新候補、サマリーカードからの遷移も必ずこちらに固定）。実装過程でモックアップ修正（サブタブピルの件数表示を他タブと同じラベルなしのプレーン整数に統一）と、デプロイ後の実機確認で見つかった不具合（契約状況モニタリングのデータ先読みがサブタブを開くまで走らず初期表示時のピル件数が0と誤表示される問題）も即修正し、再デプロイ後に解消を確認済み。詳細は同10章2026-07-29「管理部『更新期限管理』タブのサブタブ化」参照。

> **注（2026-07-30）**：以下は完了済みタスクの詳細な経緯ログとして残しているだけで、「今なにが残っているか」の判定にはもう使わない。現在地の一覧は必ず冒頭の「🔴 残タスク一覧」章を見ること。

**次の優先タスク**（詳細は `docs/SYSTEM_DESIGN.md` 第9章。以下は9-1章の実際のステータスと突き合わせ済み・2026-07-08）
1. 【完了・2026-07-10】署名機能`/sign/[id]`のVercel実機確認：社員番号105611・105315で申請→SSC承認→自動メール送信→本人確認→丸印鑑生成→署名完了→Google Drive保存、までの一連の流れを本番環境で確認済み。あわせて派生対応も完了：①ダッシュボードの「帳票PDFプレビュー」が署名済み契約では常に未署名の見た目を再生成していた不具合を修正（`drive_file_id`がある場合はGoogle Driveの実物PDFを返すよう変更）、②`/sign/[id]`の生年月日入力欄を`<input type="date">`から「半角数字8桁を1つの欄に連続入力」方式に変更。なお以下は意図的に後回しにしたスコープ外：①本人確認失敗時の試行回数制限、②URL有効期限（7日間固定）の実際のアクセス制御としての実施と期限切れ時の再発行フロー（【訂正・2026-07-27】この論点はその後の設計変更で解消済み。2026-07-13にタスク2番の通り本人確認方式自体が「社員番号＋6桁認証コード（2日間有効）」に変更され、「URL自体の7日間固定期限」という設計は廃止された。試行回数制限〔5回失効〕・再発行フロー〔`/api/sign/[id]/reissue`、3分クールダウン・試行回数引き継ぎロジック付き〕とも実装・Vercel実機確認済み。コードは`app/api/sign/[id]/verify/route.ts`・`reissue/route.ts`・`app/sign/[id]/page.tsx`で2026-07-27に再確認済み）、③`署名済み→完了`の遷移設計（ダッシュボードの「完了」タブは両ステータスを既にまとめて表示するため当面支障なし）、④丸印鑑で氏名がスペース区切り3単語以上の場合に文字がはみ出す不具合（伊藤さん確認の上「クラウドサインも同様の見た目のため対応不要」と判断・対応保留）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-10参照
2. 【完了・2026-07-14】`/sign/[id]`の本人確認方式を「社員番号＋生年月日」から「社員番号＋メール記載の6桁認証コード」へ変更：DBスキーマ・`notify-sign-request`・`/api/sign/[id]/verify`/`complete`・再発行API・`/sign/[id]`画面のUIを全て実装。Vercel本番環境で正しいコード・誤コード・失効/再発行の一連の流れを実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
3. 【全フェーズ完了・2026-07-13】社内承認＋SSC代理承認のまとめた実装：フェーズ1（契約一覧タブ）・フェーズ2（`/apply`のロールゲート緩和）・フェーズ3（管理部ダッシュボードへの「社内承認」タブ追加。`is_internal_approver`フラグ方式・画面側での制御。現在は`admin-test`のみフラグ設定済み、実際の5名分は伊藤さんがSupabase管理画面で個別に設定）が完了。フェーズ4（SSC代理承認）は、フェーズ1で管理部ロール全員が無条件・いつでも現場案件を承認できる仕組みが既にできているため「実現済み」とみなし、追加実装しないことで伊藤さんと確定。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
4. 管理部ダッシュボードの残り3タブ：CSVインポート（自動マッチ・自動通知含む）／CSV差異アラート／更新期限管理。【2026-07-14伊藤さん了承】着手順は「更新期限管理」を先行（本稼働後の実害が大きいため）、その後CSVインポート自動化・CSV差異アラート。タスク8の次に着手。【実装完了・要Vercelデプロイ＋実機確認・2026-07-15】CSVインポートタブ：管理部が画面からCSVをアップロードして取り込めるようにした（`app/api/admin/csv-import/route.ts`新設・`lib/csvImportShared.ts`新設）。伊藤さんとの再確認の結果、旧来の「CSV差異アラート」構想（書類ステータス別の多者通知・専用ダッシュボード）は簡略化し、①CSV上書きは基本OKだが「申請中以降のステータスの契約」から参照されているCSV行は上書きしない、②CSVインポート依頼の自動マッチにsystem_type（システム名）一致も条件追加、③自動マッチ成立時は自動完了＋依頼元へメール通知、の3点のみを実装。CSV差異アラートタブは別ダッシュボードを作らず案内文言のみ更新。実装過程でcsv_raw_data・csv_importsのRLSが「認証ユーザー全件読み書き可」のままだった点も発見し、書き込みをservice_role専用に強化済み（DB側のみ・再デプロイ不要）。伊藤さん提供の実CSV5ファイルでパース・件数・保護対象検知をSupabase MCP経由で検証済みだが、bashサンドボックスからSupabase本体へのネットワーク接続ができない制約により、実際のファイルアップロードAPIの一連動作（アップロード→保存→自動マッチ→通知メール）はこのセッション内では実機確認できていない。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。【完了・実機確認済み・2026-07-21】上記の実機確認を実施：担当営業からの依頼→管理部インポート→自動マッチ→依頼ステータス`completed`化→通知メール送信→担当営業側`/apply`STEP2での反映確認まで、4システム（e-staffing・HRstation・winworks・Staffia）すべてClaude in Chromeで実データを使い通しで検証済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。【実装完了・要デプロイ＋実機確認・2026-07-17】CSVインポートタブへのStaffExpress（スタッフマスタ・部門マスタ）取込追加：当初のタスク6のスコープに含まれていたが2026-07-15実装時に見落とされていた部分。既存の`scripts/import-master.js`のロジック（列順序・変換ルール・除外ルール・日付タイムゾーンバグ再発防止策）をそのままWeb化し、CSVインポートタブ内に5つ目のシステム選択肢として追加（新規タブは作らず）。上書き方針は契約CSVと異なり「全件上書き（employee_numberキー）」。列順序・注記のアコーディオン表示、取込履歴（`master_imports`）表示も実装。【完了・実機確認済み・2026-07-17】デプロイ後、安全のためテスト用の小さいxlsxファイル（実データと衝突しない部門NO=9999・社員番号555501等）を使い、部門マスタ・スタッフマスタそれぞれ新規追加、8/9始まり社員番号の除外ルール、`master_imports`への履歴記録・画面表示までをClaude in Chrome＋Supabase MCPで実機確認。テストデータは確認後すべて削除済み。あわせてタスク5番のマスタ管理タブへの伊藤さんフィードバック対応（「Claudeにご相談ください」の文言修正、最低賃金マスタの表形式への再設計・「未使用の可能性」バッジ表示）も同日デプロイ後に実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。**【フェーズ1完了・全面再監査＋修正済み・2026-07-14】更新期限管理**：新規テーブル`renewal_candidates`＋共通フック`useRenewalCandidates.ts`＋共通コンポーネント`RenewalManagementTab.tsx`を実装し、管理部（全部門）・担当営業（自部門のみ）・SSC（全部門・閲覧＋意向確認のみ、`canFinalize`propで最終確定操作を制御）の3ダッシュボードすべてに組み込み済み。実装後、機能充足性・UI/UXデザイン（既存3ダッシュボードと同じ角丸カード×行形式へ刷新・残日数KPI・絞り込み検索・csv_pending再検索ボタン）・動作面（保存エラーの可視化、日付入力のtype=date化、初期値バグ修正、送付準備完了バッジ、選択状態クリア等）の3段階で再監査・修正済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14の一連のエントリ参照。**【フェーズ2完了・2026-07-15】自動メール通知**：45/30/20/14/7日の残日数しきい値で、担当営業ごとに1日1通のダイジェストメールを自動送信（TO=担当営業、CC=SSC・管理部）。Vercel Cron（毎日8時台JST）→`app/api/cron/renewal-notify/route.ts`→`lib/mail.ts`の`sendRenewalDigestMail()`という構成。実際の担当営業アカウントがまだ無いため、環境変数`RENEWAL_NOTIFY_OVERRIDE_EMAIL`（`ito@appart.co.jp`固定・本番前に解除必要）で送信先を伊藤さんのアドレスに差し替え中。【完了・実機確認済み・2026-07-16】Vercel環境変数`CRON_SECRET`・`RENEWAL_NOTIFY_OVERRIDE_EMAIL`を伊藤さんが追加・再デプロイ済み。Claudeが`/api/cron/renewal-notify`を実機確認（200 OK・sent:2、Gmail(ito@appart.co.jp)で2通受信も確認）。更新期限管理フェーズ2は完全に動作確認完了。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。**残タスク（次回以降）**：【設計確定・実装未着手・2026-07-16】更新期限管理タブの大幅改修方針を伊藤さんと確定した（①所属部署・雇用形態の追加表示②書類種別表示③データ元3パターン整理④差異確認の表示範囲拡大⑤「手入力に切替」ボタンを`/apply`プリフィル＋STEP8直行の新規申請フローへ再設計＝「全項目を編集」フル編集画面連携はこの形で実現予定⑥原契約・反映内容のSTEP8形式確認画面新設⑦同一期間での誤申請防止の警告チェック⑧スタッフ意向・クライアント意向トグルの廃止・ステータス簡略化⑨ボタン文言の行動ベースへの統一）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16「更新期限管理タブの改修方針を確定」参照。実装はチャットA（①②⑧⑨）→B（④⑥⑦）→C（⑤一括申請）→D（⑤個別申請・`/apply`連携）の4分割で確定。【完了・実機確認済み・2026-07-16】チャットA実装済み：DB（`renewal_candidates`から意向2列削除・`document_type`列追加・関数更新）、`useRenewalCandidates.ts`（意向ロジック削除・部署名/雇用形態の現在値join追加）、`RenewalManagementTab.tsx`（意向トグル/KPI/一括ボタン削除、所属部署・雇用形態・書類種別の表示追加、ボタン文言統一）、3ダッシュボード・`lib/mail.ts`の追随修正。管理部・担当営業・SSCの3ダッシュボードすべてで実機確認済み（表示・差異確認パネル・ボタン文言とも正常）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。チャットB：④（12項目の差異確認拡張）・⑦（同一期間の安全警告）は実装・デプロイ・実機確認まで完了（2026-07-17）。⑥（原契約confirmation画面。モック確認済み）は実装完了・未デプロイ。あわせてチャットC（⑤一括申請）のうちUI部分（仕分けトグル・固定バー）も実装完了・未デプロイ。**2026-07-16 23:39 UTC〜Vercel障害（GitHubリンクのデプロイ・認証に影響）→ 復旧後さらに別のGitHub側障害でVercelの自動デプロイが停止、という2段階の障害が発生**したが、Git連携の切断・再接続、Vercelダッシュボードの「Create Deployment」による手動デプロイで2026-07-17に復旧・本番反映を確認。⑥・⑤（仕分けトグル・固定バー）とも実機確認済み（一括申請への切替・固定バーの集計連動・期間未確定行でのオプション無効化・⑥モーダルの表示、いずれも正常動作を確認）。【全チャット完了・実機確認済み・2026-07-17】⑤の契約データ生成処理（一括申請の実行本体。`renewalFieldMap.ts`新設・`executeBulkApply()`実装）とチャットD（個別申請：`/apply?renewal=`プリフィル＋最終確認直行）を実装・デプロイし、Claude in Chrome＋Supabase MCPで実際に一括申請・個別申請それぞれ1件ずつ最後まで（`contracts`への実データ保存・`renewal_candidates.status`の`'applied'`更新まで）通しで実機確認済み。実装過程で発見・修正した不具合3件（所定労働日数のCSV差異誤検知、staffテーブルに存在しない`department`列の直接SELECTによるスタッフ情報欠落、CSV数値項目の型不整合による個別申請プリフィル画面のクラッシュ）、および試用期間は前回値に関わらず更新時は必ず「無」にする方針決定（雇用期間内に収まる制約は`/apply`側で既存担保済みと確認）、タブ行の縦スクロールバー誤表示の原因（`overflow-x-auto`のみ指定時にUAが`overflow-y`もautoとして扱うCSS仕様）と修正も含め、詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17の一連のエントリ参照。これで更新期限管理タブ（チャットA〜D）は一通り完成。【完了・実機確認済み・2026-07-17】①更新期限管理経由の契約が既存の署名フロー（SSC承認→確認依頼メール→`/sign/[id]`）に問題なく乗ることを実地確認：チャットDで実際に個別申請した契約（阿部晃大・105404・パターンB）でSSC承認→メール到達（Gmail実機確認）→本人確認→内容確認完了までを通しで実行し、`status`が`申請中`→`署名待ち`→`署名済み`に正常遷移することを確認。新規実装は不要と判明し完了扱い。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。【実装完了・要デプロイ＋実機確認・2026-07-17】②部門マスタ・最低賃金/所定労働時間マスタ・労働者派遣料金額マスタの管理画面（タスク5番）：当初は新しいチャットでの着手を伊藤さんと合意していたが、伊藤さんの判断で同一チャット内で継続。管理部ダッシュボードに「マスタ管理」タブを新設（`app/dashboard/_shared/MasterManagementTab.tsx`＋`app/api/admin/master-data/route.ts`）。部門マスタは新規追加のみ（既存行の編集・削除は不可）、最低賃金・所定労働時間マスタは新規追加＋直近レコードの修正が可能、労働者派遣料金額マスタは伊藤さんのご要望で8営業所を全件表示し金額を入力する表形式（`getOfficeName()`と同じロジックで候補を固定・自由記述不可）で実装。あわせて`department_master`・`minimum_wage_master`・`standard_working_hours_master`のRLSを強化し、書き込み（INSERT/UPDATE）を管理部ロールのみに制限（従来は認証ユーザーなら誰でも書き込み可能だった）。【完了・実機確認済み・2026-07-17】デプロイ後、admin-testアカウントでClaude in Chrome＋Supabase MCPにより部門・最低賃金・所定労働時間・派遣料金額の4マスタすべての新規追加・修正・保存を実際に操作し、DBへの反映を確認（テストデータは確認後に削除）。派遣料金額マスタは実データ（本社¥24,219等）への書き込み経路のみ確認し値は変更していない。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。③Excel過去データの本番切り替え時取込は引き続き未着手のまま。【完了・実機確認済み・2026-07-15】指摘17（契約更新後に旧`renewal_candidates`行が重複表示）・指摘18（「更新しない」確定後に意向を戻してもstatusが戻らない）に対応。デプロイ後、実際にDBに存在していた重複行（社員番号105235）が管理部ダッシュボードを開いた瞬間の自動同期で1行に整理されること、意向を戻すとstatusが自動でpendingに戻ることをClaude in Chrome＋Supabase MCPで実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。
5. 【完了・実機確認済み・2026-07-17（詳細はタスク4番参照）】部門マスタ（`department_master`）・最低賃金/所定労働時間マスタ・労働者派遣料金額マスタ（`dispatch_fee_master`）の管理画面：管理部ダッシュボードに「マスタ管理」タブとして実装済み。2026-07-29にアカウント管理タブの部門プルダウンのみ20件固定リストへ再調整（本項目とは別対応）。
6. 【完了・2026-07-13】就業条件明示書（パターンB）単体の申請がSSC承認後「署名待ち」へ進まない不具合の修正：`notify-sign-request`の判定条件を修正（`closing_pattern`が`null`の場合も`'auto'`扱いに）。個別承認・一括承認どちらも解消。あわせて、この不具合調査の過程で「内容確認済みの日時がどこにも表示されない」という関連課題も見つかり、担当営業ダッシュボード（一覧・詳細）に`signed_at`（確認・署名日時）の表示を追加した（フェーズ1：可視化）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
7. スケーラビリティ改善タスク（優先順は9-1章9番参照）：①【完了・2026-07-14】DBインデックス追加（`created_by_dept_no`のみ抜けていた分を追加）。②【完了・実機確認済み・2026-07-14】全件取得の是正：`contracts.search_text`カラム追加＋バックフィル、`app/apply/page.tsx`保存時のsearch_text組み立て、共通フック`app/dashboard/_shared/useApprovedAccumulator.ts`を新設し、SSC・管理部（契約一覧／社内承認）・担当営業（完了タブ）の蓄積型タブに直近45日絞り込み＋「さらに読み込む」＋「全期間で検索」を実装。Vercel実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14参照。【完了・実機確認済み・2026-07-21】③`app/apply/page.tsx`分割：STEP1〜8の8ブロックを`_components/Step*.tsx`へ切り出し（ロジックは親に残しJSXのみ移動）。3,467行→1,955行に削減。Vercel実機で申請→完了→一覧→詳細→PDFまで一気通貫確認済み。【完了・実機確認済み・2026-07-21】④退職者除外ロジックのDB移行：3箇所の重複ロジックを`lib/staffFilters.ts`の`excludeRetiredStaffOr()`に集約し、SupabaseクエリのWHERE条件（DB側）で除外する方式に統一。実機確認済み。【方針の仮決めのみ・実装保留・2026-07-21】⑤`input_data`肥大化対策：対象を`input_data.csvMeta`のみに限定し保持期間2年を仮置きとする方針を記録（詳細・根拠は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照）。現状DB23MB/500MBで緊急性は低く、実際の削除（`csvMeta`キーを消す処理）は本稼働後の実データが貯まってから再検討・未実装のまま。ただし「月次で監視タスクを別途設定する」という当初案は、伊藤さんの指摘（Coworkを毎日使うわけではなく通知を見落とす）により撤回し、【完了・実機確認済み・2026-07-21】署名・確認完了時に`input_data.csvMeta`をGoogle Driveへ自動でバックアップ保存する仕組み（`lib/googleDrive.ts`の`uploadJsonBackup()`等・`contracts.csvmeta_backup_file_id`）と、管理部専用の復元ボタン（`app/api/admin/contracts/[id]/restore-csvmeta`・SSC/管理部契約詳細画面の「管理部専用メンテナンス操作」セクション。復元前にバックアップ日時を確認表示・`csvmeta_restored_at`/`csvmeta_restored_by`で監査記録）を実装。デプロイ後、実在スタッフ（105026・三國駿）のテスト署名で①Driveへのバックアップ作成②`csvmeta_backup_file_id`の記録を確認し、admin-testアカウントで復元ボタンの一連の流れ（バックアップ日時確認→復元実行→`input_data.csvMeta`・監査列の反映）まで実機確認済み。削除機能自体はまだ無いため、このバックアップは将来の削除に備えた事前準備という位置づけ。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。【削除機能・実装完了・要デプロイ＋実機確認・2026-07-27】保持期間2年で確定し、DB関数`cleanup_old_csvmeta(cutoff_at)`（migration: `add_cleanup_old_csvmeta_function`）で`csvmeta_backup_file_id`が存在し`signed_at`が保持期間を過ぎた契約のみ`input_data`から`csvMeta`キーだけを取り除く実装。新規Cron`app/api/cron/csvmeta-cleanup/route.ts`（`vercel.json`に`0 18 * * *`で追加登録）が毎日呼び出す。トランザクション内でのドライラン検証済み（バックアップ済み2件のみ正しく`csvMeta`が消え、他の列・他契約には影響なし）。現時点では2年以上前の署名済みデータが存在しないため実行しても対象0件。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-27参照。
8. 【完了・2026-07-14】STEP1のスタッフ検索を自部門のみに制限：`app/apply/page.tsx`の`handleSearch`に、担当営業ロールのみ`staff.dept_no`一致条件を追加（SSC・管理部は横断業務のため対象外と伊藤さんに確認済み）。STEP2の4CSVシステム検索はSTEP1で選択済みのスタッフをキーにしているため追加対応不要と確認済み。動作確認用に12部門分のダミー担当営業アカウントをSupabaseに新規作成し（ログイン情報は`ダミー担当営業アカウント一覧.xlsx`）、伊藤さんが実機で自部門制限の動作を確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14参照
9. 【絞り込み機能は完了・2026-07-14】SSC・管理部・担当営業の一覧タブに絞り込み・並び替え・検索を共通コンポーネントで追加済み。`取り下げ`ステータスの表示先は【訂正・完了・実機確認済み・2026-07-27】別チャットで対応済みと判明。3ダッシュボード＋アルバイト誓約書一覧すべてに「取り下げ」専用タブを新設（コミット`2d0758b`）、取り下げ理由・日時の表示と削除ボタン（本人の取り下げ済み申請のみ、`.eq('status','取り下げ')`のRLS制限付きDELETE）、30日経過後にVercel Cron（`/api/cron/withdrawn-cleanup`・毎日17時JST）で自動削除する仕組み（コミット`174a854`・`1bdef06`）まで実装され、`origin/main`にpush・本番デプロイ済み。2026-07-27にコード本文（フロント・cronルート・vercel.json）を再確認済み。
10. 【完了・2026-07-13】管理部ダッシュボードに、SSCが出来ることをすべて出来るようにする（フェーズ1）：「契約一覧」タブを新設し、SSCと同等の一覧表示・承認・強制承認・差し戻し・一括承認を実装。SSCの契約詳細画面を管理部にも開放する形で対応（新規の管理部専用画面は作らず既存画面を共有）。当初「閲覧専用」で出したところ伊藤さんから「SSCが出来ることは管理部も出来る」と指摘を受け、承認権限も含めて完全同等に訂正。Vercel実機確認済み。フェーズ2（`/apply`ロールゲート緩和）・フェーズ3（社内承認タブ）・フェーズ4（SSC代理承認）は未着手（タスク3番に引き続き記録）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
11. 社内システム担当者による総合レビュー（2026-07-14）の指摘40件（セキュリティ・データ整合性・件数表示・UX文言・パフォーマンス・ドキュメントのズレの6分野）。ファイル本体は`docs/reviews/総合レビュー指摘事項_2026-07-14.md`に保存済み。【完了・2026-07-15】①Supabase実環境のRLS・user_metadata確認（フェーズ0）と、それに続く認証統合フェーズ2.5・RLS強化（`staff_roles`テーブル新設・自己昇格防止トリガー・`contracts`/`renewal_candidates`/`requests`のRLSポリシーを部門・ロール基準に変更。DB側のみの変更のためVercel再デプロイ不要）。【完了・2026-07-15】②complete総当たり・メール失敗座礁・notify認可・PDF認証（指摘1・2・4・10）：`notify-sign-request`へのAuthorizationヘッダー必須化（呼び出し元6箇所を修正）、PDF取得APIへの二経路認証（署名画面用の短命トークン／社内ダッシュボード用のロール・部門チェック）、`complete` APIへの試行回数制限追加、メール送信失敗時に「SSC承認済み」へロールバックする処理を追加。**コード変更を伴うためVercel再デプロイが必要**。デプロイ後、Claude in Chrome＋Gmail連携で実機確認（PDFプレビュー・SSC承認〜通知メール送信・認証コードのメール到達）を実施し、いずれも正常動作を確認。この過程で①のRLS強化（フェーズ1）が原因の重大バグ（`contracts`のUPDATE＝SSC/管理部の承認操作が全件RLS違反で失敗）を発見・即修正済み（DB側のみ・再デプロイ不要）。【完了・2026-07-15】③一括承認の選択残留・二重承認ガード（指摘11・12）：上記「実装済み」欄参照。（※この時点では④表示・文言(指摘19・21〜24)・⑤ドキュメント修正(指摘37・38)を「残り・未着手」としていたが、同じ2026-07-15内で後述の通り実装・完了している。マスタ系テーブル（`minimum_wage_master`等）のRLS見直しのみ、マスタ管理画面着手時に合わせて検討という位置づけで未着手のまま（→2026-07-17のマスタ管理タブ実装時に`department_master`と同様`minimum_wage_master`・`standard_working_hours_master`のINSERT/UPDATEを管理部ロール限定化済み、`dispatch_fee_master`はRLSポリシー自体を設けずservice-role専用のAPI経由アクセスのみに限定する設計とし、2026-07-17に実データで再検証・完了確認済み）。）**2026-07-15の再監査で判明した、これまで残タスク一覧から漏れていた指摘**：指摘20（依頼一覧の`limit(500)`で古い未対応依頼が永久に見えなくなる。19と同じ「件数・表示ロジックバグ」の部類なのに④に入れ忘れていた）、指摘7・8・9（認証コードが`Math.random()`・reissue APIの悪用余地・署名画像の未検証。セキュリティ第1部のうち1・2・4・10だけ対応し、この3件は一度も残タスクに載せていなかった）。伊藤さんへ次の着手候補として①更新期限管理フェーズ2（45/30/20/14/7日自動メール通知。タスク4番の残タスクの中でも法務リスクが最大で伊藤さんが2026-07-14に最優先と決定済み）②指摘20・19の件数表示バグ③指摘19・21・22・23・24の表示文言＋37・38のドキュメント修正④指摘7・8・9のセキュリティ拾い残し、の4案を提示済み。着手順は伊藤さんが選定中（次回セッションで確定）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14・2026-07-15参照。【完了・2026-07-15（コード修正のみ・未デプロイ）】④のうち指摘7・8・9（セキュリティ拾い残し）に対応：指摘7＝`lib/signAuthCode.ts`の認証コード生成を`Math.random()`→Node標準`crypto.randomInt()`に変更。指摘8＝`app/api/sign/[id]/reissue/route.ts`に再発行クールダウン（3分）を追加し、有効なコードが残っている間は試行回数のリセットも行わないよう変更（5回試行→再発行→再試行での回数制限迂回を防止）。指摘9＝`app/api/sign/[id]/complete/route.ts`に署名画像（丸印鑑PNG）のdata URL形式・サイズ上限（500KB）チェックと、画面入力のフルネーム（`sealName`）とスタッフマスタ氏名の一致検証を追加（`app/sign/[id]/page.tsx`から`sealName`を送信するよう変更）。【完了・実機確認済み・2026-07-15】デプロイ後、初回ビルドが型エラーで失敗（`admin/page.tsx`の「依頼管理」タブの未定義`resetFilters`参照。今回の変更とは無関係の既存不具合）→修正し再デプロイで解消。Claude in Chrome＋Supabase MCPを使い、テスト契約（永井優大様）で①reissueクールダウン（連続2回で1回目成功・2回目429）②氏名不一致ガード（400エラー・試行回数は消費されない）③画像サイズ上限（400エラー）④正常系の署名完了（`/sign/[id]`から最後まで進み、DB上`署名済み`・`drive_file_id`記録まで確認）をすべて実機確認済み。19・20・21〜24・37・38・7・8・9すべて完了済み。【完了・コード修正のみ・未デプロイ・2026-07-15】指摘13・14・15・16（データ整合性の小粒不具合4件）に対応：指摘13＝CSV再検索時に`setCsvSelectedId(null)`が漏れていたのを追加。指摘14＝差し戻し警告の比較関数`normalizeFields()`に`monthlyStandardHours`キーが無くキー構成不一致で警告が構造的に一度も出ない不具合を修正。指摘15＝再申請保存のUPDATEに`.eq('status','差し戻し中')`の条件付き更新を追加し、0件更新時は専用エラー表示。指摘16＝`lib/googleDrive.ts`に`deleteDriveFile()`を新設し、complete API のUPDATE失敗・空振り時にアップロード済みPDFを削除しDrive孤児ファイルを防止。ローカルbashサンドボックスのtsc実行は今回も反映遅延で信頼できず（従来からの既知の制約）、次回のVercelビルドログと実機確認（CSV再検索・差し戻し再申請・同時承認・署名完了）で最終確認が必要。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。【完了・DB側のみ・要Vercelデプロイ・2026-07-15】指摘31（更新期限管理syncCandidatesの全件取得）対応：Supabase側にDB関数`get_latest_genba_contracts_for_renewal()`（DISTINCT ONでスタッフごとの最新1件に絞り込み、必要な列だけを返す）を新設し、`useRenewalCandidates.ts`の`syncCandidates()`をこの関数呼び出しに変更。RLSは呼び出しロールのものがそのまま適用されることを模擬セッションで確認済み。コード変更を伴うためVercel再デプロイと実機確認が必要。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。

12. 【完了・実装・デプロイ・実機確認まですべて完了・2026-07-22】ブラウザネイティブ`alert()`/`confirm()`メッセージボックスの全体置き換え：全40箇所（当初40箇所＋実装過程で新規判明1箇所）を①必須項目チェック→インライン警告バナー（`ValidationBanner`。25箇所超）、②はい/いいえ確認→カスタムモーダル（`useConfirm()`。7箇所）、③非同期エラー→トースト通知（`useToast()`。8箇所）の3パターンに分けて置き換え。土台の共通コンポーネント3つ（`ToastProvider`・`ConfirmDialog`・`ValidationBanner`）の新設に続き、Phase2（confirm7箇所：`/apply`・`/pledge/apply`・`MasterManagementTab.tsx`）、Phase3（alert24箇所：`/pledge/apply`5＋`/apply`19。`/apply`側は共通`NavButtons`に`error`propを追加して一元化）、Phase4（残alert8箇所のトースト化＋実装中に新規判明した取消理由未入力チェック1箇所のバナー化）を全てVercelデプロイ後にClaude in Chrome＋Supabase MCPで実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22の一連のエントリ参照。
13. 【契約状況モニタリング＝フェーズ1実装完了・要デプロイ＋実機確認／アルバイト誓約書1枚化＝設計のみ・2026-07-23】伊藤さんが記録済みタスク以外の懸念点として提起した2件を設計・一部実装。
   - **契約状況モニタリング**：【フェーズ1実装完了・実機確認済み・2026-07-23】管理部ダッシュボード「更新期限管理」タブに、①現場スタッフの契約未締結検知（雇用契約書・就業条件明示書それぞれの署名済み契約の有無。台帳なし＝契約実績ゼロは絞り込みトグルで表示/非表示切替）、②社内・有期契約スタッフの更新期限管理（雇用契約書のみ対象）を検知するセクションを新設・統合した。DB関数`get_contract_monitoring_status()`＋`useContractMonitoring.ts`＋`ContractMonitoringSection.tsx`。設計時想定と異なり、`staff.work_place`（全件NULL・未運用）のバックフィルは待たず、直近契約の`work_place`から動的推定する方式でフェーズ1のみで運用開始（伊藤さん承認・詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-23参照）。実機確認済み（対象689名中675名が台帳なし・トグル背後に正しく収まることを含め確認済み）。【フェーズ2実装完了・要デプロイ＋実機確認・2026-07-23】対応状況管理（未着手／依頼済み／対応中／解消）と担当営業への確認依頼ボタンを実装。新規テーブル`contract_monitoring_actions`＋新規API`/api/contract-monitoring/notify`＋`sendContractMonitoringFollowupMail()`。requestsテーブルは既存画面への影響回避のため使わず、専用テーブル＋メール通知のみの方式（伊藤さん承認）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-23参照。フェーズ3（`staff.work_place`の運用切り替え・バックフィル）は当初【意図的に保留・2026-07-23決定】としていたが（書き込み先が`/apply`申請保存処理という中核フローであること等を理由に非着手と判断）、2026-07-29に伊藤さんの「実害の無い検証フェーズの今のうちに直しておく方が本稼働後より安全」との再提起を受けて方針転換し、【完了・実機確認済み・2026-07-29】`/apply`保存処理への同期処理追加＋既存18名分のバックフィルを実装・デプロイ・実機確認まで完了（詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-29「契約状況モニタリングのフェーズ3」参照）。あわせて、契約状況モニタリング機能自体もその後の伊藤さんの実機テスト時の指摘を受け、管理部ダッシュボード「更新期限管理」タブ内で「期限間近の更新候補」と別サブタブに分離するUI改修（サブタブ化）を実施・完了済み（詳細は同10章2026-07-29「管理部『更新期限管理』タブのサブタブ化」参照）。
   - **アルバイト誓約書1枚化**：単日ごとに勤務時間が異なる実態にSTEP設計が対応できていなかった問題を解消。単日登録の上限を10件に設定し、パターン（単日・複数日／期間指定／MIX）に関わらず発行書類は常に1枚に統合。5STEP構成を6STEPへ再編（就業先情報と就業日程を分離）。原本Excel（`契約書関連/アルバイト誓約書(AP・CL研修/CP・SPOT).xlsx`）の罫線・体裁を踏襲した最終レイアウト案（v5）まで伊藤さんと合意済み。期間指定は就業時間1パターンのみに簡略化。【①②③実装・デプロイ・実機確認まで完了・2026-07-23（別チャットで継続）】③`department_master.office_id`拠点マッピング列を新設し、51部門のマッピングを実データで実行済み（広域本部・北日本営業部・西日本営業部・全社は伊藤さん指示により対象外）。①②`app/pledge/apply/page.tsx`を6STEPに再編し、`workDates`＋`shifts`を廃止して`singleEntries`（単日・最大10件）＋`periodShift`（期間指定・1パターン）＋`scheduleRows`（PDF用統合表データ）に変更。デプロイ後、Claude in Chrome＋Supabase MCPでMIXパターン（重複日付防止・自動所定労働時間計算含む）・拠点マスタ自動反映・STEP6最終確認表示・実際の申請保存（`pledges.input_data`の新形状）まで一気通貫で実機確認済み。④帳票PDF実装（react-pdf）も実装・デプロイ・実機確認まで完了（表題「アルバイト誓約書」・v5レイアウト・会社側住所は所属部門から拠点を逆引き。本番APIへの直接リクエストで200/application/pdf・403認可拒否とも確認済み）。⑤SSC確認画面・署名フロー接続も実装完了・要デプロイ＋実機確認（`/dashboard/ssc/pledges/[id]`新設・SSC/管理部両ダッシュボードへ「アルバイト誓約書」タブ追加・署名フローは契約書と同じマイページ`/staff/mypage`に統合〔`kind`でcontracts/pledgesを出し分け〕）。これでアルバイト誓約書①〜⑤すべて実装完了。【実装完了・実機確認済み・2026-07-23】⑤デプロイ中に伊藤さんからPDFレイアウトの詳細フィードバック9件（本社住所修正〔マスタ側は対応済み〕・就業日程10行固定枠化・業務内容欄拡大・自社拠点の就業先名フォーマット・賃金支払方法/交通費の雇用契約書との文言統一・全体の余白拡大）を受領し、`lib/pdf/PledgePdf.tsx`・`lib/pdf/renderPledgePdf.ts`を修正。デプロイ後、実在スタッフ（105026・三國駿）でSSC承認→署名完了までの一連の流れを実機実行し、Google Drive上に生成された実物PDFを目視確認して9件のレイアウト修正（10行固定枠・業務内容欄拡大・余白拡大等）が反映済みでA4 1ページに収まっていることを確認済み。テスト署名データ（pledge・staff認証情報）は確認後に削除済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-23参照。

同じ実機確認の過程で、マイページ署名フローの副次的な使い勝手の課題を発見・修正（要デプロイ＋実機確認済み・2026-07-23）：SSC承認直後にすぐ通知メールの「認証コードでログイン」を試みると、承認処理自体が既に認証コードを1通発行済みのため、再発行クールダウン（3分・指摘8対応時に導入）に引っかかって詰まる問題があった。伊藤さんと相談の上「案1：メールリンクから直接コード入力画面へ」を採用し、`lib/mail.ts`の`sendStaffLoginCodeMail()`が生成するログインURLに`?emp=社員番号`を付与、`app/staff/login/page.tsx`側でこのクエリパラメータを検知して送信ボタンを経由せず直接コード入力画面へ遷移するよう変更（認証コード自体は別途必須のため、社員番号だけURLに含まれてもログインは完了しない）。この対応はアルバイト誓約書・雇用契約書双方の署名メール、およびパスワード再設定メールに共通する`lib/mail.ts`の共有関数を通るため、3フローすべてに影響する全体的な改善。実装中に別のメール不具合も発見：`?emp=105611`のようにURLへ`=`を含めた結果、nodemailerの既定エンコーディング（quoted-printable）が行折り返し位置で`=`を破損させ、HTML版メールのボタンリンクだけが壊れる不具合が判明（プレーンテキスト版は無事だった）。`lib/mail.ts`の`createTransport`に`textEncoding: 'base64'`を追加して解消。この修正も全メール送信（署名依頼・ログインコード・更新期限通知・CSVインポート完了等）に共通するため影響範囲は誓約書に限らない。デプロイ後、Gmail連携で修正前後の実メール（105611＝破損リンク／105026＝正常リンク）を比較し破損解消を確認、続けて`?emp=105026`リンク→認証コード入力→パスワード設定→マイページ→署名完了までを通しで実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-23参照。

14. 【本番リリース前・必須・着手条件あり・2026-07-29追加】スタッフマスタ取込（StaffExpress）の`email`列固定解除：現在`staff.email`はStaffExpressの実データ列を使わず常に`ito@appart.co.jp`に固定（テスト運用中の暫定対応・意図的な仕様）。伊藤さんとの確認（CSVインポート機能の質問対応時に実機テストで発覚・報告）により、**「アカウント管理」が正しく登録された後に、この固定を解除して実メールアドレスを反映するよう変更することが本番リリース前の必須対応**と確定。着手順・アカウント管理側の完了条件は未確定のため、着手時に伊藤さんへ確認すること。詳細は`docs/SYSTEM_DESIGN.md`6章（表14行目）・10章2026-07-29「CSVインポート（スタッフマスタ）の伊藤さん質問3件」参照。
15. 【実装完了・要デプロイ＋実機確認・2026-07-29追加】最低賃金改定対応機能：最低賃金マスタ改定後、既存の申請中・締結済み契約（パターンA/C・現場）のうち雇用期間が改定日をまたぎ賃金が不足しているものを検知し、修正・再申請できる導線を新設。更新期限管理タブの3つ目のサブタブ「最低賃金改定対応」として担当営業・SSC・管理部の3ダッシュボードすべてに表示（表示範囲は`contracts`の既存RLSに委ねる。担当営業＝自部門、SSC・管理部＝全部門）。3ロールとも「修正して再申請する」ボタンから`/apply?wageAmend=契約ID`（`/apply?renewal=`と同じ設計パターン）で元契約をプリフィルし最終確認へ直行、給与額を見直して新規契約として再申請できる。デプロイ・Claude in Chromeでの実機確認が未実施。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-29「最低賃金改定対応機能」参照。

16. 【実装完了・実機確認済み・2026-07-30】上司デモ指摘⑥（CSV自動反映項目を担当営業が修正した状態で申請した場合の管理部見落としリスク）対応として新設した「CSV由来データ修正時の管理部通知メール」（`app_labor@appart.co.jp`固定宛）について、伊藤さんが実際の受信メールを見た上でのフィードバックに基づき内容を改善。①本文冒頭の承認者表記を「SSC」固定から実際の承認者ロール（SSCまたは管理部）に応じた動的表示に変更（`contracts.approved_by`を`staff_roles.role`で引く）、②対象システムの下に「就業場所名」を追加表示、③その下に「派遣期間（CSVデータ上）」（開始日〜終了日）を追加表示、④本文末尾の文言を「詳細内容は下記「詳細画面を開く」からご確認頂けます。」に変更、⑤派遣期間の日付表記を「2026年06月01日」形式に変更、⑥対象スタッフ名末尾の「様」を削除（件名・本文とも）、⑦対象スタッフ名に含まれる全角スペースを半角スペースに変換（件名・本文とも）。実装過程で、②の就業場所名が常に「―」（空欄）表示になる不具合を発見・修正（`input_data.fields`の参照キー名が実際の保存キー`workLocationName`ではなく誤って`locationName`になっていたことが原因）。デプロイ後、後藤初樺（社員番号104674・北海道営業所）のテスト契約をCSV自動反映項目修正ありの状態で新規作成し、管理部アカウント（admin-test）で強制承認、Gmail連携で実際の受信メールを確認した結果、①〜⑦すべて正しく反映されていることを実機確認済み（「管理部が承認しましたので」の動的表示・就業場所名の正しい表示・日本語日付表記・件名/本文とも「様」なし＋半角スペースを確認）。テストデータ（`renewal_candidates`→`contracts`の順）は削除済み。これでCSV由来データ修正時の管理部通知メール機能は内容改善も含めすべて完了。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-30の一連のエントリ参照。

17. 【完了・実機確認済み・2026-07-30】CSV修正管理部通知メールの「対象システム」の下に「契約番号」を追加表示。契約番号は4システム共通で存在するが呼び名が異なる項目（e-staffing＝契約No／HRstation＝契約番号／winworks＝個別契約番号／Staffia＝個別契約書番号）で、CSVインポート時にDB側`csv_raw_data.unique_key`へ正規化済みの値をそのまま使う。`app/apply/page.tsx`にstate新設・STEP2選択時に保存・申請保存時に`input_data.csvMeta.csvContractNo`へ記録。差し戻し再申請・最低賃金改定再申請は前回契約の値をそのまま復元、更新申請（`/apply?renewal=`）のみ伊藤さんとの確認の上「新しいCSV行（`renewal_candidates.new_csv_raw_data_id`）の契約番号」を優先する仕様に統一（実態＝最新のCSV反映内容と一致させるため。csvSnapshot自体は従来通り前回契約のものを複製する仕様のままで、契約番号だけこの扱いから外れる点は伊藤さんに共有済み）。`lib/mail.ts`・`app/api/contracts/[id]/notify-csv-modified/route.ts`を修正。今回の実装より前に申請済みの契約は値を持たないため「契約番号：―」表示になる（過去分への遡及反映は不可）。デプロイ後、後藤初樺（社員番号104674）のテスト契約を新規作成し（始業・終業・休憩の3項目をCSV反映値から意図的に変更）、SSCアカウントで強制承認したうえでGmail連携により実際の受信メールを確認、「対象システム：e-staffing」の直下に「契約番号：C103724569-007」が正しく表示されること、および3項目の修正内容がすべて正しく表示されることを実機確認済み。テストデータは削除済み。更新申請（`/apply?renewal=`）経由での「新しいCSV行の契約番号」反映は未検証のまま（該当データなし）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-30「CSV修正管理部通知メールへの契約番号追加」参照。

**教訓**：このCLAUDE.mdの一覧が`SYSTEM_DESIGN.md`9-1章の実際のステータスと2回連続でズレていた（完了済みタスクが「次の優先タスク」に残り続けていた）。今後、この一覧を更新する際は必ず9-1章の該当項目を番号ごと確認してから転記すること。

**ドキュメント整備TODO**：【完了・2026-07-17再確認】`contracts`・`company_master` の正式DDLは2026-06-30に取得済みで`docs/SYSTEM_DESIGN.md` 3-6/3-7は既に確定版に差し替え済み（10章2026-06-30参照）。本行はその後も長らく未完了として残っていたが、総合レビュー指摘39（DDL取得状況の矛盾）の再検証で実態と食い違っていたことが判明したため訂正した。

---

*詳細・経緯・差分監査表・全PENDINGは `docs/SYSTEM_DESIGN.md` を参照。*
