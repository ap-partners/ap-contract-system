# CLAUDE.md ― APパートナーズ 契約書管理システム

> このファイルはセッション開始時に自動で読み込まれる「常時オンの薄い層」です。
> **詳細仕様（テーブル定義・CSVマッピング・STEP仕様など）は `docs/SYSTEM_DESIGN.md` にあります。**
> 必要になったときだけ `docs/SYSTEM_DESIGN.md` の該当セクションを読んでください。ここには全体像と運用ルールだけを置きます。

---

## ⚙️ 運用ルール（必ず守る）

1. **1チャット＝4機能まで** で区切る（2026-07-15変更：従来は1機能。チャット引き継ぎ自体のトークンコストと、1本に詰め込んだ場合のコンテキスト劣化コストを天秤にかけ、伊藤さんの判断で4機能まで許容に緩和）。長い1本に詰め込まない（コンテキスト劣化＝忘れの最大要因）。関連性の低い機能を無理に4つ束ねるのではなく、区切りの良いタイミングで新チャットに移ること。
2. **仕様・判断が決まったら、その場で差分だけ追記する**。`docs/SYSTEM_DESIGN.md` を**全文再生成しない**こと（トークン浪費）。確定仕様は該当章を部分編集、判断の経緯は「意思決定ログ」に1行追記。
3. **実装コードを正（source of truth）とする**。設計書とコードがズレたらコードを優先し、ズレは設計書側を訂正する。
4. **コードと設計書の突合（監査）は節目だけ**（機能の区切り・本番リリース前など）。毎回はやらない（重い）。
5. **CSV・Excel等の機密ファイル（個人情報・契約データ）はGit管理対象外**。コミット前に `.gitignore` が効いているか必ず確認（過去に誤アップロード事故2回）。
6. テーブル／ファイルを「無いはず」と判断する前に、**必ず実環境（Supabaseのテーブル一覧・GitHub・ローカル）を確認**する（思い込みによる衝突事故あり）。
7. 「マッピングできてる？」等の確認質問には即答せず、**実データ・実コードを再検証してから回答**する。
8. ユーザー（伊藤さん）はプログラミング知識ゼロ。手順は「VSCodeを開く」から省略せず全手順を書く。
9. **画面に表示する文章は、改行位置を意識して読みやすく配置する**。特に警告・確認メッセージ・バナー等は、意味の切れ目（句点等）で改行し、不自然な位置で折り返さないようにする（2026-07-01決定）。
10. **「未実装だから」を理由に、その場しのぎの最小限対応で済ませない**。関連する新機能に着手する前に、`docs/SYSTEM_DESIGN.md`・過去のトーク履歴（`過去のトーク履歴.docx`）を確認し、想定より大きな設計・仕様がすでに決まっていないか必ず確認する。実装できるのは「計算ロジックなど中身」だけが後回しになる場合でも、**将来の完成形を見据えた構造（骨格・データ設計・UIの出し分け）は今のうちに作っておく**。中身が空でも、後から中身を差し込むだけで済む形にしておくことが目的（2026-07-02決定）。
11. **既存の大きなファイル（目安：数百行以上）を1〜数行だけ直したい時は、伊藤さんに「コードをチャットに貼り付け」ではなく「ファイルそのものをドラッグ＆ドロップでアップロード」してもらう**。貼り付けだとClaudeが全文を手で書き写す必要が生じ、トークン消費・時間・写し間違いリスクが大きい。ファイルをアップロードしてもらえれば、Claudeはコピーコマンドで正確に複製した上でピンポイント編集でき、速く正確（2026-07-02決定・教訓）。
12. **レイアウト・配置・見た目など視覚的な修正は、実装前に必ず方向性を伊藤さんに確認する**。PDF帳票のレイアウト崩れ・要素の位置ズレ・画面のデザイン変更など「見た目」に関わる修正依頼を受けた際、いきなりコードを直してレンダリングテストで確認する、という進め方はしない。先に「原因はこうで、対策としてこういう見た目にしようと思うがどうか」を、可能であればモック（簡易な画像・レイアウト案・文章での配置イメージ）を示して確認を取り、伊藤さんの合意を得てから実装に着手する。合意なく直してテストさせると、期待とズレていた場合に伊藤さんの確認の手間・時間が二重にかかるため（2026-07-09決定・教訓：印鑑位置修正で先に実装→テストしてしまい指摘を受けた）。
13. **伊藤さんがVercelのデプロイ完了を伝えたら、Claudeは自分でブラウザ（Claude in Chrome）とGmail連携を使って実機セルフチェックを行う**。伊藤さんに手作業で確認させるのではなく、Claude自身が①該当ダッシュボードにテストアカウントでログインし、対象の画面操作（承認・PDFプレビュー・署名フロー等、その回の変更内容に応じたもの）を実行し、②必要であれば送信された通知メールをGmail連携（ito@appart.co.jp）で実際に開いて内容を確認する。SELECT（閲覧）だけでなく、承認・保存などの更新系操作も含めて実際に画面から実行して確認すること（2026-07-15の教訓：RLS変更をSELECT確認だけで済ませ、UPDATE時のみ発生する不具合を見逃した）。問題が見つかった場合はその場で原因調査・修正まで行い、伊藤さんには結果（正常だったこと／見つかって直した不具合）を報告する（2026-07-15決定）。
14. **コード修正を行ったら、その回答の中で必ず`git add`・`git commit`・`git push`の3行をそのまま貼り付けられる形で毎回明示する**。「先ほどのコマンドで」「デプロイしてください」等の省略表現で済ませない。git操作は必ず伊藤さんのVSCode等の**ターミナルから**実行してもらう（Claudeがbashサンドボックス経由で直接pushすることはしない。過去にbashのファイルキャッシュ遅延でファイル破損事故が2回発生しているため）。1つの依頼の中で複数箇所を修正した場合も、全ての修正が終わった最後に1回だけ、その時点の全差分をまとめてpushするよう案内する（毎回の小修正のたびに逐一pushを促さない）（2026-07-16決定・教訓：pushの案内を省略し、伊藤さんに「まだpushしてないけど」と指摘された）。

---

## 🗺️ システム全体像（サマリ）

人材派遣会社APパートナーズの**担当営業が、雇用契約書・就業条件明示書をWebで発行申請する**システム。発行→SSC承認→従業員署名→保管までWeb完結。月額¥0運用。

**技術**：Next.js(App Router)/TypeScript/Tailwind/Supabase/Vercel自動デプロイ
**本番**：https://ap-contract-system.vercel.app/apply ／ **GitHub**：github.com/ap-partners/ap-contract-system
**Supabase**：argpiiznuzxmmqraynfo（東京） ／ **ローカル**：`C:\Users\ito\Desktop\ap-contract-system`
**テストユーザー**：sales-test / ssc-test / admin-test @appart.co.jp（全て `Test1234!`）

**ロール**：担当営業（申請）／SSC（確認・承認）／管理部（CSV取込・マスタ・監視）／従業員（署名）

**テーブル（14・2026-07-15訂正）**：`staff` `department_master` `master_imports` `csv_imports` `csv_raw_data`（以上投入済み）／`contracts`（申請本体・稼働中）／`company_master`（派遣元設定key-value・稼働中）／`requests`（依頼管理）／`renewal_candidates`（更新期限管理）／`staff_roles`（ロール管理）／`minimum_wage_master`／`standard_working_hours_master`／`dispatch_fee_master`／`csv_diff_logs`（未使用・骨格のみ）。このほか`public.users`/`clients`/`documents`/`alert_settings`/`notifications`はコード内で未参照の初期テンプレート残骸・未使用機能で、削除は保留中（詳細はSYSTEM_DESIGN.md 10章2026-07-15参照）。
→ 各カラム定義は `docs/SYSTEM_DESIGN.md` 第3章（ただし`dispatch_fee_master`・`renewal_candidates`・`staff_roles`はDDL節が未整備。指摘38の残課題）。

**画面（2026-07-15訂正）**：`/apply` `/login` `/dashboard/sales` `/dashboard/ssc` `/dashboard/admin` `/dashboard/ssc/contracts/[id]` `/dashboard/sales/contracts/[id]` `/sign/[id]` は本番稼働中。`/help/*`・アルバイト誓約書システムは未実装。以前この行は「/applyのみ本番稼働」としていたが、実装済みリストと自己矛盾していた（総合レビュー指摘38）。
**運用フェーズについて（2026-07-13念押し）**：本システムは現時点で**本稼働（実際の従業員の業務がこれに依存している状態）していない**。実際のCSVデータ・実在のスタッフ名義を検証に使っていても、それは全てテスト・検証目的であり、DB上に「止まっている」案件等が見つかっても実害・実業務への影響は無い。不具合の緊急度を判断する際、この前提を誤って「実運用に影響あり」と評価しないこと。

**書類パターン**：A=雇用契約書(6STEP)／B=就業条件明示書(6STEP)／C=兼用(8STEP)。社内は雇用契約書(A)のみ。
→ STEP内訳・自動反映ロジックは `docs/SYSTEM_DESIGN.md` 第4章。

**CSV連携**：e-staffing/HRstation/winworks/Staffiaの**4システム**（全件投入済み）。Staffiaのみ「KEF00103（契約詳細）」「KEF00104（スタッフ個人・派遣期間）」の2ファイル構成で、個別契約書番号をキーにコード側で合成する（system_typeとしては'Staffia'1つ。DB制約・実データで確認済み・2026-07-15訂正。旧記述「5ソース」はファイル数とシステム数を混同した誤解を招く表現だった）。検索キー・マッピングは `docs/SYSTEM_DESIGN.md` 第5章。

---


## ✅ 実装済み / 🔜 次にやること

**実装済み**：申請ウィザード`/apply`本体、staff・部門・5システムCSVの投入、CSV検索→STEP2/3自動反映（4システムすべて実機確認済み）、派遣元情報(STEP4)のCSV優先表示、CSV反映項目を営業が修正した場合の特別対応（STEP8）、contractsへの申請保存、STEP1「アルバイト」選択肢追加（2026-07-02）、STEP1/STEP2の依頼（スタッフマスタ登録・CSVインポート）を`requests`テーブルへ実際に保存する処理（2026-07-02）、SSC一覧・個別確認画面から社内案件を除外（2026-07-03）、管理部ダッシュボード「依頼管理」タブ（絞り込み・取消機能。2026-07-03）、担当営業ダッシュボードのタブ再編＋「依頼状況」タブ（部門全体の依頼状況表示。2026-07-03）、自動チェック機能（金額異常値・最低賃金・就業規則整合）の判定ロジック本体＋SSC確認画面へのつなぎ込み（2026-07-06）、帳票PDF生成（雇用契約書＝パターンA・就業条件明示書＝パターンB・兼用版＝パターンC　全対応。2026-07-07/08）、署名機能（フェーズ5）の基盤部分（メール送信・Google Drive連携・DBカラム・SSC承認/説明完了時の自動通知API・PDFへの署名画像埋め込みの仕組み。2026-07-08）、`/sign/[id]`本体（本人確認・パターンA/C=フルネーム入力→丸印鑑自動生成（クラウドサイン方式。手書きサインではない）・パターンB内容確認ボタン・PDF再生成・Google Drive保存・ステータス更新。2026-07-09）、スタッフマスタへの住所（`staff.address`）取込追加＋実データ1748件の再インポート（2026-07-09）、帳票PDFの角印レイアウト最終化・長い自由記述の折り返し＋自動縮小・1ページ収容の余白調整（2026-07-10）、署名機能のVercel実機確認＋ダッシュボードでの署名済みPDFプレビュー・`/sign/[id]`生年月日入力欄の改善（2026-07-10）、SSC一括承認の完全実装（`handleBulkApprove`から選択各契約に`notify-sign-request`APIを呼ぶ処理を追加。Vercel実機で動作確認済み。確認ステップ・処理中/完了の全画面オーバーレイ表示も追加。2026-07-13）、就業条件明示書（パターンB）がSSC承認後に進まない不具合の修正（`notify-sign-request`の判定条件修正。個別・一括承認どちらにも適用。2026-07-13）、担当営業ダッシュボードへの「確認済み日時」可視化（一覧・詳細画面の両方に`signed_at`を表示。2026-07-13）、SSCダッシュボード一覧への「確認済み日時」可視化（2026-07-13）、SSC承認画面の文言修正（パターンBは「署名依頼」ではなく「確認依頼」、一括承認は「署名・確認依頼」に統一。2026-07-13）、就業条件明示書（パターンB）PDFへの角印・会社名表示の追加（従来は角印関連コード自体が無く、全ステータスで角印が出ない不具合だった。パターンA/Cと同じ表示ルール・同じ内容ブロックを流用。2026-07-13）、担当営業ダッシュボード一覧への帳票種別バッジ・派遣期間（パターンB・C）表示の追加（SSC一覧と同等の情報量に統一。2026-07-13）、管理部ダッシュボードへの「契約一覧」タブ追加＋SSCと同等の承認・強制承認・差し戻し・一括承認権限の付与（フェーズ1完了。SSC詳細画面`/dashboard/ssc/contracts/[id]`を管理部にも開放する形で実装。Vercel実機確認済み。2026-07-13）、`/apply`のロールゲート緩和（フェーズ2完了。担当営業に加えSSC・管理部も申請可能に。就業場所区分の選択肢は3ロールとも同一＝絞り込みなし。2026-07-13）、管理部ダッシュボードへの「社内承認」タブ追加（フェーズ3完了。`user_metadata.is_internal_approver`フラグを持つ人にのみ表示。社内案件のみを表示し、SSCを通さず直接承認・差し戻し・一括承認できる。タブに未対応件数バッジも追加。2026-07-13）、社内案件でも「SSC」表記が出てしまっていた箇所の修正（締結パターン説明文・申請完了画面・担当営業ダッシュボードのバナー・ステータス表示・社内承認タブのバッジ、計6箇所。ステータスの内部値自体は`SSC承認済み`のまま変更していない。あわせて申請完了画面の「ダッシュボードに戻る」ボタンがSSC・管理部の場合も担当営業のダッシュボードに固定されていた不具合も修正。2026-07-13）。フェーズ4「SSC代理承認」はフェーズ1で実現済みと判断し追加実装なしで完了扱いに（2026-07-13）。`/sign/[id]`本人確認方式を「社員番号＋生年月日」から「社員番号＋メール記載の6桁認証コード」へ変更（DBスキーマ追加・`notify-sign-request`でのコード生成/メール記載・`/api/sign/[id]/verify`及び`complete`でのコード照合・5回誤入力での失効・2日間有効期限・再発行API・`/sign/[id]`画面のUI変更、全て実装。Vercel本番環境で申請→SSC承認→通知メール送信→正しいコードでの成功／誤コードでの回数表示／失効・再発行、までの一連の流れを実機確認済み。2026-07-14）、SSC・管理部・担当営業の3ダッシュボードへの絞り込み・並び替え・テキスト検索機能の追加（共通コンポーネント化。「承認済み・完了」タブの署名待ち／署名済み混在で目当ての案件が探しにくいという課題に対応。2026-07-14）、実際には到達しない「完了」ステータスの絞り込みピルを3ダッシュボードから削除＋「承認済み・完了」タブを「承認済み・署名状況」に名称変更（2026-07-14）、`contracts`テーブルへの`created_by_dept_no`インデックス追加（スケーラビリティ改善タスク①・見た目/動作への影響なし。2026-07-14）、SSC・担当営業・管理部の3ダッシュボードのUIリニューアル（伊藤さん作成のデザイン案をレビュー・型エラー修正の上で適用。モダンなSaaS系デザインに刷新。管理部にはSSC・担当営業同様の「新規発行申請」ボタンも追加。2026-07-14）、リニューアル後の全体監査で見つかった不具合の修正（担当営業の契約詳細画面に帳票PDFプレビューが元々実装されていなかった問題への対応、管理部の自動チェック警告バッジの赤/黄重要度表示の復元、SSC・管理部の一括承認確認ダイアログの説明文および警告バッジの補足表示・絵文字の復元。2026-07-14）、スケーラビリティ改善タスク②（全件取得の是正）の実装（`contracts.search_text`カラム追加＋バックフィル、共通フック`useApprovedAccumulator`新設、SSC・管理部・担当営業の蓄積型タブへ直近45日絞り込み・さらに読み込む・全期間で検索を実装。Vercel実機確認済み。2026-07-14）、3ダッシュボード共通の細かい見た目修正（契約期間欄の文字サイズ縮小で折り返し解消、詳細ボタンの文字が狭い画面幅で縦に割れる不具合をwhitespace-nowrapで解消。2026-07-14）、依頼フォームの就業場所名の必須条件修正（スタッフマスタ登録のみは不要・CSVインポートが絡む依頼は必須に変更。STEP2の単独CSVインポート依頼に就業場所名入力欄を新設し`client_name`を保存するよう修正。依頼一覧（管理部「依頼管理」・担当営業「依頼状況」）に申請者名・部門名・就業先・入社日を漏れなく常時表示するよう両カードを統一。2026-07-14）、管理部ダッシュボードへの「サマリー」タブ新設（依頼未対応・契約承認待ち・社内承認待ちをカード形式で横断表示し、クリックで該当タブへ遷移。CSV差異・更新期限は未実装のため準備中カードとして枠のみ設置。ログイン後の初期表示タブに変更。「依頼管理」タブから「承認待ち」カードを撤去。2026-07-14）、依頼一覧（管理部「依頼管理」・担当営業「依頼状況」）への直近45日デフォルト絞り込み追加（依頼データが年数とともに無制限に蓄積し件数の意味が薄れる問題への対応。2026-07-14）、`app/apply/page.tsx`分割Phase1・2（Reactに依存しない純粋関数・定数を`app/apply/_lib/helpers.ts`へ、小さな再利用UIコンポーネントを`app/apply/_components/FormParts.tsx`へ切り出し。4166行→3193行に削減。STEP1〜8本体のロジックは無変更。tsc検証済み。2026-07-14）、「この申請をやめる」等のダッシュボード復帰ボタンが管理部・SSCでログイン画面に飛ばされる不具合の修正（ロール判定なしで`/dashboard/sales`に決め打ちしていたのが原因。役割ごとの分岐に統一。Vercel実機確認済み。2026-07-14）、総合レビュー指摘11・12対応（一括承認の選択残留・二重承認ガード）：SSC・管理部（契約一覧・社内承認タブ）で絞り込み・検索・並び替えを変えたら選択状態を必ずクリアするuseEffectを`RenewalManagementTab.tsx`と同じ考え方で追加、個別承認・強制承認・差し戻し（SSC詳細画面）と一括承認（SSC・管理部の計4箇所）のUPDATEに`.eq('status','申請中')`の条件付き更新を追加し、実際に更新できた件数だけを通知・完了扱いにすることで二重承認・notify-sign-requestの二重送信を防止（2026-07-15）。UIUX総合レビュー（実機・2026-07-16）指摘の対応（D・N・A・M・E・G・C・F・I・Bの10項目：STEP1フリーズの原因調査＝ブラウザ自動化ツール固有の見かけ上の問題と判明・更新期限メール宛先不在時の管理部フォールバック・ダッシュボード横スクロール解消・更新期限メール二重送信の検証＝実害なしと確認・申請者のID断片表示を氏名表示へ・強制承認/差し戻しボタンの主副整理・担当営業KPIカード/タブの重複解消・警告バッジの色と意味の不一致修正・ログインオートフィル改善・3ダッシュボードのタブ階層統一）、全て実機確認済み・本番反映済み。あわせて署名依頼メール（`sendSignRequestMail()`）の品質改善（件名の「認証コード在中」削除・書類種別の件名/本文追加・HTML化＋文字版フォールバック・ボタン文言/余白調整・認証コード表示の白背景化・宛名への氏名追加・案内文言の明確化）も実施・実機確認済み（2026-07-16）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。認証方式自体の見直し（SMS等別チャネル併用）は伊藤さんの判断により検討自体を取りやめ・対応不要と確定（2026-07-22）。更新期限管理タブのチャットC・D（一括申請・個別申請の契約データ生成処理）を実装・デプロイ・実機確認（`executeBulkApply()`＝選択した更新候補から`contracts`へ実際にINSERTする一括申請処理をSSC一括承認と同様の確認/処理中/完了オーバーレイUIで実装、`/apply?renewal=候補ID`＝前回契約とCSV最新反映内容をマージしてSTEP1〜7を自動プリフィルし最終確認ステップへ直行する個別申請モードを`?edit=`再申請モードと同じ設計パターンで新規実装。共通の前回⇔CSVフィールド対応表`renewalFieldMap.ts`を新設し両経路で共用）。実装過程で発見・修正した実データ由来の不具合3件：①所定労働日数のCSV反映が実は一度も使われていなかったための誤差分表示の解消、②`staff`テーブルに存在しない`department`列を参照していたため一括申請のstaff_snapshotが全件nullになっていた不具合の修正（`department_master`joinへ変更）、③CSVシステムによって数値で返る項目（Staffiaの休憩時間等）が個別申請プリフィルの文字列専用処理で実行時エラーになり画面が真っ白になっていた不具合の修正（`String()`強制変換）。あわせて業務ルールとして、更新申請では試用期間を前回値に関わらず必ず「無」に自動設定し前後差分を明示する仕様を決定・実装、および3ダッシュボード共通のタブバー縦スクロールバー誤表示（`overflow-x-auto`のCSS仕様上の副作用＋1pxのサブピクセル超過が原因）を修正。すべてVercel実機確認済み（2026-07-17）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。これにより更新期限管理タブ（チャットA〜D）は一通り完成。マスタ系テーブル（`minimum_wage_master`・`standard_working_hours_master`・`dispatch_fee_master`）のRLS見直しをSupabase MCPで実環境調査し、3テーブルとも既に管理部ロール限定で強化済み・`dispatch_fee_master`はサービスロール専用の意図的設計であることを確認、追加変更は不要と判断（2026-07-17）。あわせて総合レビュー指摘のうち未対応で残っていた指摘25（担当営業の部門情報ロード中の検索誤表示）・26（署名失敗時の案内文言の分かりにくさ）・27（保存失敗時のPostgres生エラー表示）・35（verify APIのデッドコード`isPatternC`）を対応し、CLAUDE.md・SYSTEM_DESIGN.mdの残っていた古い記述（DDL取得状況の矛盾・7-5章見出し・`staff.birthday`コメント・ドキュメント整備TODO）も訂正（要デプロイ・未実機確認・2026-07-17）。【実装完了・要デプロイ＋実機確認・2026-07-17】従業員向けマイページ（`/staff/mypage`）を新規実装：過去のトーク履歴で確定していた仕様⑨（A．署名待ち書類の確認・署名／B．署名済み・確認済み書類の一覧・閲覧、新しい順で直近のみ表示し「過去の書類を見る」で展開／お知らせ機能は今回スコープ外と確定）に基づく。認証方式は伊藤さんの判断で「社員番号＋パスワード」の持続ログイン（当初設計どおり。`staff.password_hash`・`is_initial_login`を実際に使用開始）とし、初回ログイン・パスワード再設定は「社員番号＋メール記載の6桁認証コード」で本人確認する（`/sign/[id]`と同じ考え方だが、認証コードの単位を契約ごとから従業員ごと＝`staff`テーブルに変更）。新規：`middleware.ts`（`/staff/mypage`以下をセッションCookieで保護）、`lib/staffPassword.ts`（scryptによるパスワードハッシュ）・`staffSession.ts`（HMAC署名付きセッションCookie。`lib/pdfAccessToken.ts`と同じ方式で新規環境変数なし）・`staffAuth.ts`・`staffResetToken.ts`、API（`/api/staff/login`・`request-code`・`verify-code`・`set-password`・`logout`・`me`・`documents/[id]`）、画面（`/app/staff/login`・`/app/staff/mypage`・`/app/staff/mypage/documents/[id]`）。既存の`/api/sign/[id]/complete`はセッションCookieがあればそちらを優先し、無い場合のみ旧・契約単位の認証コード方式にフォールバックする形に変更（後方互換）。`notify-sign-request`は、`staff.is_initial_login`に応じてメール内容を出し分ける（未設定なら認証コード付き、設定済みならログイン案内のみ）よう変更し、契約単位の認証コード発行はやめた。DB：`staff`に`login_auth_code`・`login_auth_code_expires_at`・`login_auth_attempts`・`login_password_attempts`列を追加した際、これらの機密列を社内ユーザー（担当営業・SSC・管理部）のanonキー経由`select('*')`から読めてしまわないよう、列単位権限（`REVOKE SELECT`）で`authenticated`ロールから明示的に遮断済み（service roleは影響なし）。【完了・実機確認済み・2026-07-17】デプロイ後、初回ログイン→マイページ表示直後にログイン画面へ戻されてしまう不具合を発見・修正：原因はNext.jsのmiddlewareが既定でEdge Runtime動作するため、`lib/staffSession.ts`が使っていたNode標準`crypto`モジュール（`createHmac`・`timingSafeEqual`）がEdge Runtimeで使用不可でトークン検証が常に失敗していたこと。Web Crypto API（`crypto.subtle`）ベースの非同期実装に全面書き換えて解消。あわせて伊藤さんの指定でパスワード要件を「8文字以上」のみから「8文字以上・英大文字/小文字/数字の3種類必須」に強化（`lib/staffPassword.ts`・サーバー側・クライアント側とも対応）。デプロイ後、Claude in Chrome＋Supabase MCP＋Gmail連携で①パスワードログイン→署名待ち書類の確認・署名→マイページに戻るボタンでの遷移②認証コードでの初回ログイン→弱いパスワードの拒否確認→3種類を満たすパスワードでの設定→即マイページ表示③ログアウト→パスワードでの再ログイン、を実機確認済み。検証で実在の従業員アカウント（社員番号105026）に設定したテストパスワードは確認後に初期状態へリセット済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。スタッフマスタ取込のバルク処理化（チャンク単位でのまとめてSELECT→JS側判定→upsert方式へ全面書き換え。Vercelタイムアウト対策）＋SBクルーコード保持ロジック＋担当営業からの「スタッフ登録依頼」自動完了機能（`runStaffRegisterAutoMatch`）を新規実装し、実データのStaffExpressエクスポート（1908行）で本番投入・実機確認済み（新規35件・更新1748件・スキップ125件・エラー0件、crew_code保持157件は1件も消えず維持）。あわせてテストデータでスタッフ登録依頼→インポート→自動完了→通知メール→申請反映までの一連フローも実機確認済み（2026-07-21）。個別契約CSVインポート（e-staffing・HRstation・winworks・Staffia）についても、実データ（2026/7/1付CSV5ファイル）を使い、担当営業からの依頼→管理部インポート→自動マッチ→依頼ステータスの`completed`化→通知メール送信→担当営業側`/apply`STEP2での反映確認、まで4システムすべてClaude in Chromeで実機検証済み（新規/更新/保護件数の突合、自動マッチ成立の確認含む。2026-07-21）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。【完了・実機確認済み・2026-07-22】社内システム担当者によるQA総合監査（`QA監査進捗_2026-07-22.md`）の新規指摘5件（①C1セッション衝突＝別タブで別アカウントにログインされたことを`onAuthStateChange`で検知し強制ログアウトする`lib/useSessionCollisionGuard.ts`を新設しSSC・管理部・担当営業の計5画面に組込み／②C2重複申請＝同一スタッフ・同一書類種別で「申請中」の案件が既にある場合`app/apply/page.tsx`の新規申請保存をブロック／③D1オートフィル汚染＝`app/staff/login/page.tsx`の社員番号欄に`autoComplete="off"`／④A1設計書ズレ＝5-3章のCSVマッピング記述を実装（`helpers.ts`行番号付き）に合わせて訂正／⑤B1文字数上限なし＝`NoBreakTextarea`等の自由記述欄に既定2000文字の`maxLength`を追加）に対応し、①〜⑤すべてVercel実機（2タブでの実際のログイン競合・重複申請の実送信ブロック・DOM上のautocomplete属性・入力上限の動作）で確認済み。あわせてこの実機確認の過程で見つかった「STEP3で雇用期間の終了日を空欄のまま次へ進むとフリーズする」という事象は、`window.alert`を差し替えて再現検証した結果、`validatePeriod()`が正しくアラートを出してSTEP3に留まる正常な入力チェック動作であり、実際にフリーズして見えたのはブラウザ自動化ツール側がネイティブalertダイアログでCDP通信をブロックされていたための見かけ上の問題と判明（対応不要と判断）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。総合レビュー指摘事項（2026-07-14）の未対応9件のうち判断不要な4件に対応（要デプロイ）：指摘28・30は調査の結果いずれも既に意図通り・対応済みと判明（バグではない）、指摘34は`useContractListToolbar.tsx`のコメントを実装（`length > 0`判定）に合わせて訂正、指摘36は`/sign/[id]`のPDF埋め込み用印鑑canvasがプレビューと同解像度のままだった点を560×560に拡大して解消。残る5件のうち29（申請者ID表示）は2026-07-16の対応で既に実質解消済みと確認（追加対応不要）、32（検索デバウンス・エスケープ）・33（依頼一覧の絞り込みUI統一。依頼一覧専用の共通フック新設＋担当営業への検索欄追加）は実装・デプロイ・実機確認済み、39・40（ドキュメントの章間矛盾・トーク履歴の未反映事項）はコード変更を伴わないドキュメント修正のみで対応済み。以上で総合レビュー指摘40件はすべて着手・判断済みとなった。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。【実装完了・要デプロイ・2026-07-22】伊藤さんの依頼で全メールテンプレート（`lib/mail.ts`6種）を監査：4種は文面・件名・レイアウトとも問題なし、CSVインポート・スタッフ登録の完了通知2種のみHTML版が無かったため他と同じ装飾を追加し統一。あわせてブラウザタブに表示されるページ名がNext.js初期値「Create Next App」のまま放置されていたことが判明し、社内向け画面を「契約書管理システム」、従業員向けマイページ（`/staff`配下）を「マイページ | 契約書管理システム」に変更（新規`app/staff/layout.tsx`で指定）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-22参照。

**次の優先タスク**（詳細は `docs/SYSTEM_DESIGN.md` 第9章。以下は9-1章の実際のステータスと突き合わせ済み・2026-07-08）
1. 【完了・2026-07-10】署名機能`/sign/[id]`のVercel実機確認：社員番号105611・105315で申請→SSC承認→自動メール送信→本人確認→丸印鑑生成→署名完了→Google Drive保存、までの一連の流れを本番環境で確認済み。あわせて派生対応も完了：①ダッシュボードの「帳票PDFプレビュー」が署名済み契約では常に未署名の見た目を再生成していた不具合を修正（`drive_file_id`がある場合はGoogle Driveの実物PDFを返すよう変更）、②`/sign/[id]`の生年月日入力欄を`<input type="date">`から「半角数字8桁を1つの欄に連続入力」方式に変更。なお以下は意図的に後回しにしたスコープ外：①本人確認失敗時の試行回数制限、②URL有効期限（7日間固定）の実際のアクセス制御としての実施と期限切れ時の再発行フロー、③`署名済み→完了`の遷移設計（ダッシュボードの「完了」タブは両ステータスを既にまとめて表示するため当面支障なし）、④丸印鑑で氏名がスペース区切り3単語以上の場合に文字がはみ出す不具合（伊藤さん確認の上「クラウドサインも同様の見た目のため対応不要」と判断・対応保留）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-10参照
2. 【完了・2026-07-14】`/sign/[id]`の本人確認方式を「社員番号＋生年月日」から「社員番号＋メール記載の6桁認証コード」へ変更：DBスキーマ・`notify-sign-request`・`/api/sign/[id]/verify`/`complete`・再発行API・`/sign/[id]`画面のUIを全て実装。Vercel本番環境で正しいコード・誤コード・失効/再発行の一連の流れを実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
3. 【全フェーズ完了・2026-07-13】社内承認＋SSC代理承認のまとめた実装：フェーズ1（契約一覧タブ）・フェーズ2（`/apply`のロールゲート緩和）・フェーズ3（管理部ダッシュボードへの「社内承認」タブ追加。`is_internal_approver`フラグ方式・画面側での制御。現在は`admin-test`のみフラグ設定済み、実際の5名分は伊藤さんがSupabase管理画面で個別に設定）が完了。フェーズ4（SSC代理承認）は、フェーズ1で管理部ロール全員が無条件・いつでも現場案件を承認できる仕組みが既にできているため「実現済み」とみなし、追加実装しないことで伊藤さんと確定。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
4. 管理部ダッシュボードの残り3タブ：CSVインポート（自動マッチ・自動通知含む）／CSV差異アラート／更新期限管理。【2026-07-14伊藤さん了承】着手順は「更新期限管理」を先行（本稼働後の実害が大きいため）、その後CSVインポート自動化・CSV差異アラート。タスク8の次に着手。【実装完了・要Vercelデプロイ＋実機確認・2026-07-15】CSVインポートタブ：管理部が画面からCSVをアップロードして取り込めるようにした（`app/api/admin/csv-import/route.ts`新設・`lib/csvImportShared.ts`新設）。伊藤さんとの再確認の結果、旧来の「CSV差異アラート」構想（書類ステータス別の多者通知・専用ダッシュボード）は簡略化し、①CSV上書きは基本OKだが「申請中以降のステータスの契約」から参照されているCSV行は上書きしない、②CSVインポート依頼の自動マッチにsystem_type（システム名）一致も条件追加、③自動マッチ成立時は自動完了＋依頼元へメール通知、の3点のみを実装。CSV差異アラートタブは別ダッシュボードを作らず案内文言のみ更新。実装過程でcsv_raw_data・csv_importsのRLSが「認証ユーザー全件読み書き可」のままだった点も発見し、書き込みをservice_role専用に強化済み（DB側のみ・再デプロイ不要）。伊藤さん提供の実CSV5ファイルでパース・件数・保護対象検知をSupabase MCP経由で検証済みだが、bashサンドボックスからSupabase本体へのネットワーク接続ができない制約により、実際のファイルアップロードAPIの一連動作（アップロード→保存→自動マッチ→通知メール）はこのセッション内では実機確認できていない。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。【完了・実機確認済み・2026-07-21】上記の実機確認を実施：担当営業からの依頼→管理部インポート→自動マッチ→依頼ステータス`completed`化→通知メール送信→担当営業側`/apply`STEP2での反映確認まで、4システム（e-staffing・HRstation・winworks・Staffia）すべてClaude in Chromeで実データを使い通しで検証済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。【実装完了・要デプロイ＋実機確認・2026-07-17】CSVインポートタブへのStaffExpress（スタッフマスタ・部門マスタ）取込追加：当初のタスク6のスコープに含まれていたが2026-07-15実装時に見落とされていた部分。既存の`scripts/import-master.js`のロジック（列順序・変換ルール・除外ルール・日付タイムゾーンバグ再発防止策）をそのままWeb化し、CSVインポートタブ内に5つ目のシステム選択肢として追加（新規タブは作らず）。上書き方針は契約CSVと異なり「全件上書き（employee_numberキー）」。列順序・注記のアコーディオン表示、取込履歴（`master_imports`）表示も実装。【完了・実機確認済み・2026-07-17】デプロイ後、安全のためテスト用の小さいxlsxファイル（実データと衝突しない部門NO=9999・社員番号555501等）を使い、部門マスタ・スタッフマスタそれぞれ新規追加、8/9始まり社員番号の除外ルール、`master_imports`への履歴記録・画面表示までをClaude in Chrome＋Supabase MCPで実機確認。テストデータは確認後すべて削除済み。あわせてタスク5番のマスタ管理タブへの伊藤さんフィードバック対応（「Claudeにご相談ください」の文言修正、最低賃金マスタの表形式への再設計・「未使用の可能性」バッジ表示）も同日デプロイ後に実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。**【フェーズ1完了・全面再監査＋修正済み・2026-07-14】更新期限管理**：新規テーブル`renewal_candidates`＋共通フック`useRenewalCandidates.ts`＋共通コンポーネント`RenewalManagementTab.tsx`を実装し、管理部（全部門）・担当営業（自部門のみ）・SSC（全部門・閲覧＋意向確認のみ、`canFinalize`propで最終確定操作を制御）の3ダッシュボードすべてに組み込み済み。実装後、機能充足性・UI/UXデザイン（既存3ダッシュボードと同じ角丸カード×行形式へ刷新・残日数KPI・絞り込み検索・csv_pending再検索ボタン）・動作面（保存エラーの可視化、日付入力のtype=date化、初期値バグ修正、送付準備完了バッジ、選択状態クリア等）の3段階で再監査・修正済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14の一連のエントリ参照。**【フェーズ2完了・2026-07-15】自動メール通知**：45/30/20/14/7日の残日数しきい値で、担当営業ごとに1日1通のダイジェストメールを自動送信（TO=担当営業、CC=SSC・管理部）。Vercel Cron（毎日8時台JST）→`app/api/cron/renewal-notify/route.ts`→`lib/mail.ts`の`sendRenewalDigestMail()`という構成。実際の担当営業アカウントがまだ無いため、環境変数`RENEWAL_NOTIFY_OVERRIDE_EMAIL`（`ito@appart.co.jp`固定・本番前に解除必要）で送信先を伊藤さんのアドレスに差し替え中。【完了・実機確認済み・2026-07-16】Vercel環境変数`CRON_SECRET`・`RENEWAL_NOTIFY_OVERRIDE_EMAIL`を伊藤さんが追加・再デプロイ済み。Claudeが`/api/cron/renewal-notify`を実機確認（200 OK・sent:2、Gmail(ito@appart.co.jp)で2通受信も確認）。更新期限管理フェーズ2は完全に動作確認完了。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。**残タスク（次回以降）**：【設計確定・実装未着手・2026-07-16】更新期限管理タブの大幅改修方針を伊藤さんと確定した（①所属部署・雇用形態の追加表示②書類種別表示③データ元3パターン整理④差異確認の表示範囲拡大⑤「手入力に切替」ボタンを`/apply`プリフィル＋STEP8直行の新規申請フローへ再設計＝「全項目を編集」フル編集画面連携はこの形で実現予定⑥原契約・反映内容のSTEP8形式確認画面新設⑦同一期間での誤申請防止の警告チェック⑧スタッフ意向・クライアント意向トグルの廃止・ステータス簡略化⑨ボタン文言の行動ベースへの統一）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16「更新期限管理タブの改修方針を確定」参照。実装はチャットA（①②⑧⑨）→B（④⑥⑦）→C（⑤一括申請）→D（⑤個別申請・`/apply`連携）の4分割で確定。【完了・実機確認済み・2026-07-16】チャットA実装済み：DB（`renewal_candidates`から意向2列削除・`document_type`列追加・関数更新）、`useRenewalCandidates.ts`（意向ロジック削除・部署名/雇用形態の現在値join追加）、`RenewalManagementTab.tsx`（意向トグル/KPI/一括ボタン削除、所属部署・雇用形態・書類種別の表示追加、ボタン文言統一）、3ダッシュボード・`lib/mail.ts`の追随修正。管理部・担当営業・SSCの3ダッシュボードすべてで実機確認済み（表示・差異確認パネル・ボタン文言とも正常）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-16参照。チャットB：④（12項目の差異確認拡張）・⑦（同一期間の安全警告）は実装・デプロイ・実機確認まで完了（2026-07-17）。⑥（原契約confirmation画面。モック確認済み）は実装完了・未デプロイ。あわせてチャットC（⑤一括申請）のうちUI部分（仕分けトグル・固定バー）も実装完了・未デプロイ。**2026-07-16 23:39 UTC〜Vercel障害（GitHubリンクのデプロイ・認証に影響）→ 復旧後さらに別のGitHub側障害でVercelの自動デプロイが停止、という2段階の障害が発生**したが、Git連携の切断・再接続、Vercelダッシュボードの「Create Deployment」による手動デプロイで2026-07-17に復旧・本番反映を確認。⑥・⑤（仕分けトグル・固定バー）とも実機確認済み（一括申請への切替・固定バーの集計連動・期間未確定行でのオプション無効化・⑥モーダルの表示、いずれも正常動作を確認）。【全チャット完了・実機確認済み・2026-07-17】⑤の契約データ生成処理（一括申請の実行本体。`renewalFieldMap.ts`新設・`executeBulkApply()`実装）とチャットD（個別申請：`/apply?renewal=`プリフィル＋最終確認直行）を実装・デプロイし、Claude in Chrome＋Supabase MCPで実際に一括申請・個別申請それぞれ1件ずつ最後まで（`contracts`への実データ保存・`renewal_candidates.status`の`'applied'`更新まで）通しで実機確認済み。実装過程で発見・修正した不具合3件（所定労働日数のCSV差異誤検知、staffテーブルに存在しない`department`列の直接SELECTによるスタッフ情報欠落、CSV数値項目の型不整合による個別申請プリフィル画面のクラッシュ）、および試用期間は前回値に関わらず更新時は必ず「無」にする方針決定（雇用期間内に収まる制約は`/apply`側で既存担保済みと確認）、タブ行の縦スクロールバー誤表示の原因（`overflow-x-auto`のみ指定時にUAが`overflow-y`もautoとして扱うCSS仕様）と修正も含め、詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17の一連のエントリ参照。これで更新期限管理タブ（チャットA〜D）は一通り完成。【完了・実機確認済み・2026-07-17】①更新期限管理経由の契約が既存の署名フロー（SSC承認→確認依頼メール→`/sign/[id]`）に問題なく乗ることを実地確認：チャットDで実際に個別申請した契約（阿部晃大・105404・パターンB）でSSC承認→メール到達（Gmail実機確認）→本人確認→内容確認完了までを通しで実行し、`status`が`申請中`→`署名待ち`→`署名済み`に正常遷移することを確認。新規実装は不要と判明し完了扱い。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。【実装完了・要デプロイ＋実機確認・2026-07-17】②部門マスタ・最低賃金/所定労働時間マスタ・労働者派遣料金額マスタの管理画面（タスク5番）：当初は新しいチャットでの着手を伊藤さんと合意していたが、伊藤さんの判断で同一チャット内で継続。管理部ダッシュボードに「マスタ管理」タブを新設（`app/dashboard/_shared/MasterManagementTab.tsx`＋`app/api/admin/master-data/route.ts`）。部門マスタは新規追加のみ（既存行の編集・削除は不可）、最低賃金・所定労働時間マスタは新規追加＋直近レコードの修正が可能、労働者派遣料金額マスタは伊藤さんのご要望で8営業所を全件表示し金額を入力する表形式（`getOfficeName()`と同じロジックで候補を固定・自由記述不可）で実装。あわせて`department_master`・`minimum_wage_master`・`standard_working_hours_master`のRLSを強化し、書き込み（INSERT/UPDATE）を管理部ロールのみに制限（従来は認証ユーザーなら誰でも書き込み可能だった）。【完了・実機確認済み・2026-07-17】デプロイ後、admin-testアカウントでClaude in Chrome＋Supabase MCPにより部門・最低賃金・所定労働時間・派遣料金額の4マスタすべての新規追加・修正・保存を実際に操作し、DBへの反映を確認（テストデータは確認後に削除）。派遣料金額マスタは実データ（本社¥24,219等）への書き込み経路のみ確認し値は変更していない。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-17参照。③Excel過去データの本番切り替え時取込は引き続き未着手のまま。【完了・実機確認済み・2026-07-15】指摘17（契約更新後に旧`renewal_candidates`行が重複表示）・指摘18（「更新しない」確定後に意向を戻してもstatusが戻らない）に対応。デプロイ後、実際にDBに存在していた重複行（社員番号105235）が管理部ダッシュボードを開いた瞬間の自動同期で1行に整理されること、意向を戻すとstatusが自動でpendingに戻ることをClaude in Chrome＋Supabase MCPで実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。
5. 部門マスタ（`department_master`）・最低賃金/所定労働時間マスタ・労働者派遣料金額マスタ（`dispatch_fee_master`。2026-07-08新設）の管理画面が無い（タスク17・18まとめて検討）
6. 【完了・2026-07-13】就業条件明示書（パターンB）単体の申請がSSC承認後「署名待ち」へ進まない不具合の修正：`notify-sign-request`の判定条件を修正（`closing_pattern`が`null`の場合も`'auto'`扱いに）。個別承認・一括承認どちらも解消。あわせて、この不具合調査の過程で「内容確認済みの日時がどこにも表示されない」という関連課題も見つかり、担当営業ダッシュボード（一覧・詳細）に`signed_at`（確認・署名日時）の表示を追加した（フェーズ1：可視化）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
7. スケーラビリティ改善タスク（優先順は9-1章9番参照）：①【完了・2026-07-14】DBインデックス追加（`created_by_dept_no`のみ抜けていた分を追加）。②【完了・実機確認済み・2026-07-14】全件取得の是正：`contracts.search_text`カラム追加＋バックフィル、`app/apply/page.tsx`保存時のsearch_text組み立て、共通フック`app/dashboard/_shared/useApprovedAccumulator.ts`を新設し、SSC・管理部（契約一覧／社内承認）・担当営業（完了タブ）の蓄積型タブに直近45日絞り込み＋「さらに読み込む」＋「全期間で検索」を実装。Vercel実機確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14参照。【完了・実機確認済み・2026-07-21】③`app/apply/page.tsx`分割：STEP1〜8の8ブロックを`_components/Step*.tsx`へ切り出し（ロジックは親に残しJSXのみ移動）。3,467行→1,955行に削減。Vercel実機で申請→完了→一覧→詳細→PDFまで一気通貫確認済み。【完了・実機確認済み・2026-07-21】④退職者除外ロジックのDB移行：3箇所の重複ロジックを`lib/staffFilters.ts`の`excludeRetiredStaffOr()`に集約し、SupabaseクエリのWHERE条件（DB側）で除外する方式に統一。実機確認済み。【方針の仮決めのみ・実装保留・2026-07-21】⑤`input_data`肥大化対策：対象を`input_data.csvMeta`のみに限定し保持期間2年を仮置きとする方針を記録（詳細・根拠は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照）。現状DB23MB/500MBで緊急性は低く、実際の削除（`csvMeta`キーを消す処理）は本稼働後の実データが貯まってから再検討・未実装のまま。ただし「月次で監視タスクを別途設定する」という当初案は、伊藤さんの指摘（Coworkを毎日使うわけではなく通知を見落とす）により撤回し、【完了・実機確認済み・2026-07-21】署名・確認完了時に`input_data.csvMeta`をGoogle Driveへ自動でバックアップ保存する仕組み（`lib/googleDrive.ts`の`uploadJsonBackup()`等・`contracts.csvmeta_backup_file_id`）と、管理部専用の復元ボタン（`app/api/admin/contracts/[id]/restore-csvmeta`・SSC/管理部契約詳細画面の「管理部専用メンテナンス操作」セクション。復元前にバックアップ日時を確認表示・`csvmeta_restored_at`/`csvmeta_restored_by`で監査記録）を実装。デプロイ後、実在スタッフ（105026・三國駿）のテスト署名で①Driveへのバックアップ作成②`csvmeta_backup_file_id`の記録を確認し、admin-testアカウントで復元ボタンの一連の流れ（バックアップ日時確認→復元実行→`input_data.csvMeta`・監査列の反映）まで実機確認済み。削除機能自体はまだ無いため、このバックアップは将来の削除に備えた事前準備という位置づけ。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-21参照。
8. 【完了・2026-07-14】STEP1のスタッフ検索を自部門のみに制限：`app/apply/page.tsx`の`handleSearch`に、担当営業ロールのみ`staff.dept_no`一致条件を追加（SSC・管理部は横断業務のため対象外と伊藤さんに確認済み）。STEP2の4CSVシステム検索はSTEP1で選択済みのスタッフをキーにしているため追加対応不要と確認済み。動作確認用に12部門分のダミー担当営業アカウントをSupabaseに新規作成し（ログイン情報は`ダミー担当営業アカウント一覧.xlsx`）、伊藤さんが実機で自部門制限の動作を確認済み。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14参照
9. 【絞り込み機能は完了・2026-07-14】SSC・管理部・担当営業の一覧タブに絞り込み・並び替え・検索を共通コンポーネントで追加済み。`取り下げ`ステータスの表示先は【優先度低・フェーズ5と合わせて】未着手のまま。
10. 【完了・2026-07-13】管理部ダッシュボードに、SSCが出来ることをすべて出来るようにする（フェーズ1）：「契約一覧」タブを新設し、SSCと同等の一覧表示・承認・強制承認・差し戻し・一括承認を実装。SSCの契約詳細画面を管理部にも開放する形で対応（新規の管理部専用画面は作らず既存画面を共有）。当初「閲覧専用」で出したところ伊藤さんから「SSCが出来ることは管理部も出来る」と指摘を受け、承認権限も含めて完全同等に訂正。Vercel実機確認済み。フェーズ2（`/apply`ロールゲート緩和）・フェーズ3（社内承認タブ）・フェーズ4（SSC代理承認）は未着手（タスク3番に引き続き記録）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-13参照
11. 社内システム担当者による総合レビュー（2026-07-14）の指摘40件（セキュリティ・データ整合性・件数表示・UX文言・パフォーマンス・ドキュメントのズレの6分野）。ファイル本体は`docs/reviews/総合レビュー指摘事項_2026-07-14.md`に保存済み。【完了・2026-07-15】①Supabase実環境のRLS・user_metadata確認（フェーズ0）と、それに続く認証統合フェーズ2.5・RLS強化（`staff_roles`テーブル新設・自己昇格防止トリガー・`contracts`/`renewal_candidates`/`requests`のRLSポリシーを部門・ロール基準に変更。DB側のみの変更のためVercel再デプロイ不要）。【完了・2026-07-15】②complete総当たり・メール失敗座礁・notify認可・PDF認証（指摘1・2・4・10）：`notify-sign-request`へのAuthorizationヘッダー必須化（呼び出し元6箇所を修正）、PDF取得APIへの二経路認証（署名画面用の短命トークン／社内ダッシュボード用のロール・部門チェック）、`complete` APIへの試行回数制限追加、メール送信失敗時に「SSC承認済み」へロールバックする処理を追加。**コード変更を伴うためVercel再デプロイが必要**。デプロイ後、Claude in Chrome＋Gmail連携で実機確認（PDFプレビュー・SSC承認〜通知メール送信・認証コードのメール到達）を実施し、いずれも正常動作を確認。この過程で①のRLS強化（フェーズ1）が原因の重大バグ（`contracts`のUPDATE＝SSC/管理部の承認操作が全件RLS違反で失敗）を発見・即修正済み（DB側のみ・再デプロイ不要）。【完了・2026-07-15】③一括承認の選択残留・二重承認ガード（指摘11・12）：上記「実装済み」欄参照。（※この時点では④表示・文言(指摘19・21〜24)・⑤ドキュメント修正(指摘37・38)を「残り・未着手」としていたが、同じ2026-07-15内で後述の通り実装・完了している。マスタ系テーブル（`minimum_wage_master`等）のRLS見直しのみ、マスタ管理画面着手時に合わせて検討という位置づけで未着手のまま（→2026-07-17のマスタ管理タブ実装時に`department_master`と同様`minimum_wage_master`・`standard_working_hours_master`のINSERT/UPDATEを管理部ロール限定化済み、`dispatch_fee_master`はRLSポリシー自体を設けずservice-role専用のAPI経由アクセスのみに限定する設計とし、2026-07-17に実データで再検証・完了確認済み）。）**2026-07-15の再監査で判明した、これまで残タスク一覧から漏れていた指摘**：指摘20（依頼一覧の`limit(500)`で古い未対応依頼が永久に見えなくなる。19と同じ「件数・表示ロジックバグ」の部類なのに④に入れ忘れていた）、指摘7・8・9（認証コードが`Math.random()`・reissue APIの悪用余地・署名画像の未検証。セキュリティ第1部のうち1・2・4・10だけ対応し、この3件は一度も残タスクに載せていなかった）。伊藤さんへ次の着手候補として①更新期限管理フェーズ2（45/30/20/14/7日自動メール通知。タスク4番の残タスクの中でも法務リスクが最大で伊藤さんが2026-07-14に最優先と決定済み）②指摘20・19の件数表示バグ③指摘19・21・22・23・24の表示文言＋37・38のドキュメント修正④指摘7・8・9のセキュリティ拾い残し、の4案を提示済み。着手順は伊藤さんが選定中（次回セッションで確定）。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-14・2026-07-15参照。【完了・2026-07-15（コード修正のみ・未デプロイ）】④のうち指摘7・8・9（セキュリティ拾い残し）に対応：指摘7＝`lib/signAuthCode.ts`の認証コード生成を`Math.random()`→Node標準`crypto.randomInt()`に変更。指摘8＝`app/api/sign/[id]/reissue/route.ts`に再発行クールダウン（3分）を追加し、有効なコードが残っている間は試行回数のリセットも行わないよう変更（5回試行→再発行→再試行での回数制限迂回を防止）。指摘9＝`app/api/sign/[id]/complete/route.ts`に署名画像（丸印鑑PNG）のdata URL形式・サイズ上限（500KB）チェックと、画面入力のフルネーム（`sealName`）とスタッフマスタ氏名の一致検証を追加（`app/sign/[id]/page.tsx`から`sealName`を送信するよう変更）。【完了・実機確認済み・2026-07-15】デプロイ後、初回ビルドが型エラーで失敗（`admin/page.tsx`の「依頼管理」タブの未定義`resetFilters`参照。今回の変更とは無関係の既存不具合）→修正し再デプロイで解消。Claude in Chrome＋Supabase MCPを使い、テスト契約（永井優大様）で①reissueクールダウン（連続2回で1回目成功・2回目429）②氏名不一致ガード（400エラー・試行回数は消費されない）③画像サイズ上限（400エラー）④正常系の署名完了（`/sign/[id]`から最後まで進み、DB上`署名済み`・`drive_file_id`記録まで確認）をすべて実機確認済み。19・20・21〜24・37・38・7・8・9すべて完了済み。【完了・コード修正のみ・未デプロイ・2026-07-15】指摘13・14・15・16（データ整合性の小粒不具合4件）に対応：指摘13＝CSV再検索時に`setCsvSelectedId(null)`が漏れていたのを追加。指摘14＝差し戻し警告の比較関数`normalizeFields()`に`monthlyStandardHours`キーが無くキー構成不一致で警告が構造的に一度も出ない不具合を修正。指摘15＝再申請保存のUPDATEに`.eq('status','差し戻し中')`の条件付き更新を追加し、0件更新時は専用エラー表示。指摘16＝`lib/googleDrive.ts`に`deleteDriveFile()`を新設し、complete API のUPDATE失敗・空振り時にアップロード済みPDFを削除しDrive孤児ファイルを防止。ローカルbashサンドボックスのtsc実行は今回も反映遅延で信頼できず（従来からの既知の制約）、次回のVercelビルドログと実機確認（CSV再検索・差し戻し再申請・同時承認・署名完了）で最終確認が必要。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。【完了・DB側のみ・要Vercelデプロイ・2026-07-15】指摘31（更新期限管理syncCandidatesの全件取得）対応：Supabase側にDB関数`get_latest_genba_contracts_for_renewal()`（DISTINCT ONでスタッフごとの最新1件に絞り込み、必要な列だけを返す）を新設し、`useRenewalCandidates.ts`の`syncCandidates()`をこの関数呼び出しに変更。RLSは呼び出しロールのものがそのまま適用されることを模擬セッションで確認済み。コード変更を伴うためVercel再デプロイと実機確認が必要。詳細は`docs/SYSTEM_DESIGN.md`10章2026-07-15参照。

**教訓**：このCLAUDE.mdの一覧が`SYSTEM_DESIGN.md`9-1章の実際のステータスと2回連続でズレていた（完了済みタスクが「次の優先タスク」に残り続けていた）。今後、この一覧を更新する際は必ず9-1章の該当項目を番号ごと確認してから転記すること。

**ドキュメント整備TODO**：【完了・2026-07-17再確認】`contracts`・`company_master` の正式DDLは2026-06-30に取得済みで`docs/SYSTEM_DESIGN.md` 3-6/3-7は既に確定版に差し替え済み（10章2026-06-30参照）。本行はその後も長らく未完了として残っていたが、総合レビュー指摘39（DDL取得状況の矛盾）の再検証で実態と食い違っていたことが判明したため訂正した。

---

*詳細・経緯・差分監査表・全PENDINGは `docs/SYSTEM_DESIGN.md` を参照。*
