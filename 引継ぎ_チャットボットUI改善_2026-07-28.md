# 引継ぎメモ（2026-07-28作成・新チャット用）

このチャットはここで区切り、続きを新しいチャットで行う。新チャットの冒頭でこのファイルを読み込めば、
ゼロから調査し直さず続きに着手できる。CLAUDE.md・docs/SYSTEM_DESIGN.mdは新チャットでも自動読み込みされる
前提。本メモはその上に乗る「今回チャットの作業メモ」。

このチャットで行ったのは大きく2件：①申請者表示の変更（完了・実機確認済み）、②チャットボット（FAQ検索型）の
新規実装（コード実装済み・DB未反映・UI改善は方向確認まで済み実装は未着手）。①はもう触らなくてよい。
②の続きから着手すること。

---

## ①申請者表示を「部門名 氏名」に変更 → 完了・対応不要

`app/dashboard/_shared/useDeptNameMap.ts`を新設し、契約(contracts)・誓約書(pledges)の一覧/詳細6箇所、
依頼(requests)の表示順入替2箇所、計8箇所を実装・デプロイ・Claude in Chromeでの実機確認まで完了済み。
副次対応として、CLAUDE.md・docs/SYSTEM_DESIGN.md冒頭の「テストユーザー」記載が古いまま（`sales-test@appart.co.jp`）
だった点も発見・訂正済み（担当営業役の実際のテストログインは`ito+demo@appart.co.jp`＋`Test1234!`）。
このテーマは完了。新チャットでは触らなくてよい。

---

## ②チャットボット（FAQ検索型・無料）

### 進捗ステータス
1. FAQ設計（6段階プロセス：行動分析→困りポイント→想定問い合わせ→分類→カテゴリ設計→FAQ本文）→**完了**。
   成果物：`docs/chatbot_faq_design.md`（大カテゴリ8・実データ36件のFAQ本文まで作成済み）。
2. DBスキーマ・RLS設計＋シードSQL作成 → **完了**（ファイルは用意済み、**Supabaseへの実行は未実施**）。
3. 共通ウィジェットコンポーネント・管理部「FAQ管理」タブの実装 → **完了**（コードは書き上げてVercelにデプロイ済み）。
4. デプロイ後の実機セルフチェック → **着手したが、SQL未実行のためFAQ機能自体が動作しない状態で止まっている**。
5. UI/UXの作り直し → **伊藤さんとの相談の結果、方向性は合意済み。実装はまだ着手していない**。

### 次チャットで最初にやること（優先順）
1. **Supabaseへのマイグレーション実行を伊藤さんに依頼する**（まだなら）。手順は下記「SQL実行手順」参照。
   これをやらない限り、チャットボットの検索も質問送信もFAQ管理タブも一切動かない
   （実機確認時に`faq_entries`への問い合わせが404になることを確認済み＝テーブル未作成）。
2. 下記「UI改善の実装内容（承認済み・未着手）」を`ChatbotWidget.tsx`に反映する。
3. FAQ本文（36件）の改行位置を手直しする（`docs/chatbot_faq_design.md` 6章と `docs/sql/2026-07-28_chatbot_faq.sql`
   の両方を合わせて修正する。詳細は下記）。
4. git push案内（CLAUDE.mdルール14に従い、Claude自身はpushしない。VSCodeターミナルから伊藤さんに実行してもらう）。
5. デプロイ後、Claude in Chromeで最初から通しで実機確認する（検索・カテゴリ・回答表示・質問送信・管理部での
   回答→FAQ自動反映、まで）。

---

### 実装済みファイル一覧（今回のチャットで新規作成・変更）

**新規作成**
- `docs/chatbot_faq_design.md`：FAQ設計書本体（①〜⑥の全プロセス・カテゴリ・FAQ本文36件）
- `docs/sql/2026-07-28_chatbot_faq.sql`：DBマイグレーション（テーブル作成・RLS・シード36件）。**まだSupabaseで
  実行されていない**。実行手順は下記。
- `lib/faq.ts`：型定義（`FaqEntry`・`FaqInquiry`）、大カテゴリ一覧（`FAQ_MAJOR_CATEGORIES`）、検索ロジック
  （`searchFaqEntries`・`filterFaqByMajorCategory`・`getRelatedEntries`）
- `app/dashboard/_shared/ChatbotWidget.tsx`：ヘッダー用の共通ウィジェット本体。**今回のUI改善はこのファイルを
  作り直す**。
- `app/dashboard/_shared/FaqManagementTab.tsx`：管理部ダッシュボード専用の「FAQ管理」タブ（未回答質問一覧・
  回答フォーム・回答済み一覧・登録済みFAQの編集/削除）

**既存ファイルへの追記**
- `app/dashboard/admin/page.tsx`：`TabType`に`'faq'`追加、タブ一覧に「FAQ管理」追加（バッジ件数は
  `faqUnansweredCount`。`faq_inquiries`のstatus='unanswered'件数を取得する`useEffect`を新設）、
  `{activeTab === 'faq' && <FaqManagementTab />}`のレンダー追加、ヘッダーに`<ChatbotWidget />`追加
  （`NewDocumentMenu`と`LoggedInUserChip`の間）
- `app/dashboard/ssc/page.tsx`：ヘッダーに`<ChatbotWidget />`追加（同じ位置）
- `app/dashboard/sales/page.tsx`：ヘッダーに`<ChatbotWidget />`追加（同じ位置）

### DB設計の要点（`docs/sql/2026-07-28_chatbot_faq.sql`の中身）
- `faq_entries`：id・major_category（大カテゴリコードA〜H）・major_label・minor_category（中カテゴリコード。
  一意制約あり）・question（一覧表示用タイトル）・keywords（別の言い回し。スペース区切り）・answer・
  related_labels（関連質問のminor_categoryカンマ区切り）・escalation_note・sort_order・source
  （'initial'|'user_submitted'）
- `faq_inquiries`：question_text・submitted_by（auth.users参照）・submitted_by_email・submitted_by_role・
  submitted_at・status（'unanswered'|'answered'）・answer_text・answered_by・answered_by_email・
  answered_at・created_faq_id（faq_entries参照）
- RLS：`faq_entries`はSELECTを認証済み全員に許可、INSERT/UPDATE/DELETEは`current_role_name() = '管理部'`のみ
  （`current_role_name()`は2026-07-15に作成済みの既存SECURITY DEFINER関数を再利用）。`faq_inquiries`はINSERTを
  全員に許可、SELECTは本人or管理部、UPDATEは管理部のみ。
- 書き込みはAPIルートを経由せず、クライアントから直接Supabaseへ（RLSで保護）。他の管理タブ（マスタ管理等）は
  APIルート経由だが、FAQは2テーブルのみのシンプルな構成のためAPIルートを新設しない設計にした（意図的な判断）。

### SQL実行手順（伊藤さんに依頼する内容。前回チャットで一度案内済みだが、まだ完了していない可能性が高い）
1. VSCodeで`docs\sql\2026-07-28_chatbot_faq.sql`を開き、全文コピー。
2. https://supabase.com/dashboard を開き、プロジェクト「argpiiznuzxmmqraynfo」を選択。
3. 左メニュー「SQL Editor」→「New query」→貼り付け→「Run」。
4. 「Table Editor」→`faq_entries`を開き、36件入っていることを確認。
5. 実行後、チャットボットのアイコンを開いて「FAQの読み込みに失敗しました」が出なくなることを確認する
   （前回チャットで確認した404エラーの原因はこれ）。

---

### UI改善の実装内容（伊藤さんと合意済み・実装はまだ着手していない）

伊藤さんに実機で見せたところ、下記3点の指摘を受けた（参考画像：TOKIUMサポートのチャットウィジェット）。
`mcp__visualize`でモックアップを2回提示し、方向性の合意を得た（実装はまだ）。

**指摘①：カテゴリ選択が横スクロールになっていて使いにくい**
- 原因：`ChatbotWidget.tsx`の`chipBase`まわり、`overflow-x-auto`で横一列にチップを並べていた
  （`FAQ_MAJOR_CATEGORIES.map`のチップ行）。
- 対応方針：横スクロールのチップ行を廃止し、**8大カテゴリ全部を2列カードグリッド**（アイコン＋ラベル、
  `grid-template-columns: 1fr 1fr`で自然に折り返す）に変更する。8個なら4行に収まり、横スクロールは発生しない
  （縦方向の自然なスクロールは問題ない。あくまで横スクロールが問題視されていた）。

**指摘②：回答文の改行位置が読みにくい**
- 原因：`answer`を`whitespace-pre-line`で1つの長い段落としてそのまま表示していた。CLAUDE.mdルール9
  （意味の切れ目で改行する）を、FAQ本文を書いた時点で徹底できていなかった。
- 対応方針：`docs/chatbot_faq_design.md`6章の36件の回答文、および`docs/sql/2026-07-28_chatbot_faq.sql`の
  シードSQLの`answer`列を、文の切れ目（句点）ごとに改行を入れる形に書き直す。**この2つのファイルは内容を
  一致させること**（SQLはまだ未実行なので、実行前に修正してしまえば手戻りなし）。行間も広げる
  （`line-height`を今より大きく）。

**指摘③：全体的なUIがダサい・チャットらしい温度感がない**
- 対応方針（モックアップで合意済み）：
  - ヘッダーバーをオレンジ（`#F59E42`。チャットアイコンと同じ色）の帯にする。
  - 回答表示は「戻る」をテキストリンクではなく、ヘッダー内の`←`アイコン（chevron-left）に変更する。
  - 質問一覧は現状の白ボタン羅列から、TOKIUMのようなフラットな行リスト＋右端にchevron-rightアイコン、に変更。
  - 回答表示は、ボットのアバターアイコン（小さい丸、オレンジ系）＋吹き出し風のグレー背景ブロック、に変更。
  - 伊藤さんが保存した`public/icons/chatbot-icon.png`を、ヘッダーの起動ボタンだけでなく、パネル内の
    アバターアイコンとしても使い回すかどうかは次チャットで決める（モックアップでは仮の線アイコンを使用した）。
    伊藤さんに確認するか、Claude側で「同じ画像を使い回す」で決め打ちしてよいかは要判断（本人にまだ確認していない）。

**指摘④（伊藤さんからの追加の鋭い指摘）：エスカレーション注記が「管理部に相談してください」という
説明文で終わっていて、実際に相談する手段がその場にない**
- 原因：現状の設計では「質問を送る」フォームは検索結果が0件のときにしか出現しない。エスカレーション注記
  （`escalation_note`）はただの説明テキストとして表示されるだけで、そこから実際に問い合わせる導線が無かった。
- 対応方針（モックアップで合意済み）：
  1. **「質問を送る」入力欄をパネル下部に常時表示**する（TOKIUMの下部入力バーと同じ考え方）。今の実装のように
    検索0件時だけ条件表示にするのをやめる。
  2. エスカレーション注記がある回答には「この件で質問を送る」ボタンを添え、押すと常時入力欄にフォーカスが
    移る（できれば「〔A1 ログインできない〕の件で」のように、どのFAQを見ていたかの文脈を入力欄に
    自動で頭出しできるとなお良い。これは実装時の詳細検討でよい）。
  3. 旧来の「検索0件のときだけ出す送信フォーム」のロジック（`showSendQuestion`まわり）は、この常時入力欄に
    統合し、二重実装にしない。

### 実装時に変更するファイル（次チャットでの作業対象）
- `app/dashboard/_shared/ChatbotWidget.tsx`：上記①〜④を反映して全面的に書き直す。
- `docs/chatbot_faq_design.md`：6章のFAQ本文36件の改行位置を修正。
- `docs/sql/2026-07-28_chatbot_faq.sql`：シードSQLの`answer`列を同じ内容に修正（まだ未実行なので、実行前に
  直せば良い。もし伊藤さんが先にSQLを実行してしまっていた場合は、修正後に`update`文で該当行を上書きする
  対応に切り替えること）。

### その他の申し送り
- `FaqManagementTab.tsx`（管理部の回答フォーム）は今回のUI改善の対象外（伊藤さんからの指摘は利用者側の
  チャットウィジェットに関するものだったため）。ただし、回答フォームで新規登録するFAQの`answer`も、
  今後は改行を意識した文章にするよう伊藤さんに一言添えるとよいかもしれない（必須ではない）。
- CLAUDE.mdルール12（見た目の修正は実装前に方向確認）に従い、今回は`mcp__visualize`でモックアップを2回
  提示し、伊藤さんの承認（「OK」）を得てから本メモの作成に進んだ。実装自体はまだ行っていないので、
  次チャットで①のSQL実行状況を確認した上で、②のUI改善実装に着手してよい。
- CLAUDE.mdルール14（git push案内）に従い、実装後は`git add`・`git commit`・`git push`のコマンドを
  伊藤さんに提示すること（Claude自身はpushしない）。
- CLAUDE.mdルール13（デプロイ後の実機セルフチェック）に従い、デプロイ後はClaude in Chromeで
  ①チャットボットのカテゴリグリッドが横スクロールしないこと、②回答文の改行が読みやすいこと、
  ③常時入力欄から質問を送れること、④エスカレーション注記のボタンが機能すること、⑤管理部の
  「FAQ管理」タブで未回答質問に回答するとFAQへ反映され、チャットボット側の検索で即座にヒットするようになる
  こと、を一通り確認すること。

### 今回のチャットのタスクリスト状況（参考。新チャットではTaskListが空になっている可能性が高いので、
必要なら以下を参考に作り直す）
1. 申請者表示を「部門名 氏名」に変更（実装）→完了
2. チャットボット：従業員側リサーチ再実施→完了
3. チャットボット：社内ユーザー側リサーチ→完了
4. チャットボット：4ロールの行動・心理分析→完了
5. チャットボット：想定問い合わせ洗い出し・分類→完了
6. チャットボット：カテゴリ設計・FAQ本文作成→完了
7. デプロイ後の実機セルフチェック（申請者表示）→完了
8. 担当営業側の実機セルフチェック（申請者表示）→完了
9. チャットボット：DBスキーマ・RLS設計→完了（ファイル作成のみ。実行は未）
10. チャットボット：FAQシードデータSQL作成→完了（改行修正が必要。上記参照）
11. チャットボット：共通ウィジェットコンポーネント実装→完了（ただし今回のUI改善で作り直す）
12. チャットボット：3ダッシュボードへの組み込み→完了
13. チャットボット：管理部「FAQ管理」タブ実装→完了
14. チャットボット：SQL実行案内・git push案内→完了（案内はしたが、伊藤さんの実行完了は未確認）
15. チャットボット：デプロイ後の実機セルフチェック→**未完了・SQL未実行のため中断中**。UI改善実装後に
    最初からやり直すこと。
